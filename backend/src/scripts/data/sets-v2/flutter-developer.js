module.exports = {
  category: 'Flutter Developer',
  sets: {
    1: [
      {
        question: 'A parent rebuild supplies a new widget instance with the same runtimeType and key as the previous one. What happens to the existing State object of that StatefulWidget?',
        options: [
          'didUpdateWidget is called with the old widget after the widget field is swapped, then build runs; initState is not called again',
          'The State is disposed and a fresh State is created via createState because the widget instance changed',
          'didChangeDependencies is invoked first, before the widget field is updated, then build runs',
          'The element is deactivated and reactivated, so deactivate and activate run before the next build',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Which statement about the widget, element, and render trees is accurate?',
        options: [
          'Every Element owns exactly one RenderObject, created during mount',
          'StatelessWidget subclasses create RenderObjects lazily on their first paint',
          'Only RenderObjectElements create RenderObjects; component elements for Stateless and Stateful widgets never own one',
          'The render tree is discarded and rebuilt from scratch whenever setState marks an element dirty',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'You move a subtree whose root StatefulWidget has a GlobalKey to a different parent elsewhere in the tree within a single frame. What happens?',
        options: [
          'The State is disposed and recreated because the element could not stay under the same parent',
          'The element is grafted to the new location; the State object survives and initState does not run again',
          'Flutter throws immediately because elements holding GlobalKeys cannot change parents',
          'The State survives only if the old and new parents share the same RenderObject type',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Column shows a list of stateful tiles built without any keys. The first item is removed from the data list and the UI rebuilds. What is the resulting State pairing?',
        options: [
          'The last tile State is disposed; every remaining tile keeps its previous State, so state is now paired with the wrong item',
          'The first tile State is disposed and all other tiles keep the correct pairing',
          'All tile States are disposed because the child count changed between frames',
          'Flutter detects the data shift and re-runs initState on every remaining tile',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Widget build(BuildContext context) {\n  return Scaffold(\n    drawer: Drawer(),\n    body: TextButton(\n      onPressed: () => Scaffold.of(context).openDrawer(),\n      child: Text("open"),\n    ),\n  );\n}\nTapping the button throws. Why?',
        options: [
          'openDrawer must only be called from a post-frame callback, never from a gesture handler',
          'Scaffold.of requires a ScaffoldMessenger ancestor, which MaterialApp did not provide here',
          'The Drawer must be given a GlobalKey before it can be opened programmatically',
          'The context passed to Scaffold.of sits above the Scaffold being created, so the lookup fails; wrapping the button in a Builder fixes it',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Calling context.watch<T>() inside initState throws a FlutterError. What is the underlying reason?',
        options: [
          'Provider instances are registered only after the first frame, so nothing exists to watch yet',
          'watch returns a Future during initialization and initState cannot await it',
          'Inherited widget dependencies cannot be registered from initState; the framework only allows it during build or didChangeDependencies',
          'watch requires a ChangeNotifier, and ChangeNotifier subscriptions are illegal before the element is mounted twice',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
      points: 1,
      },
      {
        question: 'In Riverpod, a FutureProvider is declared with autoDispose. When exactly is its cached state destroyed?',
        options: [
          'At the end of every frame, regardless of listeners, unless keepAlive was called',
          'When its last listener is removed and no keepAlive link is active, the state is disposed shortly after',
          'Only when the ProviderContainer itself is disposed at app shutdown',
          'As soon as the Future completes, so the next read always refetches',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Inside a Bloc on<MyEvent> handler you store the Emitter in a field and call it from a Timer that fires after the handler has returned. What happens?',
        options: [
          'The state is emitted normally because the Emitter remains valid for the lifetime of the Bloc',
          'The emission is silently dropped and a debug warning is printed',
          'The Bloc re-dispatches the original event so the handler can run again',
          'A StateError is thrown because emit was called after the event handler completed',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A spawned isolate computes a very large object graph and must hand it back to the main isolate. Which approach avoids the deep copy that SendPort.send normally performs?',
        options: [
          'Call Isolate.exit(sendPort, result), which transfers ownership of the result to the receiver as the isolate terminates',
          'Wrap the result in a Completer and complete it across the isolate boundary',
          'Mark the result class as immutable so the VM shares it automatically over send',
          'Send the object through a broadcast StreamController shared by the isolates',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'void main() {\n  scheduleMicrotask(() => print("m1"));\n  Future(() => print("f1"));\n  Future.microtask(() => print("m2"));\n  print("s");\n}\nWhat is the output order?',
        options: [
          'm1, m2, f1, s',
          's, f1, m1, m2',
          's, m1, m2, f1',
          's, m2, m1, f1',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'void main() {\n  Future.value(1).then(print);\n  print(2);\n}\nWhat prints, and why?',
        options: [
          '1 then 2, because the future is already complete so then runs synchronously',
          '2 then 1, because then callbacks are always scheduled as microtasks even on an already-completed future',
          '2 then 1, because Future.value defers completion to the next event-loop turn',
          '1 then 2, because print inside then is hoisted ahead of the synchronous print',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Events are added to a non-broadcast StreamController before anyone calls listen. What happens to those events?',
        options: [
          'They are buffered by the controller and delivered in order once a listener subscribes',
          'They are dropped because a stream without a listener discards events',
          'The controller throws a StateError on the first add without a listener',
          'They are delivered synchronously to the first listener during the listen call itself',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'class A { String? name; }\nvoid f(A a) {\n  if (a.name != null) print(a.name.length);\n}\nThis fails to compile even though name was null-checked. Why?',
        options: [
          'The null check only promotes types inside a switch statement, not an if statement',
          'String? cannot be promoted at all; only non-nullable generics promote',
          'Promotion requires the field to be declared late as well as nullable',
          'Public instance fields never promote because a getter could return a different value between reads; copy it to a local variable first',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What does identical(const SizedBox(width: 8), const SizedBox(width: 8)) evaluate to, and why?',
        options: [
          'false, because each const invocation allocates a distinct object at runtime',
          'false, because identical compares element identity, not widget identity',
          'true, because compile-time const instances with equal fields are canonicalized to a single object',
          'true, but only in debug mode where the VM interns widgets aggressively',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Container(width: 100, height: 100, color: Colors.red) is the entire body of a Scaffold filling the screen with tight constraints. What size does it render at?',
        options: [
          'Exactly 100x100, centered by default in the available space',
          'The full screen size, because tight incoming constraints override the requested width and height',
          'Exactly 100x100, aligned to the top-left corner of the body',
          'It throws a constraints violation error in debug mode',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A ListView with default settings is placed directly inside a Column. What happens at layout time?',
        options: [
          'It throws: the viewport receives unbounded height because Column offers its children unconstrained vertical space',
          'The ListView silently sizes itself to the total height of its children',
          'The Column clips the ListView to the screen height and layout succeeds',
          'The ListView renders only its first visible child and disables scrolling',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Why is wrapping every row of a long list in IntrinsicHeight considered a performance hazard?',
        options: [
          'IntrinsicHeight forces its child onto its own compositing layer, exhausting GPU memory',
          'It disables RepaintBoundary insertion for the whole list',
          'It makes every row participate in hit testing even when off-screen',
          'It triggers a speculative intrinsic measurement pass before real layout, and nested intrinsics can degrade toward quadratic layout cost',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A State mixes in SingleTickerProviderStateMixin and creates two AnimationControllers with vsync: this. What is the result?',
        options: [
          'The controllers share one Ticker and advance in perfect lockstep',
          'The second controller runs at half the frame rate of the first',
          'Creating the second controller fails an assertion because the mixin can vend only a single Ticker; TickerProviderStateMixin is required',
          'The second controller silently never ticks until the first is disposed',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What is the purpose of the child parameter on AnimatedBuilder (or ListenableBuilder)?',
        options: [
          'It declares which widget the animation is applied to so transforms can target it',
          'It is built once, cached, and passed into the builder on every tick, so that subtree is not rebuilt 60 times per second',
          'It provides the initial frame shown before the controller starts',
          'It is required so the builder can compute layout constraints for the animation',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Which value will fail to encode when sent over a MethodChannel using the default StandardMessageCodec?',
        options: [
          'A DateTime instance',
          'A Uint8List of ten thousand bytes',
          'A Map with String keys and nested List values',
          'A double with a NaN value',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Dart calls invokeMethod on a MethodChannel. On Android, on which thread does the registered MethodCallHandler execute by default?',
        options: [
          'A dedicated binder thread from the channel thread pool',
          'The Dart UI thread, shared with the Flutter engine',
          'A random worker thread chosen by the messenger per call',
          'The Android platform main (UI) thread',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A small continuously animating spinner causes the entire screen to repaint every frame. What is the idiomatic fix?',
        options: [
          'Move the spinner into an Overlay entry so it paints in a different tree',
          'Replace the spinner animation with an implicit AnimatedOpacity',
          'Wrap the animating widget in a RepaintBoundary so its repaints are isolated to its own layer',
          'Call setState less frequently by throttling the ticker to 30fps',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Why are frame timings collected in debug mode unreliable for diagnosing jank?',
        options: [
          'Debug mode caps the refresh rate at 30fps so all frames look slow',
          'Debug builds run JIT-compiled code with asserts and service extensions enabled; only profile mode reflects release performance',
          'Debug mode disables the raster thread entirely, merging it with the UI thread',
          'The timeline is sampled only once per second in debug mode',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In the Navigator 2.0 Router API, which component is responsible for converting incoming RouteInformation into your typed app-state configuration?',
        options: [
          'The RouteInformationParser, via parseRouteInformation',
          'The RouterDelegate, via its build method',
          'The BackButtonDispatcher, via its callback chain',
          'The TransitionDelegate, via resolve',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Using the declarative pages API, you rebuild the Navigator with the topmost Page removed from the pages list. What does the user see?',
        options: [
          'The route vanishes instantly with no animation because it was removed declaratively',
          'An assertion fails: pages can only grow, never shrink, between builds',
          'The route below is rebuilt from scratch, losing its state',
          'The corresponding route is popped and its exit transition animation plays',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A widget test calls tester.pumpAndSettle() while an indeterminate CircularProgressIndicator is on screen. What happens?',
        options: [
          'It returns after one frame because the indicator uses a repeating ticker that counts as settled',
          'The indicator is automatically paused so the call returns immediately',
          'It throws after the timeout, because the repeating animation schedules frames forever and never settles',
          'It waits for the real wall-clock timeout of the test runner, then passes',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Inside testWidgets, a Future.delayed(Duration(seconds: 1)) never completes even though the test keeps running. Why?',
        options: [
          'The test binding cancels all timers created outside pumpWidget',
          'testWidgets runs inside a fake-async zone; timers only fire when pump or pumpAndSettle advances the fake clock',
          'Future.delayed is compiled out in test builds for determinism',
          'The default test timeout of one second cancels the pending future first',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Future<void> load() async {\n  final data = await fetch();\n  setState(() => items = data);\n}\nThis intermittently throws "setState() called after dispose()". What is the correct guard?',
        options: [
          'Check the mounted property after the await and skip setState when the State is no longer in the tree',
          'Wrap setState in a try-catch and ignore the FlutterError',
          'Move the fetch call into didChangeDependencies where disposal cannot happen',
          'Use a synchronous fetch so no gap exists between check and use',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Besides running once immediately after initState, when else does didChangeDependencies fire on a State?',
        options: [
          'Every time setState is called on that State',
          'Whenever the parent rebuilds with a new widget configuration',
          'Whenever the keyboard visibility or window insets change',
          'Whenever an InheritedWidget the State depends on notifies a change',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'AnimatedSwitcher(duration: d, child: Text("$count")) shows no transition when count changes. Why not?',
        options: [
          'AnimatedSwitcher only animates when the child runtimeType changes, so Text can never transition',
          'The duration applies only to the outgoing child, and there is none on first change',
          'The new Text has the same runtimeType and key as the old one, so it is treated as the same widget; give each child a ValueKey(count)',
          'Text is a RepaintBoundary internally, which suppresses the switcher animation',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
    ],
    2: [
      {
        question: 'A widget calls setState synchronously inside its own build method. What does the framework do?',
        options: [
          'It queues the rebuild for the next frame and continues normally',
          'It throws: setState or markNeedsBuild is not allowed while the framework is already building that widget',
          'It rebuilds the widget twice in the same frame, once per call',
          'It silently ignores the call because the element is already dirty',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'An element holding a GlobalKey is moved to a new tree location within one frame. Which State lifecycle callbacks fire during the move?',
        options: [
          'dispose on the old location followed by initState at the new location',
          'Only didUpdateWidget, since the widget configuration is unchanged',
          'No callbacks at all; the move is invisible to the State',
          'deactivate when detached, then activate when reattached; dispose is never called',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'final list = const [1, 2];\nlist.add(3);\nWhat happens when this Dart code runs?',
        options: [
          'An UnsupportedError is thrown at runtime; assigning a const list to a non-const variable compiles fine but the list stays immutable',
          'It fails to compile because add is not defined on a const List',
          'The list becomes [1, 2, 3] since the variable itself is not const',
          'A copy-on-write occurs and list now points to a fresh growable list',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Future<void> f() async {\n  print("a");\n  await null;\n  print("b");\n}\nvoid main() {\n  f();\n  print("c");\n}\nWhat is the output order?',
        options: [
          'a, b, c',
          'c, a, b',
          'a, c, b',
          'c, b, a',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A broadcast StreamController emits three events, and only afterwards does a widget subscribe with await for. What does the subscriber receive?',
        options: [
          'All three events, replayed from an internal buffer in order',
          'Only events emitted after its subscription begins; the earlier three are lost because broadcast streams do not buffer',
          'The most recent event immediately, then subsequent events',
          'A StateError, because broadcast streams forbid late subscriptions',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'class C {\n  late final int x = _compute();\n  int _compute() { print("computed"); return 1; }\n}\nWhen does "computed" print?',
        options: [
          'During construction of C, since field initializers run in the constructor',
          'Never, unless x is explicitly assigned first',
          'At class load time, once per isolate',
          'On the first read of x; the late initializer is deferred and runs at most once',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A parent rebuilds and provides a child widget that is the identical const instance as before. Why can the framework skip rebuilding that child subtree?',
        options: [
          'Element.update short-circuits when identical(oldWidget, newWidget) is true, so the child element is left entirely untouched',
          'const widgets are compiled into the render tree and never participate in rebuilds',
          'The framework diffs the props of the two widgets and finds no changes',
          'const widgets register themselves as RepaintBoundaries, which blocks rebuild propagation',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Row(children: [TextField()]) throws at layout time. What is the root cause?',
        options: [
          'TextField requires a Material ancestor that Row fails to provide',
          'Row lays out children before text scale is known, so text measurement fails',
          'Row gives its non-flex children unbounded horizontal constraints, and TextField tries to expand to max width, which is infinite; wrap it in Expanded or Flexible',
          'TextField needs a focus scope that Row strips from its children',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Container requesting width 300 is placed inside a SizedBox(width: 200, height: 200). How wide does the Container render?',
        options: [
          '300 wide, overflowing the SizedBox with a striped warning in debug',
          '200 wide, because the incoming max-width constraint clamps the requested size',
          '250 wide, splitting the difference per the flex resolution algorithm',
          '0 wide, because conflicting sizes collapse to the minimum constraint',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Inside build you call context.size to decide which layout to return. What is wrong with this?',
        options: [
          'Nothing, as long as the widget is a RenderObjectWidget',
          'context.size returns logical pixels but build works in physical pixels',
          'It only fails for stateless widgets; stateful widgets can read size in build',
          'Size is not available during build; it asserts because layout has not happened yet. Use LayoutBuilder to react to constraints instead',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What does wrapping a subtree in TickerMode(enabled: false) do to AnimationControllers vsynced inside it?',
        options: [
          'Their tickers are muted, so animation callbacks stop firing while the subtree is disabled, and resume when re-enabled',
          'The controllers are disposed and must be recreated when re-enabled',
          'Their values are reset to the lower bound immediately',
          'Nothing; TickerMode affects only implicit animations, not controllers',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'An AnimationController completes controller.reverse() all the way back. What AnimationStatus do status listeners receive at the end?',
        options: [
          'AnimationStatus.completed',
          'AnimationStatus.reverse',
          'AnimationStatus.dismissed',
          'AnimationStatus.forward',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Two Hero widgets on the same route are accidentally given the same tag, and a navigation begins. What happens?',
        options: [
          'The framework animates whichever hero was mounted first and ignores the other',
          'An assertion fails during the hero flight because each tag must be unique among heroes on a route',
          'The heroes are merged into one flight with an averaged flight path',
          'The navigation completes with no hero animation and no error',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Dart calls invokeMethod("scan") but no native handler on the channel implements that method (the native side calls result.notImplemented or nothing is registered). What does the Dart future do?',
        options: [
          'It completes with null so callers can treat it as unsupported',
          'It never completes, leaking the awaiting coroutine',
          'It completes with an empty map as a sentinel value',
          'It completes with a MissingPluginException error',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Code inside an isolate spawned via Isolate.run tries to use a MethodChannel and fails. What makes platform channels work from a background isolate?',
        options: [
          'Pass the RootIsolateToken to the isolate and call BackgroundIsolateBinaryMessenger.ensureInitialized with it before using channels',
          'Recreate the MethodChannel with a unique name suffix per isolate',
          'Wrap every invokeMethod call in compute so it hops to the root isolate',
          'Set the isolate as a daemon isolate so the engine attaches a messenger automatically',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'ListView wraps each child in a RepaintBoundary by default (addRepaintBoundaries: true). What is the actual tradeoff of this behavior?',
        options: [
          'It disables item recycling, so all children stay alive off-screen',
          'It forces every child to relayout on scroll, trading paint time for layout time',
          'Each boundary caches its child as a separate layer, cutting repaint cost during scroll but adding memory and compositing overhead; trivially cheap children may be faster without it',
          'It makes scrolling smoother only on displays above 60Hz',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Animating the opacity parameter of an Opacity widget on every frame is a known performance smell. Why?',
        options: [
          'Opacity rebuilds its entire child widget subtree each frame',
          'Partial opacity generally forces an offscreen buffer (saveLayer) per frame; FadeTransition or AnimatedOpacity drive a layer property instead',
          'Opacity discards the RepaintBoundary above it, widening repaints',
          'Opacity values are quantized to 8 bits, causing visible banding that must be dithered on the CPU',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In the Flutter performance overlay, what does the top graph (of the two stacked graphs) represent?',
        options: [
          'Frame build times on the Dart UI thread',
          'Garbage collection pauses in the Dart VM',
          'The number of layers composited per frame',
          'Frame times on the raster (GPU) thread that executes the layer tree',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'With the Router API, how does an Android system back gesture reach your navigation state?',
        options: [
          'The BackButtonDispatcher receives it and invokes RouterDelegate.popRoute, where you mutate app state and notify listeners',
          'The engine pops the top-most route directly and then informs the RouterDelegate',
          'RouteInformationParser.parseRouteInformation is called with the previous URL',
          'The Navigator widget intercepts it via onPopPage before any delegate involvement',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Your app uses a nested Navigator for a tab. showDialog called with the inner context renders the dialog underneath the bottom navigation bar. What is the standard fix?',
        options: [
          'Wrap the dialog in an Overlay so it floats above all navigators',
          'Give the dialog route a higher elevation via its barrier settings',
          'Call showDialog with useRootNavigator: true (or Navigator.of(context, rootNavigator: true)) so the dialog is pushed onto the root navigator',
          'Move the bottom navigation bar into the inner Navigator so it participates in the same stack',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In a widget test, tester.tap(find.byType(FloatingActionButton)) runs, yet the UI checked immediately afterwards is unchanged. Why?',
        options: [
          'tap only works on widgets with a Key assigned',
          'tap dispatches the pointer events, but no new frame is built until you call pump; assertions before pump see the old tree',
          'The FakeAsync zone swallows gesture callbacks unless wrapped in runAsync',
          'tester.tap requires ensureVisible first, otherwise it taps at Offset.zero',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A widget under test calls a MethodChannel. Which is the supported way to fake the native response inside the test?',
        options: [
          'Spawn a local platform stub process that answers on the real channel',
          'Override debugDefaultTargetPlatformOverride so the channel short-circuits',
          'Inject a fake MethodChannel subclass through the widget constructor; the binding forbids handler mocks',
          'Register a mock handler via TestDefaultBinaryMessengerBinding defaultBinaryMessenger.setMockMethodCallHandler for that channel',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Bloc whose states use Equatable emits a new state instance that is == to the current state. What do BlocBuilder widgets observe?',
        options: [
          'Nothing; the emit is ignored because the new state equals the current one, so no rebuild occurs',
          'One rebuild, since every emit always notifies listeners',
          'A rebuild only for builders without a buildWhen condition',
          'An exception, because emitting equal states is treated as a logic error',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Why is calling ref.watch inside a button onPressed callback considered wrong in Riverpod?',
        options: [
          'watch is async and would need to be awaited inside the callback',
          'It throws immediately because watch only exists on WidgetRef, not on Ref',
          'watch is meant for reactive contexts like build; in an event handler it can create lingering subscriptions and stale rebuild behavior, so ref.read is the correct one-shot access',
          'The provider would be disposed as soon as the callback returns',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'With Provider, a widget uses context.select((Cart c) => c.itemCount). When does that widget rebuild?',
        options: [
          'On every notifyListeners from Cart, like a normal watch',
          'Only when the value returned by the selector changes according to ==, letting other Cart mutations skip this widget',
          'Only once, because select creates a one-time snapshot',
          'Whenever any ancestor provider of any type notifies',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'An InheritedWidget rebuilds with new data but its updateShouldNotify returns false. What do existing dependents experience?',
        options: [
          'They rebuild anyway, since dependency registration ignores updateShouldNotify',
          'They are unregistered as dependents and must re-subscribe on next build',
          'They throw a stale-dependency assertion in debug mode',
          'They are not notified, so they can keep showing values derived from the old data until something else triggers their rebuild',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Iterable<int> g() sync* {\n  print("run");\n  yield 1;\n}\nvoid main() {\n  final it = g();\n  print("before");\n  it.first;\n}\nWhat is the output order?',
        options: [
          'before, run — sync* bodies are lazy and execute only when iteration starts',
          'run, before — generator bodies run eagerly up to the first yield',
          'before only — first does not trigger execution of the body',
          'run, before, run — the body runs once at creation and again at iteration',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Future fails, but no then, catchError, or await ever attaches to it. What happens to the error?',
        options: [
          'It is silently discarded once the future is garbage collected',
          'It is rethrown synchronously at the point the future was created',
          'It becomes an unhandled asynchronous error delivered to the current zone error handler, which by default crashes tests and logs in apps',
          'It is stored on the future and only surfaces if someone eventually awaits it',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Why must WidgetsFlutterBinding.ensureInitialized and runApp be called in the same zone when using runZonedGuarded?',
        options: [
          'ensureInitialized captures the zone for isolate spawning, and mismatches break compute',
          'Callbacks the binding registers are bound to their creation zone; a mismatch means framework callbacks run outside the guarded zone and their errors escape the zone handler',
          'runApp asserts identical zones purely for hot-reload bookkeeping',
          'Platform channel replies are dropped when zones differ',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Library A and library B each independently evaluate const Point(1, 2) from the same class. What does identical(a, b) return across those libraries?',
        options: [
          'false, because canonicalization is scoped per library',
          'false, because each isolate re-instantiates const objects per compilation unit',
          'true only when running in AOT release mode',
          'true; const canonicalization is program-wide, so equal const expressions share one instance regardless of where they appear',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
    ],
    3: [
      {
        question: 'For a StatefulWidget being inserted into the tree for the first time, which is the correct callback order before the first frame paints?',
        options: [
          'createState, didChangeDependencies, initState, build',
          'initState, createState, build, didChangeDependencies',
          'createState, initState, didChangeDependencies, build',
          'createState, initState, build, didChangeDependencies',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'You edit code inside initState and hot reload, but the change does not take effect until a hot restart. Why?',
        options: [
          'Hot reload injects new code and calls reassemble and build on existing elements, but initState is not re-run for States that are already mounted',
          'Hot reload only recompiles stateless widgets; stateful ones need restart',
          'initState is inlined by the compiler and cannot be hot swapped',
          'The debugger caches the old State class until the isolate restarts',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'During tree diffing, under what condition does Widget.canUpdate allow an existing element to take a new widget instead of inflating a fresh one?',
        options: [
          'The two widgets must be identical instances in memory',
          'The new widget must have the same hashCode as the old widget',
          'The two widgets must compare equal with ==',
          'The old and new widgets must have the same runtimeType and the same key',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A ListView inside a TabBarView resets to offset zero whenever the user switches tabs and back. Which mechanism is designed to preserve the scroll offset across such rebuilds?',
        options: [
          'Assign a UniqueKey to the ListView so its element is never rebuilt',
          'Give the ListView a PageStorageKey so its scroll offset is saved in the surrounding PageStorage bucket and restored on re-creation',
          'Wrap the ListView in a RepaintBoundary to freeze its paint state',
          'Set primary: true so the PrimaryScrollController remembers the offset',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'By mistake, the same GlobalKey instance ends up attached to two widgets that are in the tree during the same frame. What does Flutter do?',
        options: [
          'The second widget silently steals the State from the first',
          'The framework disambiguates them by their parent chain',
          'It throws a duplicate GlobalKey error during build, since a GlobalKey must identify at most one element in the whole tree per frame',
          'Only the first registration wins and the second widget becomes stateless',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'onPressed: () async {\n  await api.save();\n  Navigator.of(context).pop();\n}\nWhat is the real risk the use_build_context_synchronously lint warns about here?',
        options: [
          'After the await, the widget may have been unmounted; using its BuildContext then can operate on a defunct element and crash or act on the wrong tree',
          'The pop will run before save completes because context lookups are eager',
          'Navigator.of is not allowed inside async closures at all',
          'The context becomes a different instance after each await, pointing at the root',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Why is dependOnInheritedWidgetOfExactType effectively O(1) while findAncestorStateOfType is O(depth)?',
        options: [
          'Inherited widgets are stored in a global registry keyed by runtimeType at app level',
          'findAncestorStateOfType has to compare hashCodes at each level, which dominates',
          'Inherited lookups are resolved at compile time via the type argument',
          'Each element carries a hash map of all inherited elements in scope, so inherited lookups are a map read, whereas ancestor-state lookups walk parent pointers one by one',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'An autoDispose FutureProvider refetches from the network every time the user navigates away and back. Which explanation and remedy are correct?',
        options: [
          'FutureProvider never caches by design; switch to StreamProvider for caching',
          'Navigating away removes the last listener so the state is disposed; call ref.keepAlive() (optionally with a timed link) to retain the cache',
          'The provider family hash changes per navigation; override its == to fix it',
          'Riverpod clears all providers on route changes unless a ProviderScope wraps each page',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In Riverpod, what is the semantic difference between ref.listen and ref.watch inside a widget build?',
        options: [
          'listen is only usable inside providers, never widgets',
          'watch fires callbacks with previous and next values; listen just rebuilds',
          'watch subscribes and rebuilds the widget on change; listen subscribes and invokes a callback with old and new values without rebuilding, suited to side effects like snackbars',
          'They are aliases; listen exists for backwards compatibility',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'You need to show a snackbar exactly once per state transition of a Bloc, without rebuilding the subtree. Which widget matches that contract?',
        options: [
          'BlocListener, whose listener callback runs once per state change and does not rebuild children',
          'BlocBuilder with buildWhen returning false, so only the builder side effect runs',
          'BlocSelector selecting the whole state so the snackbar shows in build',
          'RepaintBoundary around a BlocBuilder to suppress the rebuild',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A developer calls setState(() {}) with an empty closure and no field changes. What does the framework do?',
        options: [
          'It compares old and new state and skips the rebuild as an optimization',
          'It throws in debug mode because the closure must mutate state',
          'It rebuilds only if the widget overrides shouldRebuild',
          'It marks the element dirty and build runs next frame anyway; setState performs no diffing of your state',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Future<void> f() async {\n  print("1");\n  await Future(() => print("2"));\n  print("3");\n}\nvoid main() {\n  print("0");\n  f();\n  print("4");\n}\nWhat is the output order?',
        options: [
          '0, 1, 2, 3, 4',
          '0, 1, 4, 2, 3',
          '0, 4, 1, 2, 3',
          '0, 1, 4, 3, 2',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A listener on a non-broadcast stream calls subscription.pause() while the producer keeps adding events to the controller. What happens to those events?',
        options: [
          'They are dropped until resume is called',
          'The controller throws because pausing forbids adds',
          'They are buffered and delivered in order after resume, which is why long pauses can grow memory unboundedly',
          'The producer is synchronously blocked inside add until resume',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'String? s = maybeNull();\nprint(s!.length);\nAt runtime s is null. What exactly happens?',
        options: [
          'A TypeError is thrown at the ! operator (null check operator used on a null value); the compiler accepted it because ! is a runtime assertion',
          'It prints 0 because length of null coalesces to zero',
          'It fails to compile since the compiler proves s can be null',
          'A NoSuchMethodError is thrown when length is invoked on null',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In const MyWidget(child: SizedBox()), the inner SizedBox() has no const keyword. Is it a const instance?',
        options: [
          'No, only the outer widget is const; the child is allocated fresh each build',
          'Only if SizedBox declares all fields final and overrides ==',
          'No, and the code fails to compile because const constructors require const arguments to be labeled',
          'Yes; arguments of a const constructor invocation are in a const context, so const is implied for the child',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Inside a Column, what is the practical difference between Expanded(child: box) and Flexible(fit: FlexFit.loose, child: box) for a box wanting height 50?',
        options: [
          'They render identically because Column ignores fit for single children',
          'Expanded forces the child to fill its allotted extent (tight), so the box stretches; loose Flexible lets the child be its preferred 50 within the allotted max',
          'Flexible gives the child unbounded height while Expanded bounds it',
          'Expanded distributes space by intrinsic height while Flexible uses flex factors',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In debug mode, a custom RenderBox performLayout sets size to something outside the incoming BoxConstraints. What happens?',
        options: [
          'The parent silently clamps the size during paint',
          'The child is skipped during compositing but still hit-testable',
          'An assertion fails, because a render box must pick a size that satisfies the constraints handed down by its parent',
          'The framework retries layout with loosened constraints',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Two full-size GestureDetectors overlap inside a Stack. Which one receives an onTap for a tap in the overlapping region, and why?',
        options: [
          'The one later in the children list: hit testing visits children front-to-back (reverse paint order), and its opaque hit claims the tap',
          'The one earlier in the children list, because hit testing follows paint order',
          'Neither; overlapping detectors cancel each other in the gesture arena',
          'Each receives onTap once, since Stack broadcasts hits to all children',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'An AnimatedContainer is mid-way through animating its color to blue when a rebuild retargets it to green. What does it do?',
        options: [
          'It jumps instantly to green since the previous animation is cancelled',
          'It completes the blue animation first, then animates to green',
          'It throws unless the previous animation is awaited via an AnimationStatusListener',
          'It starts a new tween from the currently interpolated color toward green, animating smoothly from wherever it was',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'final anim = CurvedAnimation(parent: controller, curve: Interval(0.5, 1.0));\nWhen controller.value is 0.25, what is anim.value?',
        options: [
          '0.25, because Interval only reshapes timing after its start point',
          '0.0, because the interval maps everything before its begin fraction to zero; the child animation has not started yet',
          '0.5, the midpoint of the remapped range',
          '1.0, because values outside the interval clamp to the end',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A State forgets to dispose an AnimationController that is still repeating when the widget is removed. What symptom appears in debug builds?',
        options: [
          'The controller pauses automatically and nothing is reported',
          'A memory leak occurs but Flutter has no way to detect or report it',
          'An assertion reports that a Ticker was still active after disposal of its TickerProvider, pointing at the leaked controller',
          'The next route inherits the running controller and its animations replay',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A native Android handler returns a HashMap over a MethodChannel. In Dart, result as Map<String, String> throws a type cast error. Why?',
        options: [
          'The codec decodes maps as Map<Object?, Object?>; the runtime cast to a more specific generic type fails, so you must use cast or Map.from to retype entries',
          'Android HashMaps arrive as JSON strings that must be decoded first',
          'The channel strips generics, delivering a Map<dynamic, Null> on release builds only',
          'Dart maps from channels are immutable views that forbid casting',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What ordering guarantee do platform channel messages have?',
        options: [
          'No guarantees; the engine may deliver any message in any order',
          'Messages are globally ordered across all channels by timestamp',
          'Only EventChannel streams are ordered; MethodChannel calls may race each other arbitrarily on delivery',
          'Messages sent on a given channel are delivered in the order sent, but replies can complete out of order relative to other in-flight calls',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'The DevTools timeline shows long Build phases on the UI thread while raster times stay short. Where should optimization effort go first?',
        options: [
          'Reduce shader complexity, since builds wait on shader compilation',
          'Shrink rebuild scope: push setState lower, use const children, and cache subtrees so fewer widgets rebuild per frame',
          'Add more RepaintBoundaries, since paint is dominating the frame',
          'Move the layer tree to the raster thread with foregroundPainter',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Frames are janky and the raster thread bar is red while the UI thread is nearly idle. Which cause fits this signature?',
        options: [
          'Excessive widget rebuilds triggered by an over-eager InheritedWidget',
          'A JSON decode running synchronously in a button callback',
          'Expensive painting effects such as repeated saveLayer, big blurs, or first-use shader compilation on the raster side',
          'Too many microtasks scheduled between frames by streams',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Parsing a 5MB JSON payload with jsonDecode in the onData of a network stream produces visible jank. Why does moving it to compute or Isolate.run help?',
        options: [
          'Decoding runs on another isolate with its own event loop, so the UI isolate keeps producing frames; only the (copied or transferred) result crosses back',
          'compute defers the decode to idle time between frames on the same isolate',
          'Isolates share the decoded object so no serialization cost exists at all',
          'The raster thread performs the decode when it is delegated via compute',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'On Flutter web with the Router API, the user edits the URL bar and hits enter. What is the correct sequence that updates your UI?',
        options: [
          'The Navigator pops all routes, then pushes a route named after the new URL',
          'RouterDelegate.build is called first, then the parser validates the URL after paint',
          'setNewRoutePath fires immediately, and parseRouteInformation runs afterwards to sync the address bar',
          'RouteInformationProvider emits the new RouteInformation, parseRouteInformation converts it to a configuration, setNewRoutePath applies it, and the delegate notifies listeners to rebuild',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In golden (screenshot) tests, text renders as solid rectangles instead of glyphs. What is going on?',
        options: [
          'The golden comparator strips text layers to reduce image diffs',
          'The test environment uses the Ahem placeholder font by default; real fonts must be loaded explicitly (for example via a FontLoader in setUp) to render glyphs',
          'Goldens are rendered without a GPU so text always rasterizes as boxes',
          'The device pixel ratio defaults to zero in tests, collapsing glyph outlines',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'When testing values emitted by a broadcast stream, why should expectLater(stream, emitsInOrder([1, 2])) be called before the code that adds the events?',
        options: [
          'expectLater synchronously drains the stream, so late calls deadlock',
          'emitsInOrder requires the stream to be paused when it attaches',
          'expectLater subscribes the matcher immediately; a broadcast stream does not buffer, so events added before subscription would be missed and the expectation would hang',
          'The test framework forbids awaiting a stream after any add call',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A widget test needs to exercise real asynchronous work, like an actual file read or a real HTTP call against a local server, which never completes under the default test binding. What is the supported escape hatch?',
        options: [
          'Wrap the awaiting code in tester.runAsync, which runs it with real async event handling instead of the fake-async clock',
          'Increase the timeout on testWidgets so the fake clock eventually catches up',
          'Call pumpAndSettle repeatedly until the OS delivers the I/O result',
          'Switch the whole file from testWidgets to test, keeping pumpWidget calls unchanged',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
    ],
  },
};
