module.exports = {
  category: 'Python Developer',
  sets: {
    1: [
      {
        question: 'A single CPython script contains:\na = 256; b = 256\nc = 257; d = 257\nprint(a is b, c is d)\nWhat does it print when run as a file?',
        options: [
          'True False',
          'False False',
          'True True',
          'False True',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Why does sys.getrefcount(obj) typically report a value one higher than the number of references you can account for in your code?',
        options: [
          'Passing obj as an argument to getrefcount creates a temporary reference that is counted',
          'CPython always pre-allocates one hidden reference per object for the garbage collector',
          'The interned object cache holds an extra reference to every live object',
          'getrefcount adds 1 to reserve space for weak references',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Which statement about the CPython GIL is accurate?',
        options: [
          'The GIL is released after exactly 100 bytecode instructions, guaranteeing fair scheduling',
          'Each Python thread acquires its own private GIL, so only C extension calls are serialized',
          'Because of the GIL, list.append called from multiple threads can corrupt the list structure',
          'A thread holding the GIL releases it around blocking I/O system calls, so I/O-bound threads can overlap',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What does this print in CPython?\ns1 = "hello"\ns2 = "hel" + "lo"\ns3 = "".join(["hel", "lo"])\nprint(s1 is s2, s1 is s3)',
        options: [
          'True True',
          'True False',
          'False False',
          'False True',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Since PEP 442 (Python 3.4+), how does the cyclic garbage collector handle reference cycles that contain objects defining __del__?',
        options: [
          'Such cycles are still uncollectable and are moved to gc.garbage forever',
          'The cycle is collected: finalizers run at most once, then the objects are freed',
          '__del__ is skipped entirely for objects inside cycles to avoid resurrection',
          'The collector raises ResourceWarning and leaks the cycle intentionally',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What does this print?\ndef f(x, acc=[]):\n    acc.append(x)\n    return acc\nprint(f(1), f(2))',
        options: [
          '[1] [1, 2]',
          '[1] [2]',
          '[1, 2] [2]',
          '[1, 2] [1, 2]',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What does this print?\nfuncs = [lambda: i for i in range(3)]\nprint([f() for f in funcs])',
        options: [
          '[2, 2, 2]',
          '[0, 1, 2]',
          '[0, 0, 0]',
          'Raises NameError because i is out of scope',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What happens here?\nt = ([1, 2],)\nt[0] += [3]\nprint(t)',
        options: [
          'No exception; prints ([1, 2, 3],)',
          'TypeError is raised and t is left unchanged as ([1, 2],)',
          'TypeError is raised, yet t is now ([1, 2, 3],)',
          'No exception; prints ([1, 2],) because tuples block the mutation',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What does this print?\nx = [1, 2]\ny = x\nx += [3]\nx = x + [4]\nprint(y)',
        options: [
          '[1, 2, 3]',
          '[1, 2]',
          '[1, 2, 3, 4]',
          '[1, 2, 4]',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What does this print?\nprint(False == False in [False])',
        options: [
          'False',
          'Raises TypeError',
          'Raises SyntaxError',
          'True',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What does this print?\nprint(-1 ** 2, (-1) ** 2)',
        options: [
          '1 1',
          '-1 1',
          '1 -1',
          '-1 -1',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What does this print?\nd = {True: "a", 1: "b", 1.0: "c"}\nprint(d)',
        options: [
          '{True: "a", 1: "b", 1.0: "c"}',
          '{1.0: "c"}',
          '{True: "c"}',
          '{True: "a"}',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What does this print?\nd = dict.fromkeys("ab", [])\nd["a"].append(1)\nprint(d["b"])',
        options: [
          '[1]',
          '[]',
          'Raises KeyError',
          'None',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'An attribute name exists both as a descriptor on the class and as a key in the instance __dict__. Which lookup rule applies?',
        options: [
          'The instance __dict__ always wins, regardless of descriptor type',
          'A data descriptor (defines __set__ or __delete__) wins over the instance __dict__; a non-data descriptor loses to it',
          'The descriptor always wins, regardless of whether it defines __set__',
          'A non-data descriptor wins over the instance __dict__; a data descriptor loses to it',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'class A:\n    __slots__ = ("x",)\nclass B(A):\n    pass\nb = B()\nb.y = 1\nWhat happens?',
        options: [
          'AttributeError, because __slots__ is inherited and forbids new attributes',
          'TypeError at class creation, because subclassing a slotted class requires __slots__',
          'It works: B instances get a __dict__ since B does not declare __slots__',
          'It works, but b.y is silently stored in the "x" slot',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What does this print?\nclass A:\n    x = 1\n    def __getattr__(self, name):\n        return 42\na = A()\nprint(a.x, a.y)',
        options: [
          '42 42, because __getattr__ intercepts every attribute access',
          'Raises AttributeError on a.y',
          '1 followed by AttributeError',
          '1 42, because __getattr__ runs only when normal lookup fails',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A metaclass defines:\nclass Meta(type):\n    def __call__(cls, *a, **k):\n        print("intercepted")\n        return super().__call__(*a, **k)\nclass A(metaclass=Meta): pass\nWhen does "intercepted" print?',
        options: [
          'Every time A() is instantiated, because instantiating A invokes type(A).__call__',
          'Once, at the moment class A is defined',
          'Never, because __call__ on a metaclass only fires for calls to Meta itself',
          'Only when A.__init__ is explicitly invoked',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Which statement about __init_subclass__ is correct?',
        options: [
          'It runs on each subclass when instances of the subclass are created',
          'It must be decorated with @staticmethod to receive the subclass',
          'It is invoked on the parent class at subclass definition time, receiving the new subclass as cls, and is implicitly a classmethod',
          'It replaces the need for a metaclass __new__ only when the parent also defines __set_name__',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'class A:\n    def hi(self): print("A")\nclass B(A):\n    def hi(self): print("B"); super().hi()\nclass C(A):\n    def hi(self): print("C"); super().hi()\nclass D(B, C):\n    def hi(self): print("D"); super().hi()\nD().hi()\nWhat is printed?',
        options: [
          'D B A',
          'D B C A',
          'D B A C A',
          'D C B A',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What does this print?\nx = [1, 2]\ng = (i for i in x)\nx = [3, 4]\nprint(list(g))',
        options: [
          '[3, 4]',
          '[]',
          'Raises StopIteration',
          '[1, 2]',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In Python 3.7+, what happens when a StopIteration exception propagates out of code running inside a generator body?',
        options: [
          'It is converted into a RuntimeError instead of silently ending the generator (PEP 479)',
          'It silently terminates the generator as if it returned',
          'It is re-raised unchanged to the caller of next()',
          'It is converted into a GeneratorExit',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What does this print?\ndef g():\n    yield 1\n    return 99\ndef h():\n    r = yield from g()\n    yield r\nprint(list(h()))',
        options: [
          '[1]',
          '[1, None]',
          '[1, 99]',
          'Raises StopIteration with value 99',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'async def f():\n    return 1\nprint(f())\nWhat happens when this script runs (no event loop started)?',
        options: [
          'Prints 1, because trivial coroutines are executed eagerly',
          'Prints a coroutine object repr; the body never executes, and a RuntimeWarning about a never-awaited coroutine is emitted',
          'Raises RuntimeError: no running event loop',
          'Blocks forever waiting for an event loop',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'results = await asyncio.gather(slow(), fast())\nfast() finishes long before slow(). What is the order of results?',
        options: [
          'Completion order: fast result first, slow result second',
          'Nondeterministic, depending on event loop scheduling',
          'gather raises unless tasks finish in argument order',
          'Argument order: slow result first, fast result second, regardless of completion order',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'x = 10\ndef f():\n    print(x)\n    x = 20\nf()\nWhat happens?',
        options: [
          'UnboundLocalError: the assignment makes x local for the whole function body, so the print references an unassigned local',
          'Prints 10, then binds a new local x = 20',
          'Prints 10, then rebinds the global x to 20',
          'NameError: name x is not defined',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'class A:\n    x = 1\n    ys = [x for _ in range(2)]\nWhat happens at class definition time?',
        options: [
          'ys becomes [1, 1] because the class body is the enclosing scope',
          'NameError: the comprehension has its own scope, and class scopes are not searched by nested scopes',
          'ys becomes [None, None] because x is not yet bound',
          'SyntaxError: comprehensions are not allowed in class bodies',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'def outer():\n    x = 1\n    def inner():\n        nonlocal x\n        x += 1\n    inner(); inner()\n    return x\nprint(outer())\nWhat is printed?',
        options: [
          '1, because each inner() call gets a fresh closure cell',
          '2, because nonlocal rebinding only persists once',
          '3',
          'Raises SyntaxError: nonlocal requires x to be declared global first',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'def f():\n    try:\n        return 1\n    finally:\n        return 2\nprint(f())\nWhat is printed?',
        options: [
          '1, because the try return wins',
          'Raises RuntimeError: conflicting returns',
          '1 then 2 on separate lines',
          '2, because a return in finally overrides the pending return from try',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'try:\n    raise ValueError("boom")\nexcept ValueError as e:\n    pass\nprint(e)\nWhat happens at the print?',
        options: [
          'NameError: the except target is deleted when the except block exits',
          'Prints boom',
          'Prints ValueError("boom") with the full repr',
          'Prints None because e was unbound by pass',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Inside an except block you execute: raise KeyError("k") from None. What effect does "from None" have?',
        options: [
          'It clears the traceback of the new KeyError entirely',
          'It converts the KeyError into a warning',
          'It sets __suppress_context__ so the implicitly chained original exception is hidden from the traceback display',
          'It makes the KeyError unpicklable but otherwise changes nothing',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
    ],
    2: [
      {
        question: 'Why can decorating instance methods with functools.lru_cache cause a memory leak?',
        options: [
          'lru_cache stores results in a global registry that is never trimmed',
          'The cache keys include self, so the cache keeps strong references to instances, preventing them from being garbage collected',
          'lru_cache disables __del__ on the owning class',
          'Bound methods are re-created per call, so every call adds a distinct cache entry forever',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: '@functools.lru_cache(maxsize=None)\ndef f(x):\n    return len(x)\nf([1, 2, 3])\nWhat happens?',
        options: [
          'Returns 3 and caches the result keyed by the list contents',
          'Returns 3 but the call is never cached',
          'Raises RecursionError due to hashing the wrapper',
          'Raises TypeError: list is unhashable, so it cannot be a cache key',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'from functools import partial\ndef f(x, y=0):\n    return (x, y)\np = partial(f, y=5)\nprint(p(1), p(1, y=9))\nWhat is printed?',
        options: [
          '(1, 5) (1, 9)',
          '(1, 5) then TypeError: got multiple values for y',
          '(1, 0) (1, 9)',
          '(5, 1) (9, 1)',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'from itertools import groupby\nprint([k for k, g in groupby("aabba")])\nWhat is printed?',
        options: [
          "['a', 'b']",
          "['a', 'a', 'b', 'b', 'a']",
          "['a', 'b', 'a']",
          'Raises ValueError: input must be sorted',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'from itertools import groupby\npairs = [(k, g) for k, g in groupby("aabb")]\nprint(list(pairs[0][1]))\nWhat is printed?',
        options: [
          "['a', 'a']",
          '[], because each group shares the underlying iterator, which was already advanced past that group',
          "['b', 'b']",
          'Raises StopIteration',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'After calling a, b = itertools.tee(source_iter), which usage rule applies?',
        options: [
          'a and b may be consumed from different threads without locking',
          'tee eagerly copies all remaining items, so source_iter can be reused freely',
          'a and b stay synchronized: advancing a also advances b',
          'source_iter should no longer be used directly; consuming it can make items disappear from a and b',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'print(list(zip([1, 2, 3], "ab")))\nWhat is printed, and how could you make unequal lengths an error in Python 3.10+?',
        options: [
          "[(1, 'a'), (2, 'b')]; pass strict=True to zip to raise ValueError on unequal lengths",
          "[(1, 'a'), (2, 'b'), (3, None)]; pass fillvalue=None to change padding",
          'Raises ValueError immediately because lengths differ',
          "[(1, 'a'), (2, 'b')]; wrap with itertools.zip_longest to raise on unequal lengths",
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'from functools import reduce\nprint(reduce(lambda a, b: a - b, [1, 2, 3], 10))\nWhat is printed?',
        options: [
          '-4',
          '6',
          '4',
          '10',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A function is annotated to take list[float]. Why does a static type checker reject passing a list[int], even though int is compatible with float?',
        options: [
          'Because int is not a subtype of float in the typing system at all',
          'Because list is invariant in its element type; a covariant abstraction like Sequence[float] would accept it',
          'Because the checker requires an explicit cast for any numeric container',
          'Because list[float] only accepts literals written with a decimal point',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What does isinstance([1, 2], list[int]) do at runtime?',
        options: [
          'Returns True because every element is an int',
          'Returns True because the object is a list, ignoring the parameter',
          'Returns False because runtime lists carry no element type',
          'Raises TypeError: isinstance() argument 2 cannot be a parameterized generic',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What is the primary runtime effect of "from __future__ import annotations" in a module?',
        options: [
          'All annotations are stored as strings and not evaluated at definition time, so forward references and undefined names in annotations do not raise',
          'It enables PEP 604 union syntax at runtime only inside typing.cast calls',
          'It makes annotations mandatory for all public functions',
          'It causes annotations to be type-checked when the module is imported',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'With a @runtime_checkable typing.Protocol, what does isinstance(obj, MyProto) actually verify?',
        options: [
          'That obj was registered against the protocol with ABCMeta.register',
          'Method signatures and attribute types match the protocol exactly',
          'Only the presence of the protocol members (structural name check); signatures and types are not validated',
          'That obj inherits from the Protocol class somewhere in its MRO',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: '@dataclass\nclass A:\n    items: list = []\nWhat happens when this class is defined?',
        options: [
          'The class is created; each instance shares the same list, like a mutable default argument',
          'ValueError is raised at definition time: mutable default; use field(default_factory=list)',
          'The dataclass machinery silently converts it to field(default_factory=list)',
          'TypeError at first instantiation, not at definition',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: '@dataclass(frozen=True)\nclass P:\n    x: int\np = P(1)\np.x = 5\nWhat happens at the assignment?',
        options: [
          'It works, because frozen only affects __init__ arguments',
          'It works, but a DeprecationWarning is emitted',
          'AttributeError is raised only if __slots__ is also set',
          'dataclasses.FrozenInstanceError (an AttributeError subclass) is raised; frozen also lets the dataclass generate __hash__',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'On Windows (spawn start method), why does multiprocessing code fail or spawn processes endlessly without an if __name__ == "__main__" guard?',
        options: [
          'The child process re-imports the main module from scratch; unguarded top-level process creation re-executes in every child',
          'Windows forbids more than one Python process per console session',
          'spawn requires the main module to be a compiled .pyc file',
          'The GIL is inherited by child processes and deadlocks them',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'For a CPU-bound pure-Python workload in CPython, which parallelization claim is correct?',
        options: [
          'threading scales linearly with cores because the GIL is per-thread',
          'asyncio speeds it up by interleaving bytecode execution across cores',
          'multiprocessing achieves true parallelism because each process has its own interpreter and GIL; threads do not help here',
          'threading helps as long as sys.setswitchinterval is lowered enough',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A worker thread is started with daemon=True and is writing a file when the main thread exits. What happens?',
        options: [
          'The interpreter waits for the daemon thread to finish its current job',
          'The interpreter exits without waiting; the daemon thread is stopped abruptly and its finally blocks may never run',
          'The daemon thread is promoted to non-daemon and keeps the process alive',
          'A RuntimeError is raised in the main thread until join() is called',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'd = {1: "a"}\nfor k in d:\n    d[k + 1] = "b"\nWhat happens?',
        options: [
          'The loop runs forever, adding keys 2, 3, 4, ...',
          'The loop runs exactly once and d becomes {1: "a", 2: "b"}',
          'The new key is skipped because dict iteration snapshots the keys',
          'RuntimeError: dictionary changed size during iteration',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'x = [3, 1, 2].sort()\nprint(x)\nWhat is printed?',
        options: [
          'None, because list.sort sorts in place and returns None',
          '[1, 2, 3]',
          '[3, 1, 2]',
          'Raises AttributeError: literals cannot be sorted in place',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Which property of Python sorting enables sorting by a secondary key first and then by a primary key in a second pass to get a correct multi-key ordering?',
        options: [
          'Timsort runs in O(n) on nearly-sorted data',
          'sorted() copies the list, so earlier passes are never visible',
          'Python sorts are stable: equal primary keys preserve the relative order produced by the earlier pass',
          'The key function is memoized between successive sorted() calls',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'print(round(0.5), round(1.5), round(2.5))\nWhat is printed?',
        options: [
          '1 2 3',
          '0 2 2',
          '0 1 2',
          '1 2 2',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'x = float("nan")\nprint(x == x, x in [x])\nWhat is printed?',
        options: [
          'False False',
          'True True',
          'True False',
          'False True, because containment checks identity before equality',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'print(all([]), any([]))\nWhat is printed?',
        options: [
          'True False',
          'False False',
          'False True',
          'True True',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'print(-7 // 2, -7 % 2)\nWhat is printed?',
        options: [
          '-3 -1',
          '-3 1',
          '-4 1',
          '-4 -1',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'print(" a  b ".split(" "))\nWhat is printed?',
        options: [
          "['a', 'b']",
          "['', 'a', '', 'b', '']",
          "['a', '', 'b']",
          "[' a', ' b ']",
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'from itertools import cycle, islice\nprint(list(islice(cycle([1, 2]), 5)))\nWhat is printed?',
        options: [
          '[1, 2, 1, 2]',
          '[1, 1, 1, 1, 1]',
          'Nothing: it loops forever because cycle never ends',
          '[1, 2, 1, 2, 1]',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'How does functools.singledispatch decide which registered implementation to call?',
        options: [
          'By the runtime type of the first positional argument only',
          'By matching the full call signature against each registered overload',
          'By the static type annotations of all arguments',
          'By the number of arguments passed at the call site',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A decorator wraps a function without using functools.wraps. Which consequence follows?',
        options: [
          'The wrapper raises TypeError when called with keyword arguments',
          'The original function is garbage collected after decoration',
          'The decorated function exposes the wrapper __name__ and __doc__, and __wrapped__ is missing, which breaks introspection and some tooling',
          'The decorator silently loses its closure over the original function',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Which statement about functools.cached_property is correct?',
        options: [
          'It recomputes whenever any instance attribute changes',
          'On first access it stores the value in the instance __dict__, so later accesses bypass the descriptor entirely; it fails on classes whose instances lack a __dict__',
          'It caches per-class, so all instances share one computed value',
          'It requires the class to also define __hash__ for cache keying',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'def f(x: Optional[int]) -> int: ...\nWhich reading of the annotation is correct?',
        options: [
          'x may be omitted at the call site because Optional implies a default',
          'x defaults to None automatically',
          'Optional[int] narrows to int automatically after any comparison',
          'Optional[int] is exactly Union[int, None]; it says nothing about whether the argument can be omitted',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
    ],
    3: [
      {
        question: 'df has index labels 0..4. Compare df.loc[1:3] and df.iloc[1:3]. Which is correct?',
        options: [
          'The two expressions are equivalent, each returning rows at positions 1 and 2',
          'The two expressions are equivalent, each returning rows labeled 1, 2 and 3',
          'loc slices by label and INCLUDES the stop label (rows 1, 2, 3); iloc slices by position and excludes the stop (rows 1, 2)',
          'loc excludes the stop label; iloc includes the stop position',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In pandas, why is df[df.a > 0]["b"] = 1 unreliable, and what is the correct idiom?',
        options: [
          'Chained indexing may assign into a temporary copy (SettingWithCopyWarning); use df.loc[df.a > 0, "b"] = 1',
          'Boolean masks cannot be used for assignment at all; use df.query instead',
          'It always mutates the original but bypasses dtype checks; use df.assign',
          'It fails only when b contains NaN; fillna first, then assign',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In pandas, df1 has 3 rows with key "k" and df2 has 2 rows with key "k". How many rows does df1.merge(df2, on="key") produce for that key?',
        options: [
          '3, keeping the left frame row count',
          '6, the cartesian product of matching rows',
          '2, keeping the right frame row count',
          '5, the union of both sides',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In pandas, what does the mask df["a"] == np.nan evaluate to for a column that contains NaN values?',
        options: [
          'True exactly where the NaNs are',
          'NaN where the NaNs are and False elsewhere',
          'It raises TypeError: cannot compare float to NaN',
          'All False, because NaN never compares equal to anything; use df["a"].isna() instead',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'You load integers into a pandas column but one value is missing (NaN). What dtype does the column classically get, and why?',
        options: [
          'object, because mixed types force object dtype',
          'int64, with NaN stored as a sentinel value of -1',
          'float64, because NaN is a float and classic int64 arrays cannot represent missing values',
          'category, because pandas treats gaps as categorical',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'qs = Book.objects.filter(pages__gt=100)\nqs = qs.exclude(archived=True)\nHow many SQL queries have been executed after these two lines in Django?',
        options: [
          'Zero: QuerySets are lazy and only hit the database when evaluated (iteration, list(), len(), bool(), etc.)',
          'Two: one per method call',
          'One: the filter executes and exclude runs in Python',
          'One: Django merges both calls into a single immediate query',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In Django, what is the practical difference between len(qs) and qs.count() on an unevaluated QuerySet?',
        options: [
          'They are identical; len delegates to count()',
          'len(qs) fetches and materializes every row then counts in Python; qs.count() issues SELECT COUNT(*) and returns just a number',
          'qs.count() caches results so later iteration is free; len(qs) does not',
          'len(qs) raises TypeError because QuerySets have no __len__',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'When should you use Django prefetch_related instead of select_related?',
        options: [
          'When you want a single SQL JOIN for a ForeignKey',
          'When the related field is a OneToOneField only',
          'Never: prefetch_related is a deprecated alias of select_related',
          'For many-to-many or reverse foreign key relations: it runs a separate query per relation and joins objects in Python, which JOIN-based select_related cannot do',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Why does Counter.objects.filter(pk=1).update(views=F("views") + 1) avoid the race condition that obj.views += 1; obj.save() has?',
        options: [
          'update() takes a table lock for the whole transaction',
          'F expressions are evaluated in Python but inside a retry loop',
          'The increment is expressed in SQL and executed atomically by the database, avoiding the read-modify-write gap between processes',
          'save() is asynchronous while update() is synchronous',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Which side effects are SKIPPED when you insert rows with Django bulk_create?',
        options: [
          'The model save() method and pre_save/post_save signals are not invoked for the created objects',
          'Database-level defaults and NOT NULL constraints are bypassed',
          'Auto primary keys are never assigned on any database backend',
          'Field validators run twice instead of once',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Inside with transaction.atomic(): you catch a DatabaseError from a failed INSERT and continue issuing ORM queries in the same block. What happens in Django?',
        options: [
          'The queries succeed because the atomic block rolls back only the failed statement',
          'TransactionManagementError: after a database error the transaction is unusable until the atomic block ends (use a nested atomic/savepoint to recover)',
          'Django silently opens a new connection and continues',
          'The whole block auto-retries up to three times',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In FastAPI, an endpoint declared as async def calls time.sleep(10) internally. Compare with the same endpoint declared as plain def. Which is true?',
        options: [
          'Both block the event loop equally; async def only changes documentation',
          'The plain def version blocks the event loop; the async def version is safe',
          'FastAPI rejects time.sleep at startup in async endpoints',
          'The async def version blocks the whole event loop for 10s; the plain def version runs in a threadpool, so other requests continue',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In FastAPI, the same dependency function is required by three sub-dependencies within one request. How many times does it execute by default?',
        options: [
          'Three times, once per declaration',
          'Zero: shared dependencies must be marked singleton',
          'Once: dependencies are cached per request (use_cache=True by default); pass use_cache=False to force re-execution',
          'Once per worker process for the entire application lifetime',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A FastAPI endpoint has response_model=UserOut, but returns a dict that also contains a hashed_password key not present in UserOut. What ends up in the HTTP response?',
        options: [
          'Only the UserOut fields: response_model filters and serializes output, dropping extra keys like hashed_password',
          'All keys, since response_model only drives the OpenAPI docs',
          'A 500 error: extra keys fail response validation',
          'All keys, but hashed_password is masked with asterisks',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'class M(BaseModel):\n    n: int\nM(n="42")\nIn Pydantic v2 default (non-strict) mode, what happens?',
        options: [
          'ValidationError: strings are never accepted for int fields',
          'Validation succeeds and m.n is the int 42, because lax mode coerces numeric strings; strict mode would reject it',
          'm.n stays the string "42" but type checkers flag it',
          'Pydantic keeps the raw string as well as a coerced copy on the model',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What does pip install -e . actually do for local development?',
        options: [
          'Copies the package into site-packages and re-copies it on every import',
          'Compiles the package to a wheel cached in ~/.cache/pip',
          'Installs in editable mode: site-packages gets a link/finder pointing at your source tree, so code edits apply without reinstalling',
          'Installs the package only for the current shell session',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What is the key installation-time difference between a wheel (.whl) and an sdist?',
        options: [
          'Wheels can only contain pure-Python code; sdists carry compiled extensions',
          'sdists install faster because they are smaller',
          'Wheels require the same Python minor version everywhere; sdists are version-independent binaries',
          'A wheel is a prebuilt distribution unpacked into place; an sdist must invoke the build backend (possibly compiling C code) on the target machine',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Two directories on sys.path both contain a folder named myco/ (no __init__.py) with different submodules inside. What does PEP 420 make possible?',
        options: [
          'myco becomes a namespace package whose portions from both directories merge under one import name',
          'ImportError: duplicate top-level package names are forbidden',
          'Only the first myco on sys.path is importable; the second is shadowed entirely',
          'Both work only if an __init__.py is added to exactly one of them',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Module a.py has top-level print("loading"). It is imported by ten different modules in one process. How many times does "loading" print, and why?',
        options: [
          'Ten times: import re-executes the module each time',
          'Once: after the first import the module object is cached in sys.modules, and later imports just return it; importlib.reload would re-execute it',
          'Twice: once at compile time and once at first import',
          'Zero: top-level code runs only under python -m',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Modules a and b import each other. a does "from b import f" at top level, and b does "import a" at top level. Which failure mode is typical and which change avoids it?',
        options: [
          'Deadlock: both modules wait on the import lock forever; only threads can break it',
          'SystemError: circular imports are detected and both modules are unloaded',
          '"from b import f" can hit a partially initialized b and raise ImportError/AttributeError; using "import b" and accessing b.f later (or importing inside the function) avoids it',
          'Both imports always succeed because Python resolves cycles by topological sort',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Why do relative imports (from . import util) work under python -m pkg.mod but raise ImportError when the same file runs as python pkg/mod.py?',
        options: [
          'python -m compiles the whole package first, making siblings visible',
          'Relative imports need at least two dots to work from scripts',
          'Running a file directly sets __package__ to "pkg" but clears sys.path',
          'Under -m the module keeps its package context (__package__ set from pkg); run as a script it is plain __main__ with no package, so there is no anchor for the relative import',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A decorator factory is defined: def tag(name): def deco(f): ... return deco. A developer writes @tag (no parentheses) above a function. What happens?',
        options: [
          'The function itself is passed to tag as the name argument, so the function is replaced by the inner deco closure, causing bizarre behavior at call time rather than a decoration error',
          'SyntaxError: decorator factories require parentheses',
          'Python detects the arity mismatch and raises TypeError at definition',
          'The decorator is applied with name defaulting to None',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'class A:\n    count = 0\n    def inc(self):\n        self.count += 1\na = A(); b = A()\na.inc(); a.inc(); b.inc()\nprint(a.count, b.count, A.count)\nWhat is printed?',
        options: [
          '3 3 3',
          '2 1 0, because self.count += 1 reads the class attribute then binds a new instance attribute',
          '2 1 3',
          '0 0 3',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'class P:\n    def __eq__(self, other):\n        return isinstance(other, P)\nWhat happens when you use P() as a dict key?',
        options: [
          'It works, using the default id-based hash',
          'It works, but all P instances collide on one bucket',
          'TypeError: unhashable type, because defining __eq__ without __hash__ sets __hash__ to None',
          'RecursionError from __eq__ calling itself',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'class A:\n    def __len__(self):\n        return 0\nprint(bool(A()))\nWhat is printed?',
        options: [
          'False, because with no __bool__ defined, truthiness falls back to __len__',
          'True, because every object without __bool__ is truthy',
          'Raises TypeError: __bool__ is required for bool()',
          '0, because bool() returns the raw __len__ result',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'class CM:\n    def __enter__(self): return self\n    def __exit__(self, *exc): return True\nwith CM():\n    raise ValueError("x")\nprint("done")\nWhat happens?',
        options: [
          'ValueError propagates; "done" never prints',
          'The with statement re-raises as RuntimeError',
          '"done" prints twice because __exit__ runs the body again',
          'Prints done: a truthy return from __exit__ suppresses the exception',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In a @contextlib.contextmanager generator, where does an exception raised inside the with block appear, and what must the generator do to guarantee cleanup?',
        options: [
          'It appears in __exit__ only; the generator never sees it',
          'It is thrown into the generator at the yield statement, so cleanup code must be in a try/finally (or except) around the yield',
          'It is swallowed automatically unless the generator re-raises',
          'It appears after the generator returns, so cleanup placed after yield always runs',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'vals = [last := x for x in range(3)]\nprint(last)\nWhat is printed?',
        options: [
          'NameError: last is local to the comprehension',
          '0, the first bound value',
          '2: the walrus target binds in the enclosing scope, unlike the loop variable x, so it survives the comprehension',
          'The list [0, 1, 2]',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A coroutine in an asyncio application calls requests.get(url) (a synchronous HTTP client). What is the consequence and the standard fix?',
        options: [
          'asyncio detects the blocking call and moves it to a thread automatically',
          'Only that one task pauses; the loop continues other tasks unaffected',
          'The call raises RuntimeError because sockets are forbidden in coroutines',
          'The entire event loop is blocked until the request finishes, starving every other task; offload with asyncio.to_thread / run_in_executor or use an async client',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Inside a running coroutine you call t = asyncio.create_task(work()) and then run a long CPU loop with no await before it finishes. When does work() begin executing?',
        options: [
          'Immediately at create_task, preempting the current coroutine',
          'Not until the current coroutine yields control at an await (or returns): create_task only schedules the task on the loop',
          'In a background thread, concurrently with the CPU loop',
          'Never, unless you also call loop.run_forever()',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
    ],
  },
};
