module.exports = {
  category: 'DevOps Engineer',
  sets: {
    1: [
      {
        question: 'A Pod has one container with requests cpu=500m, memory=256Mi and limits cpu=1, memory=512Mi. What QoS class does Kubernetes assign, and why does it matter under node memory pressure?',
        options: [
          'Guaranteed, because both requests and limits are explicitly set for CPU and memory',
          'Burstable, because requests do not equal limits; it is evicted before Guaranteed pods when usage exceeds requests',
          'BestEffort, because the limits are not enforced by the kubelet until an OOM event occurs',
          'Guaranteed, because memory limits are set; CPU values never affect QoS classification',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A container\'s readiness probe starts failing while its liveness probe keeps passing. What does the kubelet and control plane do?',
        options: [
          'The kubelet restarts the container after failureThreshold consecutive readiness failures',
          'The Pod is evicted from the node and rescheduled onto another node with healthy networking',
          'The Pod IP is removed from Service endpoints so it stops receiving traffic, but the container is not restarted',
          'The Deployment controller replaces the Pod because it no longer satisfies minReadySeconds',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A high-priority Pod triggers scheduler preemption on a node whose victim Pods are protected by a PodDisruptionBudget currently at its minAvailable floor. What happens?',
        options: [
          'Preemption treats the PDB as best-effort: the scheduler prefers victims whose PDBs are not violated, but will still evict PDB-protected Pods if needed',
          'Preemption is blocked entirely; the high-priority Pod stays Pending until the PDB allows a disruption',
          'The scheduler drains the node using the Eviction API, so the PDB hard-blocks all victim removals',
          'The PDB is ignored only if the preemptor has priorityClass system-cluster-critical',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A node crosses the kubelet hard eviction threshold memory.available<100Mi. In what order does the kubelet select Pods to evict?',
        options: [
          'Strictly by QoS class: all BestEffort first, then all Burstable, then Guaranteed, regardless of actual usage',
          'By container restart count, so crash-looping Pods are removed before stable ones',
          'Randomly among Pods without a PriorityClass, since hard eviction bypasses ranking for speed',
          'First Pods whose memory usage exceeds their requests, ranked by Pod priority and then by usage above requests',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Applications in a cluster using kube-proxy in iptables mode see intermittent 5-second DNS resolution delays. Packet captures show UDP lookups to the ClusterIP of kube-dns being lost. What is the classic root cause?',
        options: [
          'CoreDNS pods exceeding their CPU limits and being throttled by the CFS scheduler',
          'Conntrack races when parallel UDP DNS packets from the same socket are DNAT-ed, causing packet drops that force a client retry after the 5s timeout',
          'kube-proxy reprogramming iptables rules every sync period, briefly blackholing all ClusterIP traffic',
          'The node\'s /etc/resolv.conf rotating between upstream resolvers with different latencies',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'You create a RoleBinding in namespace "staging" that references the built-in ClusterRole "admin" for user alice. What access does alice gain?',
        options: [
          'Full admin on every namespace, because ClusterRoles always grant cluster-wide permissions',
          'No access, because a ClusterRole can only be referenced by a ClusterRoleBinding',
          'Admin permissions on namespaced resources only within the "staging" namespace',
          'Read-only cluster-wide access plus write access in "staging"',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Pod has terminationGracePeriodSeconds: 30 and a preStop hook that sleeps 20 seconds. Describe the shutdown sequence when the Pod is deleted.',
        options: [
          'preStop runs first; SIGTERM is sent only after preStop completes, and SIGKILL comes at the 30s deadline measured from deletion, so the app gets roughly 10s after SIGTERM',
          'SIGTERM is sent immediately in parallel with preStop, and the 30s timer starts after preStop finishes',
          'preStop runs after SIGTERM as a last-chance cleanup, then SIGKILL follows 30s later',
          'The preStop sleep extends the grace period, so the app always gets a full 30s after SIGTERM',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A LoadBalancer Service is switched to externalTrafficPolicy: Local. Which pair of behavioral changes should you expect?',
        options: [
          'Traffic is load-balanced across all cluster nodes, and client source IPs are replaced by node IPs',
          'kube-proxy adds a second SNAT hop, and the cloud LB health checks every node identically',
          'Session affinity becomes mandatory, and the healthCheckNodePort is disabled',
          'Client source IP is preserved (no SNAT to another node), and nodes without local endpoints fail the LB health check on healthCheckNodePort',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Pod defines an init container requesting 2Gi memory and two app containers requesting 512Mi each. What memory request does the scheduler use to place the Pod?',
        options: [
          '3Gi, the sum of the init container and all app containers',
          '2Gi, because the effective request is the maximum of (highest init container request) and (sum of app container requests)',
          '1Gi, because init containers are ignored for scheduling and only app containers count',
          '2.5Gi, because the largest init container is averaged with the app container total',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'An HPA scales a Deployment on a custom metric that briefly spikes every few minutes. Replicas ramp up fast but take a long time to come back down. Which built-in mechanism explains the slow scale-down?',
        options: [
          'The HPA only evaluates scale-down decisions once per hour to protect etcd',
          'The metrics adapter caches custom metric values for 15 minutes by default',
          'The default downscale stabilization window (300s) makes the HPA use the highest desired replica count computed over the window before shrinking',
          'kube-controller-manager batches all scale-down events until cluster utilization falls below 50%',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Dockerfile has "RUN apt-get update" and, on the next line, "RUN apt-get install -y nginx". Months later a rebuild adds curl to the install line and fails with 404s fetching packages. Why?',
        options: [
          'The update layer was cached, so the modified install step runs against a stale package index; update and install must be combined in one RUN',
          'apt requires --fix-missing whenever a new package is appended to an existing install command',
          'BuildKit garbage-collected the apt lists directory between the two RUN instructions',
          'The base image pinned a snapshot mirror that only allows packages present at first build',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In a Node.js Dockerfile, why does "COPY package.json package-lock.json ./" followed by "RUN npm ci" and only then "COPY . ." rebuild dependencies less often than "COPY . ." first?',
        options: [
          'npm ci runs faster when node_modules is copied from the host build context',
          'COPY . . disables the layer cache entirely for all subsequent instructions',
          'Docker compares file mtimes, and package.json changes its mtime less often than source files',
          'Layer cache keys for COPY use content checksums of the copied files, so unrelated source edits do not invalidate the dependency-install layer',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A container defines ENTRYPOINT in shell form: ENTRYPOINT node server.js. On "docker stop" the container always waits the full 10s then dies. Why?',
        options: [
          'Shell form disables the default STOPSIGNAL, so Docker skips SIGTERM and goes straight to the timeout',
          'PID 1 is /bin/sh, which does not forward SIGTERM to the node child, so the app never sees the signal and is SIGKILLed at the timeout',
          'node ignores SIGTERM unless started with --handle-signals',
          'docker stop sends SIGHUP first, which /bin/sh converts into a terminal disconnect the app ignores',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In a multi-stage Dockerfile, "ARG VERSION=1.2" is declared before the first FROM. A later stage uses "RUN echo $VERSION" and prints an empty string. Why?',
        options: [
          'ARG values are stripped from RUN instructions for security unless marked with --mount=type=secret',
          'Default values for ARG are only honored when passed explicitly with --build-arg',
          'An ARG before the first FROM is only in scope for FROM lines; each stage must redeclare "ARG VERSION" to use it inside the stage',
          'Multi-stage builds reset all ARGs after the second stage to keep layers reproducible',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'kill -9 on a process appears to do nothing: the process stays in ps output in state "D". What is happening?',
        options: [
          'The process is in uninterruptible sleep (typically blocked on I/O, e.g. a hung NFS mount); the SIGKILL is queued and only takes effect when the kernel operation returns',
          'SIGKILL was masked by the process via sigprocmask, so the kernel dropped the signal',
          'State D means the process is a zombie, and zombies can only be reaped by their parent',
          'systemd intercepted the signal because the process belongs to a protected slice',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'On cgroups v2, what is the practical difference between setting memory.high and memory.max for a service\'s cgroup?',
        options: [
          'memory.high is a hard cap enforced per-process, while memory.max applies to the whole cgroup tree',
          'memory.high only accounts anonymous pages, while memory.max also counts page cache',
          'They behave identically; memory.high exists solely for cgroups v1 compatibility',
          'Exceeding memory.high throttles the cgroup and drives reclaim without killing, while exceeding memory.max invokes the OOM killer on the cgroup',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A disk is 95% full per df, but du on every directory accounts for far less. Log rotation recently deleted several large logs. What is the most likely cause and the fastest way to confirm?',
        options: [
          'Reserved root blocks are inflating df; confirm with tune2fs -l and reduce the reserve',
          'A process still holds deleted files open, so their blocks are not freed; confirm with lsof +L1 (or lsof | grep deleted) and restart or signal the holder',
          'The filesystem journal is full; confirm with dumpe2fs and remount with data=writeback',
          'Sparse files are being double-counted by df; confirm by comparing du --apparent-size',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A systemd unit with Restart=always crash-loops, then systemd stops restarting it and marks it failed. Which mechanism caused this and how do you resume restarts immediately?',
        options: [
          'WatchdogSec expired; resume by sending sd_notify WATCHDOG=1 manually',
          'Restart=always is capped at 3 attempts per boot by kernel policy; resume by rebooting',
          'StartLimitIntervalSec/StartLimitBurst rate limiting tripped; run systemctl reset-failed <unit> then start it (or raise the limits in the unit)',
          'The unit hit TimeoutStartSec repeatedly; resume with systemctl daemon-reexec',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'You must ensure a critical agent on a Linux host is never chosen by the kernel OOM killer, even under severe memory pressure. Which setting achieves this?',
        options: [
          'Write -1000 to /proc/<pid>/oom_score_adj, which disables OOM selection for that process',
          'Set vm.overcommit_memory=2 so the OOM killer is disabled system-wide',
          'Give the process nice -20 so the scheduler protects it from OOM decisions',
          'Set RLIMIT_MEMLOCK to unlimited, which exempts locked processes from the OOM killer',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A container based on a minimal image accumulates thousands of zombie (defunct) processes over days. What is the root cause and correct fix?',
        options: [
          'The kernel PID namespace leaks PIDs under overlayfs; fix by switching the storage driver',
          'Zombies indicate the app is fork-bombing; fix by setting a pids cgroup limit',
          'The container lacks a writable /proc, so exited children cannot be cleaned; fix with privileged mode',
          'PID 1 in the container never calls wait() on orphaned children; fix by running an init (e.g. tini / --init) as PID 1 to reap them',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A service making many short-lived outbound HTTP connections to one upstream starts failing with EADDRNOTAVAIL, and ss shows tens of thousands of sockets in TIME_WAIT on the client. What is the correct primary fix?',
        options: [
          'Set tcp_tw_recycle=1 to aggressively reclaim TIME_WAIT sockets',
          'Reuse connections with HTTP keep-alive / connection pooling (optionally widen ip_local_port_range), since each closed connection parks an ephemeral 4-tuple in TIME_WAIT',
          'Lower tcp_fin_timeout to 5 so TIME_WAIT sockets expire in 5 seconds',
          'Increase somaxconn on the client so more sockets can queue',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'From inside a Kubernetes Pod, lookups for external names like api.stripe.com generate several NXDOMAIN queries (api.stripe.com.default.svc.cluster.local, etc.) before succeeding. What causes this?',
        options: [
          'CoreDNS negative caching is disabled by default, forcing recursive walks of the zone',
          'The Pod uses dnsPolicy: Default, which routes queries through the node\'s search domains',
          'The Pod resolv.conf sets ndots:5 with cluster search domains, so any name with fewer than 5 dots is tried against each search suffix before being queried as absolute',
          'kube-proxy rewrites DNS queries for Services and retries external names as a fallback',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A new TLS endpoint works in Chrome and Firefox but fails from curl and most non-browser clients with "unable to get local issuer certificate". The leaf cert is valid. What is the most likely server-side misconfiguration?',
        options: [
          'The server sends only the leaf certificate; browsers recover via AIA fetching or cached intermediates, but strict clients need the intermediate chain served by the server',
          'The certificate uses only a SAN and no Common Name, which older CLI clients require',
          'The server prefers TLS 1.3 ciphers that curl cannot negotiate without --tls-max',
          'OCSP stapling is disabled, which browsers tolerate but OpenSSL-based clients reject',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Clients behind an AWS ALB report sporadic 502s under steady load. The backend app closes idle keep-alive connections after 5s; the ALB idle timeout is 60s. What is the likely cause and fix?',
        options: [
          'ALB retries non-idempotent requests by default; disable retry on the target group',
          'The ALB requires HTTP/2 to reuse backend connections; enable h2 on the target group',
          'The app\'s 5s keep-alive causes TLS renegotiation storms; switch the target group to HTTPS',
          'The backend closes connections the ALB still considers reusable, so requests race onto dying sockets; set the app keep-alive timeout longer than the ALB idle timeout',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'During a traffic surge, clients see connection timeouts to a service although the host CPU is low. "ss -lnt" shows the listen socket\'s Recv-Q at or near its Send-Q value. What does this indicate?',
        options: [
          'The NIC ring buffer is overflowing; increase rx descriptors with ethtool -G',
          'The accept queue (listen backlog, capped by net.core.somaxconn) is full because the app accepts too slowly, so new handshakes are dropped or reset',
          'conntrack is full, so SYNs are dropped before reaching the socket',
          'TCP fast open is exhausting cookie space; disable tcp_fastopen',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A security engineer sets the setuid bit on a root-owned Bash script (chmod 4755 deploy.sh) so operators can run it with elevated rights, but it still executes with the caller\'s privileges. Why?',
        options: [
          'setuid requires the sticky bit to be set as well on scripts',
          'The script\'s shebang points to /bin/bash instead of /bin/sh, which strips privileges',
          'The Linux kernel ignores the setuid bit on interpreted scripts because the interpreter is executed separately, making setuid scripts a known-unsafe pattern',
          'Bash must be compiled with --enable-setuid for this to work',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A CI job container is run with -v /var/run/docker.sock:/var/run/docker.sock to build images. What is the real security impact of this mount?',
        options: [
          'Anything in that container can instruct the host Docker daemon to start privileged containers or mount the host filesystem, i.e. it is effectively root on the host',
          'The container can list images but cannot create containers without also mounting /var/lib/docker',
          'Risk is limited to the CI container\'s own cgroup because the daemon enforces namespace boundaries per client',
          'It only becomes dangerous if the container also runs with --privileged',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Pod spec sets securityContext.runAsNonRoot: true. The image\'s Dockerfile ends with "USER appuser" (a named user). The Pod fails to start with "container has runAsNonRoot and image has non-numeric user". Why?',
        options: [
          'runAsNonRoot only supports users defined in the node\'s /etc/passwd, not image-local users',
          'The kubelet requires named users to also exist in a ServiceAccount before admission',
          'The image must define USER before the final FROM stage for the metadata to persist',
          'The kubelet cannot verify a named user is non-root without the image\'s /etc/passwd at validation time; use a numeric USER in the image or set runAsUser in the Pod spec',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In sshd_config you set "PermitRootLogin prohibit-password". What exactly does this allow and forbid for the root account?',
        options: [
          'Root may log in only from hosts listed in /etc/hosts.allow, with any auth method',
          'Root may authenticate with public keys (and other non-password methods) but never with a password or keyboard-interactive password',
          'Root is fully blocked from SSH; the value is an alias for "no"',
          'Root may log in with a password only if the connection originates from localhost',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A compliance review flags that Kubernetes Secrets are "encrypted". What is the accurate statement about Secret storage in a default kubeadm-style cluster?',
        options: [
          'Secrets are encrypted with the cluster CA key, so compromising etcd alone is insufficient',
          'Secrets are hashed in etcd; only the kubelet can reconstruct the plaintext',
          'Secrets are only base64-encoded in etcd by default; real encryption at rest requires configuring an EncryptionConfiguration (e.g. aescbc or a KMS provider) on the API server',
          'Secrets are encrypted per-namespace with keys derived from the namespace UID',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
    ],
    2: [
      {
        question: 'You run "terraform state rm aws_s3_bucket.logs" to stop managing a bucket. What happens to the bucket and what will the next terraform apply do (given the resource block still exists in code)?',
        options: [
          'The bucket is destroyed immediately and recreated on the next apply',
          'The bucket is destroyed on the next apply because state removal marks it tainted',
          'Nothing changes until you run terraform refresh, which re-imports it automatically',
          'The bucket keeps existing untouched, but the next apply tries to create a new one and fails with a name conflict because state no longer tracks it',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Terraform module creates 5 subnets with count over a list of CIDRs. You delete the 2nd CIDR from the middle of the list. What does terraform plan show?',
        options: [
          'Destruction/recreation cascades from index 1 onward, because count-indexed addresses shift when a middle element is removed; for_each keyed by CIDR would avoid this',
          'Only the 2nd subnet is destroyed; Terraform matches remaining resources by attribute diffing',
          'An in-place update of the 2nd subnet\'s CIDR with no other changes',
          'A plan error, because count lists cannot shrink without terraform state mv',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'You add lifecycle { create_before_destroy = true } to an aws_launch_template consumed by an ASG, but applying a change to a uniquely named IAM role with the same lifecycle fails. What is the classic pitfall of create_before_destroy?',
        options: [
          'It requires depends_on to be set on every downstream resource, otherwise the graph inverts',
          'It only works on resources with fewer than two references in state',
          'The replacement resource is created while the old one still exists, so resources with fixed unique names (or other uniqueness constraints) collide unless names are generated (e.g. name_prefix)',
          'It silently downgrades to destroy-then-create when the provider does not support tainting',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'During an incident someone runs "terraform apply -target=aws_ecs_service.api" repeatedly for a week. What long-term risk does habitual -target use create?',
        options: [
          'It locks the targeted resource\'s state entry so full applies cannot modify it later',
          'Untargeted resources and dependency updates are skipped, so state and reality drift and the next full apply produces a large, surprising diff',
          'Each -target apply forks the state file, requiring a manual merge with terraform state push',
          '-target disables the provider plugin cache, slowly corrupting .terraform.lock.hcl',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A CI job died mid-apply and now every terraform command fails with a state lock error (S3 backend with DynamoDB locking). What is the safe remediation?',
        options: [
          'Delete the state object from S3; the lock is stored inside the state file itself',
          'Re-run terraform apply with -lock=false, which clears the stale lock on success',
          'Rotate the backend credentials, which invalidates all outstanding lock leases',
          'Confirm no apply is actually still running, then run terraform force-unlock <lock-id>; forcing while another apply runs risks state corruption',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'An external autoscaler modifies desired_count on an ECS service that Terraform also manages, so every plan shows a diff. What is the idiomatic Terraform fix?',
        options: [
          'Add lifecycle { ignore_changes = [desired_count] } so Terraform stops reconciling that attribute while still managing the rest of the resource',
          'Set desired_count = null in the resource, which tells the provider to skip the field',
          'Run terraform apply -refresh-only after each autoscaling event to accept the new value',
          'Move desired_count into a data source so it is read instead of managed',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Terraform output is marked sensitive = true for a database password. What protection does this actually provide?',
        options: [
          'The value is encrypted inside the state file using the backend\'s KMS key',
          'The value is stored only in memory during apply and never written to state',
          'It only redacts the value from CLI/plan output; the plaintext still lives in the state file, so state storage itself must be secured/encrypted',
          'It restricts terraform output access to users with the "sensitive" workspace permission',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A resource has lifecycle { prevent_destroy = true }. Someone deletes the entire resource block from the .tf files and runs apply. What happens?',
        options: [
          'Apply fails, because prevent_destroy persists in state and blocks any delete',
          'prevent_destroy no longer protects it: once the block (and thus the lifecycle setting) is removed from config, the plan proposes destroying the resource',
          'Terraform automatically converts the resource to a data source to preserve it',
          'The resource is removed from state but kept in the cloud, like terraform state rm',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A CI pipeline caches node_modules keyed on the branch name. Builds pass on the feature branch but dependency bugs appear after merging to main. What cache key design prevents this class of problem?',
        options: [
          'Key the cache on the CI runner\'s hostname so each machine keeps a consistent copy',
          'Use a single global cache key and rely on npm install to reconcile differences',
          'Key the cache on the pipeline run number so every build gets a pristine cache',
          'Key the cache on a hash of the lockfile (e.g. package-lock.json), so any dependency change misses the cache and identical lockfiles share it across branches',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A GitHub Actions workflow uses "on: pull_request_target" and checks out the PR head ref to run its tests. Why do security teams treat this pattern as critical?',
        options: [
          'pull_request_target runs with the base repo\'s secrets and write token, so executing attacker-controlled PR code (checked-out head) lets a fork exfiltrate secrets or push code',
          'It bypasses branch protection because target events skip required status checks',
          'It allows the PR author to edit the workflow file itself, which then runs unreviewed',
          'It runs on a privileged self-hosted runner by default instead of a GitHub-hosted VM',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A canary release strategy requires a database schema change that renames a column. What migration approach keeps both canary and stable versions healthy during the rollout?',
        options: [
          'Rename the column in a pre-deploy migration and pin the stable version to a read replica',
          'Run the rename inside a transaction so both app versions see a consistent snapshot',
          'Expand-and-contract: add the new column, dual-write/backfill, ship code reading the new column, and only drop/rename the old column after the rollout completes',
          'Ship the rename with the canary and rely on the ORM to alias the old column name',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A team\'s pipeline rebuilds the application image from source in every environment stage (dev, staging, prod). Which deployment-integrity principle does this violate?',
        options: [
          'Trunk-based development, because rebuilding requires long-lived release branches',
          'Build once, promote the same immutable artifact: per-stage rebuilds can differ (base image drift, dependency resolution), so what was tested is not what ships',
          'Blue-green deployment, because two images cannot share a target group',
          'Least privilege, because each stage needs source-repo credentials',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A GitHub Actions deploy workflow sets "concurrency: { group: prod-deploy, cancel-in-progress: true }". What failure mode does cancel-in-progress introduce for deployments specifically?',
        options: [
          'Queued runs are silently dropped instead of executing after the current run',
          'The concurrency group blocks all other workflows in the repository while a deploy runs',
          'Cancelled runs still hold the group lock for their full timeout, serializing everything',
          'A newer push can kill a deploy mid-flight (e.g. between infra mutation steps), leaving the environment half-updated; deploys usually want queuing without cancellation',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A release pipeline computes the version with "git describe --tags" but in CI it fails or returns only a commit hash, while working locally. What is the usual cause?',
        options: [
          'CI performed a shallow clone (fetch-depth: 1) without tags, so the tag history git describe needs is absent; fetch tags/full depth or set fetch-depth: 0',
          'git describe requires an annotated HEAD commit, which CI merge commits never are',
          'The CI runner\'s git version defaults to describing branches instead of tags',
          'Detached HEAD state disables tag resolution unless GIT_DIR is exported',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'An AWS identity has an IAM policy allowing s3:GetObject on a bucket, but requests fail with AccessDenied. The bucket belongs to the same account. Which explanation is consistent with IAM evaluation logic?',
        options: [
          'Identity policies are ignored for S3; only bucket ACLs grant object access',
          'The allow must exist in both an identity policy and the bucket policy for same-account access',
          'An explicit Deny (e.g. in an SCP, permissions boundary, or bucket policy, often via a condition like aws:SourceVpce) overrides the Allow, since deny always wins in policy evaluation',
          'The policy is attached to a group, and group policies only apply to console sessions',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'After enforcing IMDSv2 on EC2 instances, applications running in Docker containers on those instances can no longer retrieve credentials from the instance metadata service. What is the cause?',
        options: [
          'IMDSv2 requires TLS, and containers lack the Amazon root CA bundle',
          'The default IMDSv2 hop limit of 1 causes the PUT token response to be dropped after the extra network hop through the docker bridge; raise HttpPutResponseHopLimit to 2',
          'IMDSv2 authenticates callers by process UID, and containerized processes map to an unknown UID',
          'Docker\'s NAT rewrites the X-aws-ec2-metadata-token header, invalidating the session token',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'You lock down a subnet with a Network ACL allowing inbound TCP 443 only. Immediately, responses to outbound requests initiated from instances in the subnet start failing. Security groups are unchanged. Why?',
        options: [
          'NACLs require an explicit inbound rule for ICMP before any TCP traffic can return',
          'NACLs evaluate rules bottom-up, so the default deny is now matched first',
          'The NACL blocked the subnet\'s route table update, detaching the internet gateway',
          'NACLs are stateless: return traffic to outbound connections arrives on ephemeral ports (1024-65535), which the inbound rules no longer allow, unlike stateful security groups',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'An ASG behind an ALB terminates instances during scale-in while they are still finishing requests, despite connection draining. Which mechanism lets you run cleanup (e.g. flush logs, finish jobs) before termination proceeds?',
        options: [
          'An autoscaling lifecycle hook on instance termination, which holds the instance in Terminating:Wait until the hook is completed or times out',
          'Raising the target group deregistration delay above the job duration, which also delays instance shutdown',
          'Enabling instance scale-in protection permanently on all instances in the group',
          'A termination policy of OldestLaunchConfiguration, which drains connections first',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A latency-sensitive AWS Lambda experiences sporadic multi-second p99 spikes at traffic ramp-ups, plus throttles when a burst arrives. Which combination correctly addresses cold starts versus throttling?',
        options: [
          'Reserved concurrency removes cold starts by keeping instances warm; provisioned concurrency raises the account limit',
          'Raising memory removes throttles because concurrency is computed from memory size',
          'Provisioned concurrency pre-initializes execution environments to cut cold starts, while reserved concurrency carves out (and caps) concurrency so other functions cannot starve it — reserved alone does not pre-warm',
          'Both settings are equivalent; the only difference is billing granularity',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A database on a 500GB gp2 EBS volume performs fine for weeks, then IOPS collapse to a fraction of previous levels during a sustained batch job and latency soars. What happened?',
        options: [
          'EBS throttles any volume exceeding 80% space utilization until it is resized',
          'The volume exhausted its burst credit balance and fell back to its baseline IOPS (3 IOPS/GB on gp2); moving to gp3 with provisioned IOPS decouples performance from size',
          'The instance\'s EBS-optimized bandwidth resets monthly and had expired',
          'gp2 volumes deprioritize sequential writes after sustained load to protect the fleet',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In GCP, a folder-level IAM binding grants roles/editor to a group. A project admin wants to block that access for one sensitive project inside the folder. What is true about GCP IAM inheritance?',
        options: [
          'Setting an empty IAM policy on the project clears inherited folder bindings',
          'Project-level "deny" is achieved by granting roles/viewer, which downgrades inherited roles',
          'Removing the group from the project policy is enough; project policies shadow folder policies',
          'The effective policy is the union of bindings up the hierarchy, so folder grants cannot be revoked at project level; you need IAM Deny policies or restructuring (move the project / narrow the folder grant)',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'On GKE, pods reach Google APIs with unexpectedly broad permissions. Investigation shows they use the node\'s default Compute Engine service account. What is the recommended fix?',
        options: [
          'Enable Workload Identity so each KSA maps to a dedicated GSA, and block the metadata server path pods were using to inherit node credentials',
          'Attach scopes to the node pool restricting it to logging.write, which pods then inherit safely',
          'Mount per-pod JSON service account keys from Secret Manager and rotate them daily',
          'Switch node pools to spot VMs, which run with an unprivileged service account by design',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A batch workload runs on GCP Spot VMs. What termination contract must the workload design around?',
        options: [
          'A 2-minute warning via the metadata server, identical to AWS spot interruption notices',
          'Spot VMs are only reclaimed at the top of each hour, so checkpoints can align to the clock',
          'A preemption signal (ACPI G2 soft-off / preempted metadata) with roughly 30 seconds to shut down before the VM is stopped, and no guarantee it will be restarted',
          'Spot VMs cannot be reclaimed while CPU utilization exceeds 50%',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'You cut a DNS record\'s TTL from 3600s to 60s, wait a day, then repoint the record during a migration. Some clients still hit the old IP hours later. Which explanation is most plausible?',
        options: [
          'The authoritative server keeps serving the old value until its SOA expire timer lapses',
          'Some resolvers and applications (e.g. JVMs with default caching, corporate resolvers enforcing minimum TTLs, OS-level caches) ignore or clamp low TTLs, so stale answers outlive the published TTL',
          'Recursive resolvers refresh records only when the serial in the SOA changes, which requires a zone transfer',
          'Negative caching of the old record blocks the new one until the NXDOMAIN TTL expires',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Long-lived, mostly idle TCP connections between services in different clouds die silently: no FIN or RST is seen, and the next write hangs for minutes. What is the standard explanation and mitigation?',
        options: [
          'TLS session tickets expired, so the connection must be re-handshaked; enable ticket rotation',
          'The remote peer entered TIME_WAIT and dropped the socket; lower tcp_fin_timeout',
          'Path MTU shrank mid-connection, blocking large segments; disable PMTUD',
          'A stateful middlebox (NAT/firewall) expired the idle flow mapping and silently drops packets; enable TCP keepalives (or app-level heartbeats) at an interval below the middlebox idle timeout',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Over a new site-to-site VPN, small API responses succeed but any response over ~1400 bytes hangs until timeout. Ping works at default size. What is the diagnosis?',
        options: [
          'A PMTUD blackhole: the tunnel lowers effective MTU, and ICMP Fragmentation Needed is filtered, so large packets vanish; fix with MSS clamping or lowering interface MTU',
          'The VPN\'s IKE rekey interval interrupts long transfers; extend the SA lifetime',
          'TCP window scaling is disabled across the tunnel, capping segment size at 1400 bytes',
          'The tunnel encapsulates only the first fragment of each datagram by design',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A script begins with set -e, then runs:\ncount=$(grep -c ERROR app.log)\necho "errors: $count"\nWhen app.log contains no ERROR lines, the script exits silently before the echo. Why?',
        options: [
          'set -e aborts whenever any command writes to stderr, and grep warns on zero matches',
          'Command substitution runs in a subshell where set -e is inherited twice, doubling the failure',
          'grep -c exits 1 when it finds zero matches, and set -e treats the assignment\'s non-zero substitution exit status as fatal; guard it (e.g. || true) or check explicitly',
          'The assignment fails because $(...) returns an empty string, which set -e treats as unset',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In Bash, what does "set -o pipefail" change about the exit status of "cmd1 | cmd2 | cmd3"?',
        options: [
          'The pipeline exits with the status of cmd1, since it is the data producer',
          'Without pipefail the pipeline\'s status is cmd3\'s alone; with pipefail it becomes the last non-zero status of any stage, so upstream failures are no longer masked',
          'pipefail makes the shell abort the pipeline as soon as any stage writes to stderr',
          'pipefail sums the exit codes of all stages, producing a composite status',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A deployment script sets "trap cleanup EXIT" and later runs a critical section inside parentheses: ( do_migration ). If do_migration is killed, cleanup runs twice in some versions of the script. What subtlety explains double or surprising trap execution with subshells?',
        options: [
          'Bash queues EXIT traps per file descriptor, so redirections multiply executions',
          'The EXIT trap converts to an ERR trap inside subshells, firing on every non-zero command',
          'Subshells always re-source the script from the top, re-registering the trap',
          'Subshells inherit the EXIT trap at creation in some shells/modes, so the trap can fire when the subshell exits and again when the main shell exits; reset traps inside subshells or avoid inheriting them',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A CI job deploys manifests with "kubectl apply -f dir/ --prune -l app=web". Engineers report an unrelated Service in the same namespace was deleted during a deploy. What makes --prune dangerous here?',
        options: [
          'Prune deletes any live object matching the label selector (and prune whitelist) that is absent from the applied manifest set, so mislabeled or externally created objects get swept; scoping labels strictly or using ApplySet/GitOps controllers is safer',
          'Prune ignores label selectors and always deletes all unmanaged objects in the namespace',
          'Prune only considers objects created by the same kubectl version, deleting newer ones',
          'Prune deletes objects whose last-applied-configuration annotation is larger than 256KB',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
    ],
    3: [
      {
        question: 'A Prometheus counter is reset when its pod restarts mid-window. How does rate(http_requests_total[5m]) handle the reset, and what artifact can still appear?',
        options: [
          'rate() returns NaN for any window containing a reset, gapping the graph',
          'rate() detects the counter drop and compensates by assuming a reset from zero, but if the counter climbed and reset between scrapes, the increase during that gap is undercounted',
          'rate() adds the pre- and post-reset values, double-counting the overlap',
          'rate() ignores the reset entirely because counters are monotonic by contract',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'With a 30s scrape interval, the query rate(errors_total[1m]) intermittently returns no data for some series. Why is the range too small?',
        options: [
          'rate() requires the range to be a multiple of the evaluation interval, and 1m is not aligned',
          'Ranges under 2m are rounded down to instant vectors, which rate() rejects',
          'rate() needs at least two samples in the window; with 30s scrapes, a 1m window can contain only one sample after any scrape jitter or a failed scrape (rule of thumb: range >= 4x scrape interval)',
          'The lookback delta caps range vectors at twice the scrape interval',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'To get a service-wide p99 from a Prometheus histogram exported by many pods, which query is correct?',
        options: [
          'histogram_quantile(0.99, sum by (le) (rate(http_request_duration_seconds_bucket[5m]))) — buckets must be aggregated preserving the le label before computing the quantile',
          'avg(histogram_quantile(0.99, rate(http_request_duration_seconds_bucket[5m]))) — averaging per-pod p99s yields the fleet p99',
          'histogram_quantile(0.99, rate(http_request_duration_seconds_sum[5m])) — the sum series carries the latency distribution',
          'quantile(0.99, http_request_duration_seconds_count) — count series are sufficient for quantiles',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'An alert rule "expr: up == 0" never fires when an entire scrape job\'s targets are deleted from service discovery. Why, and what is the robust pattern?',
        options: [
          'up is only generated for healthy targets, so the expression is inverted; use up != 1',
          'The rule needs "for: 0s"; any non-zero for-duration suppresses alerts on deleted targets',
          'Deleted targets emit up == -1, which the comparison misses',
          'When targets vanish, their series go stale and return no samples at all — a comparison on nothing matches nothing; alert on absence with absent() (or absent_over_time) per job',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'For a 99.9% availability SLO over 30 days, a multiwindow burn-rate alert pages at burn rate 14.4 over 1h (with a 5m short window). What does the 14.4 figure represent?',
        options: [
          'The number of nines of the SLO multiplied by the days in the window, a heuristic constant',
          'The rate at which the error budget is being consumed such that continuing for 1h burns about 2% of the 30-day budget — fast enough to justify paging rather than a ticket',
          'The maximum tolerated error percentage during any single hour under the SLO',
          'The ratio between the short and long alert windows required to suppress flapping',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A service has a 99.9% monthly availability SLO. Roughly how much total downtime per 30-day month does the error budget allow, and what does exhausting it typically trigger in an SRE model?',
        options: [
          'About 4.3 hours; exhausting it triggers an automatic SLO relaxation for the next month',
          'About 4.3 minutes; exhausting it pages the on-call for every subsequent error',
          'About 43 minutes; exhausting it typically freezes risky releases and shifts effort to reliability work until the budget recovers',
          'About 7.2 hours; exhausting it only affects reporting dashboards',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A dashboard computes fleet error ratio as avg(rate(errors[5m]) / rate(requests[5m])) across instances. A low-traffic instance with 90% errors dominates the panel while overall user impact is tiny. What is the correct aggregation?',
        options: [
          'sum(rate(errors[5m])) / sum(rate(requests[5m])) — aggregate numerators and denominators separately so instances are weighted by traffic, instead of averaging per-instance ratios',
          'max(rate(errors[5m])) / min(rate(requests[5m])) — bounding the worst case is the standard',
          'avg_over_time of the existing expression, which smooths out low-traffic outliers',
          'median(rate(errors[5m]) / rate(requests[5m])) — the median ratio removes skew correctly',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A recording rule computes job:latency:p99 from raw histograms, and an alert references the recorded series. After renaming the underlying metric, alerts silently stopped evaluating meaningfully. What Prometheus behavior makes this failure quiet?',
        options: [
          'Recording rules cache their last value for 24h, masking upstream breakage for a day',
          'The rule engine converts broken expressions into constant zeros to keep dashboards stable',
          'Renamed metrics keep emitting under both names until the retention window expires',
          'A rule whose expression matches no series simply produces no output — no error is raised — and alerts on the missing recorded series also match nothing; you need meta-monitoring (absent() on recorded series, rule health metrics)',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'During a Sev-1, an engineer hot-patches a Deployment with kubectl to mitigate, but Argo CD reverts the change within minutes and the outage resumes. Which Argo CD feature caused this and what is the safer emergency workflow?',
        options: [
          'The sync webhook re-applies on every cluster event; the fix is deleting the webhook',
          'Automated sync with selfHeal reverts live drift to Git; during incidents either disable auto-sync/selfHeal for the app or land the mitigation through Git so the desired state matches',
          'Argo CD\'s resource hooks garbage-collect patched fields; annotate the Deployment with Prune=false',
          'App-of-apps reconciliation resets child apps hourly; move the app to the root project',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A team wants to store Kubernetes Secret manifests in a public-readable Git repo for GitOps. Which approach is actually safe for this constraint?',
        options: [
          'Base64-encode the Secret data field, since Git hosting encrypts repositories at rest',
          'Store secrets as Helm values files with restrictive file permissions in the repo',
          'Encrypt secret values in-repo (e.g. Sealed Secrets, which only the in-cluster controller can decrypt, or SOPS with KMS) so Git never holds usable plaintext',
          'Reference secrets by name only and let kubectl prompt for values at apply time',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'An Argo CD application must create a CRD, wait for it to be established, then create custom resources of that kind, then run a schema migration Job before the app rolls out. Which Argo CD mechanisms sequence this correctly?',
        options: [
          'Sync waves (argocd.argoproj.io/sync-wave) to order resources across phases, plus resource hooks (e.g. PreSync for the migration Job); Argo also handles CRD-before-CR ordering within a wave',
          'Alphabetical manifest filenames, which Argo applies in lexical order like kubectl',
          'Setting the app\'s revision history limit so retries replay resources in order',
          'A single sync with prune enabled, which topologically sorts on ownerReferences',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A GitOps-managed Deployment references image myapp:latest. The team pushes a new image build to the same tag but Argo CD reports Synced and nothing redeploys. Why?',
        options: [
          'Argo CD pins images by digest automatically, so tags are ignored after first sync',
          'The image pull policy defaults to Never for GitOps-managed workloads',
          'latest is a reserved tag that Kubernetes resolves only at namespace creation',
          'The desired state in Git is unchanged — same tag string — so there is no drift to reconcile; mutable tags break GitOps, use immutable tags/digests (optionally with an image updater writing back to Git)',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In a formal incident command structure, the incident commander of a Sev-1 starts personally debugging the database. Why is this considered an anti-pattern?',
        options: [
          'Debugging requires production access, which commanders must surrender during incidents',
          'The IC role is coordination: maintaining state, delegating workstreams, managing comms and decisions; an IC who dives into debugging stops coordinating, so handoff or delegation is required',
          'ICs are legally accountable for changes made during the incident, creating liability',
          'Database work is always owned by the scribe under ICS conventions',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A postmortem draft says: "The outage happened because the engineer failed to check the dashboard before deploying." What is the blameless-postmortem critique of this sentence?',
        options: [
          'It should name the engineer explicitly so accountability is traceable',
          'It understates severity; postmortems must quantify error-budget impact in the causes section',
          'It attributes cause to individual error and counterfactual behavior instead of asking why the system allowed it — e.g. why the deploy pipeline had no automated check or gate; fixes should target the system',
          'It is acceptable as long as the engineer reviews and signs the document',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'An on-call rotation receives ~40 pages a week, mostly for conditions like "CPU > 80%" that rarely correspond to user impact. What is the accepted remediation philosophy?',
        options: [
          'Page only on symptoms of user impact (SLO-threatening error rates, latency); route cause-based, non-urgent signals to tickets/dashboards — sustained noise trains responders to ignore pages',
          'Raise thresholds uniformly by 10% each week until page volume halves',
          'Split every alert across two rotations so no individual exceeds 20 pages',
          'Convert all pages to auto-remediation scripts before reviewing their validity',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A downstream dependency slows to 5s per request. Upstream services with 3 aggressive retries each begin timing out, and the dependency collapses entirely. Which design flaw amplified the failure and what mitigates it?',
        options: [
          'Missing horizontal autoscaling on the dependency; larger minReplicas absorbs retry load',
          'Retry counts were too low to ride out latency; raising retries to 5 restores throughput',
          'Load balancer health checks were too lax; failing hosts faster shrinks the blast radius',
          'Retry amplification: layered retries multiply load on an already-degraded service; mitigate with exponential backoff plus jitter, retry budgets, circuit breakers, and deadline propagation',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A cleanup script runs: find /data -name "*.tmp" | xargs rm. It occasionally deletes the wrong files on this system. Which filename characteristic breaks this pipeline, and what is the robust form?',
        options: [
          'Symlinked names make xargs follow links outside /data; add -H to find',
          'Filenames containing whitespace, quotes, or newlines are split/mangled by xargs; use find -print0 | xargs -0 rm (or find -delete / -exec rm {} +)',
          'Files larger than ARG_MAX are skipped silently; use --max-chars',
          'Hidden dotfiles are expanded by the shell before find runs; quote the path instead',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Bash script does: if [ $(date +%m) -eq 08 ]; then ...; fi\nand starts failing every August and September... wait, specifically with "value too great for base". What is wrong?',
        options: [
          '-eq cannot compare zero-padded strings; only == handles leading zeros',
          'date +%m emits locale-dependent month names in some LANG settings',
          'In arithmetic contexts, a leading zero marks octal, and 08/09 are invalid octal digits; strip the pad (date +%-m) or force base 10 (10#$month)',
          'The [ builtin caps operands at 7 bits, so any two-digit month overflows',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In Bash: local output=$(critical_command); if [ $? -ne 0 ]; then rollback; fi — rollback never runs even when critical_command fails. Why?',
        options: [
          '$? reflects the exit status of "local", which succeeds regardless of the substitution\'s failure; declare/assign separately (local output; output=$(critical_command)) so $? sees the command',
          'Command substitution always returns 0 unless the shell itself crashes',
          'local suppresses subshell signals, so failures inside $( ) are converted to success',
          '[ $? -ne 0 ] resets $? before comparing; use (( $? )) instead',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A script running under set -u crashes with "unbound variable" on: for f in "${FILES[@]}"; do ... — but only on some hosts. What explains the host-dependent behavior?',
        options: [
          'set -u only applies to arrays when IFS is unset, which varies by distro profile',
          'Hosts with associative-array support silently convert FILES to a map, unsetting index 0',
          'The for loop copies the array into the environment, hitting ARG_MAX on smaller hosts',
          'In Bash before 4.4, expanding an empty array with "${arr[@]}" trips set -u (treated as unbound), while newer Bash allows it; guard with ${FILES[@]+"${FILES[@]}"} or check the length first',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A team switches production images from ubuntu-based to distroless. Which security claim about this change is accurate?',
        options: [
          'Distroless images are immune to library CVEs because they contain no OS packages at all',
          'Removing the shell and package manager shrinks the attack surface and hampers post-exploitation (no /bin/sh for reverse shells, nothing to install), but the app runtime and its libraries can still carry CVEs',
          'Distroless enforces read-only root filesystems at the kernel level',
          'Distroless images cannot run as root, eliminating privilege-escalation paths',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'To default-deny all ingress traffic to every pod in a namespace, you apply a NetworkPolicy with podSelector: {} and policyTypes: [Ingress] and no ingress rules. Which statement about the semantics is correct?',
        options: [
          'podSelector: {} matches no pods, so the policy is a no-op until pods are labeled',
          'The policy blocks ingress and egress, since omitting egress rules denies both directions',
          'The empty podSelector selects all pods in the namespace, and once a pod is selected by any Ingress policy, only explicitly allowed ingress is admitted — so this denies all ingress (assuming the CNI enforces NetworkPolicy)',
          'The policy also blocks kubelet health probes, so all pods will fail liveness checks',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A container must bind port 80 as a non-root user with the tightest capability set. Which securityContext approach is correct on a modern kernel/runtime?',
        options: [
          'Drop ALL capabilities and add back only NET_BIND_SERVICE (or rely on runtimes/kernels where unprivileged binding below 1024 is allowed / sysctl net.ipv4.ip_unprivileged_port_start=0); avoid granting broad caps or root',
          'Set privileged: true but run as UID 1000, which limits the damage of the privilege',
          'Add CAP_SYS_ADMIN, the capability governing network binds below 1024',
          'Run as root but set allowPrivilegeEscalation: false, which is equivalent to dropping capabilities',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A workflow uses "uses: some-org/some-action@v3". Security review demands pinning to a full commit SHA instead. What attack does SHA-pinning actually prevent?',
        options: [
          'Typosquatting of the action name in the marketplace search index',
          'Runner privilege escalation via the actions cache poisoning path',
          'Man-in-the-middle substitution of the action tarball during download',
          'A compromised or malicious maintainer force-moving the v3 tag (tags are mutable) to point at malicious code that then runs with your workflow\'s secrets; a commit SHA is immutable',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'You scale a StatefulSet from 5 replicas to 3 and later back to 5. What happens to the PersistentVolumeClaims of pods 3 and 4 in a default configuration?',
        options: [
          'The PVCs are deleted at scale-down and fresh empty volumes are provisioned at scale-up',
          'The PVCs are retained at scale-down, and the recreated pods reattach to their original PVCs (and data) by ordinal naming; PVC cleanup requires explicit deletion or a retention policy',
          'The PVCs enter a Released state and must be manually rebound with a volumeName patch',
          'Scale-down is blocked until the PVCs are removed, because StatefulSets protect volumes',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A namespace deletion hangs for hours in Terminating. kubectl get all shows nothing left in it. What is the most common cause and correct diagnostic path?',
        options: [
          'etcd compaction is pending; deletion completes after the next compaction cycle',
          'The namespace controller waits for all PVs in the cluster to be reclaimed first',
          'Either objects with unremoved finalizers remain (get all misses many kinds; list via api-resources) or an aggregated APIService is unavailable, blocking the discovery the namespace controller needs; inspect the namespace status conditions',
          'A NetworkPolicy referencing the namespace must be deleted from every other namespace',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A pod in CrashLoopBackOff restarts immediately at first, then the gaps between restarts grow. What is the kubelet\'s backoff behavior?',
        options: [
          'Exponential backoff on restarts (10s, 20s, 40s, ...) capped at 5 minutes, reset after a container runs successfully for 10 minutes',
          'Linear backoff adding 30s per failure with no cap until the pod is deleted',
          'Fixed 60s delay between restarts once the pod has failed 3 times',
          'Backoff timing is controlled by the Deployment\'s progressDeadlineSeconds field',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In a 3-member etcd cluster backing a Kubernetes control plane, two members go down. What is the cluster\'s condition?',
        options: [
          'etcd continues serving reads and writes from the surviving member until a second failure',
          'The surviving member self-promotes after the election timeout and restores full service',
          'Only Kubernetes Secrets become unavailable, since they require quorum encryption',
          'Quorum (2 of 3) is lost: etcd cannot commit writes or elect a leader, so the API server cannot persist changes; existing workloads keep running but the control plane is effectively read-degraded/frozen',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'After adding a user_id label to a popular request metric, Prometheus memory ballooned and queries slowed drastically. What is the underlying mechanism?',
        options: [
          'Labels are stored as uncompressed strings in the WAL, growing disk linearly with requests',
          'Each unique label combination creates a distinct time series; unbounded-cardinality labels (user IDs, request IDs) explode the series count, inflating index/head memory — keep such data in logs/traces, not metric labels',
          'PromQL evaluates one goroutine per label value, exhausting the scheduler',
          'High-cardinality labels force full-table scans in the remote-write queue',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A busy NAT/ingress Linux host intermittently drops new connections; dmesg shows "nf_conntrack: table full, dropping packet". Which pair of actions addresses this correctly?',
        options: [
          'Disable SYN cookies and raise somaxconn, since the accept queue is the real bottleneck',
          'Set tcp_tw_reuse=0 to keep old flows alive longer so fewer new entries are created',
          'Raise net.netfilter.nf_conntrack_max (with hashsize) and/or reduce timeout for idle/DNS-style flows — or bypass conntrack (NOTRACK) for high-volume stateless traffic',
          'Lower the MTU so each flow consumes fewer conntrack cells per packet',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
    ],
  },
};
