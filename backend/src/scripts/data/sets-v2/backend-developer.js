module.exports = {
  category: 'Backend Developer',
  sets: {
    1: [
      {
        question: 'A Dynamo-style store uses N=5, W=3, R=3. A network partition splits the replicas into groups of 3 and 2. Assuming strict quorums, what is the observable behavior until the partition heals?',
        options: [
          'Both sides keep accepting writes and reconcile later via read repair',
          'Neither side can write because W=3 requires acknowledgement from every replica',
          'The 3-node side keeps serving quorum reads and writes; the 2-node side can satisfy neither, sacrificing availability there',
          'The 2-node side serves stale reads while the 3-node side blocks all writes until healing',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In Raft, a newly elected leader finds an entry from a previous term replicated on a majority of nodes but not marked committed. Per the Raft safety rules, how does it commit that entry?',
        options: [
          'It replicates a new entry from its own term; committing that entry indirectly commits the older one',
          'It marks the entry committed immediately since it already exists on a majority',
          'It rewrites the entry with its current term number and re-replicates it',
          'It truncates the entry, because entries from prior terms are always discarded on election',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Your payment API supports idempotency keys, but two identical requests with the same key arrive concurrently and both miss the stored-response lookup, charging the card twice. What is the robust fix?',
        options: [
          'Compare request body hashes and reject the second request if the hashes match',
          'Insert the key into a table with a unique constraint at request start, so the loser of the race fails or waits and then replays the stored response',
          'Serialize all payment requests through a single worker so duplicates can never overlap',
          'Cache the key in local process memory before starting the charge',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Kafka consumer reads events and writes results to PostgreSQL. The team demands "exactly-once processing". Which design actually achieves the closest guarantee?',
        options: [
          'Enable enable.idempotence=true on the producer that feeds the topic',
          'Use Kafka transactions and have the consumer read with isolation.level=read_committed',
          'Commit Kafka offsets synchronously immediately after each database write',
          'Store the consumed offset in the same PostgreSQL transaction as the business write, making redelivery a no-op (effectively-once)',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A worker pulls a job from a queue, finishes the side effect (sends an email), then crashes before acknowledging. The broker uses ack-after-processing. What outcome must the system design assume?',
        options: [
          'The job is redelivered and the email may be sent twice — this is at-least-once delivery, so the effect must be made idempotent or deduplicated',
          'The broker detects the completed side effect and discards the job',
          'The job is lost because the crash happened after processing began',
          'The broker retries only if the consumer reconnects within the visibility timeout, otherwise the job is dropped',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Table events has a composite B-tree index on (created_at, user_id). The query is:\nSELECT * FROM events WHERE user_id = 42 ORDER BY created_at DESC LIMIT 20;\nIt is slow at scale. Why?',
        options: [
          'LIMIT prevents the planner from using any composite index',
          'B-tree indexes cannot serve descending sort orders',
          'user_id is not a leftmost prefix of the index, so no seek is possible; an index on (user_id, created_at) would serve both the filter and the sort',
          'The index is unusable because SELECT * forces a full heap scan regardless of predicates',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In PostgreSQL, a reporting transaction has been open for 6 hours. Meanwhile, a high-churn table doubles in size on disk even though row count is flat, and autovacuum runs constantly. What is happening?',
        options: [
          'Autovacuum is disabled by the open transaction and will resume when it commits',
          'The old snapshot holds back the xmin horizon, so VACUUM cannot reclaim dead tuples created after it started, and bloat accumulates',
          'The reporting transaction takes ACCESS EXCLUSIVE locks that block heap pruning',
          'WAL segments are being retained inside the table files until checkpoint',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Two concurrent transactions each verify SELECT count(*) FROM oncall WHERE shift=7 returns >= 2, then each deletes a different doctor from that shift. Both commit under snapshot isolation, leaving zero doctors on call. Which anomaly is this, and which isolation level prevents it?',
        options: [
          'Lost update — prevented by REPEATABLE READ',
          'Phantom read — prevented by adding SELECT ... FOR SHARE',
          'Dirty write — prevented by READ COMMITTED',
          'Write skew — permitted by snapshot isolation because the writes touch disjoint rows; SERIALIZABLE prevents it',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Two batch jobs each run one transaction updating thousands of account rows and regularly deadlock against each other. Which change eliminates (not just retries) the deadlocks?',
        options: [
          'Lower the lock timeout so victims are chosen faster',
          'Have every transaction update rows in one consistent global order, e.g. sorted by primary key',
          'Switch both transactions to READ UNCOMMITTED so they skip row locks',
          'Wrap each UPDATE in a savepoint so the database can roll back single statements',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'At READ COMMITTED, this code intermittently loses concurrent deductions:\nbal = SELECT balance FROM acct WHERE id=1;\nUPDATE acct SET balance = :bal - 10 WHERE id=1;\nWhat is the correct minimal fix?',
        options: [
          'Make the write relative (SET balance = balance - 10) or lock the row with SELECT ... FOR UPDATE before computing',
          'Raise the isolation level to REPEATABLE READ, which blocks concurrent readers of the row',
          'Add an index on balance so the UPDATE finds the current value',
          'Run the SELECT and UPDATE on the same connection so they share a snapshot',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Node.js service becomes unresponsive to all I/O whenever a specific job runs. The job schedules its next step with process.nextTick recursively. Why does this starve the server, and what fixes it?',
        options: [
          'nextTick callbacks run on a worker thread that saturates the CPU; use cluster mode',
          'Each nextTick allocates a new microtask queue, exhausting memory before I/O runs',
          'nextTick defers to the timers phase, so timers fire but sockets do not; use setTimeout(0)',
          'nextTick callbacks drain before the event loop proceeds, so recursive scheduling never yields to the poll phase; setImmediate lets I/O run between iterations',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In an Express 4 app, an async route handler throws after an awaited call:\napp.get("/u/:id", async (req,res)=>{ const u = await load(req.params.id); res.json(u); });\nWhen load() rejects, clients see requests hang until timeout. Why?',
        options: [
          'res.json was already partially written, so Express cannot change the status code',
          'The rejection is swallowed by the event loop because async functions run outside the request context',
          'Express 4 does not catch rejected promises from async handlers, so no error reaches the error middleware; wrap handlers or call next(err) in a catch',
          'Node terminates the process on unhandled rejection before the response is sent',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Go service leaks goroutines. Profiling shows thousands blocked at "chan send". The pattern: a worker sends its result on an unbuffered channel, but the requester returned early on context cancellation. What is the idiomatic fix?',
        options: [
          'Give the result channel a buffer of 1, or have the worker select between the send and ctx.Done()',
          'Call runtime.Goexit() in the requester before returning',
          'Close the channel from the requester so the pending send unblocks',
          'Wrap the send in a recover() so the goroutine exits when the channel is garbage collected',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Java service configures new ThreadPoolExecutor(4, 32, ..., new LinkedBlockingQueue<>()) but never runs more than 4 concurrent tasks even under heavy load. Why?',
        options: [
          'LinkedBlockingQueue rejects tasks once 32 are queued, throttling the pool to core size',
          'The pool only grows beyond corePoolSize when the queue is full; an unbounded queue is never full, so maximumPoolSize is never used',
          'maximumPoolSize applies only when allowCoreThreadTimeOut is enabled',
          'Threads above corePoolSize require a ScheduledThreadPoolExecutor',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Kafka consumer group constantly rebalances during traffic spikes, and messages get reprocessed. Logs show the consumer was "removed because it exceeded a poll deadline". Which setting/behavior explains it?',
        options: [
          'session.timeout.ms expired because the heartbeat thread was blocked by GC',
          'The group coordinator moved to another broker mid-poll, invalidating the generation',
          'auto.offset.reset=earliest forces rewinds whenever the group is rebalancing',
          'Processing a polled batch takes longer than max.poll.interval.ms, so the broker evicts the member; reduce max.poll.records or process asynchronously with pause/resume',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Kafka producer with retries=5, max.in.flight.requests.per.connection=5, enable.idempotence=false publishes updates keyed by account id. Consumers occasionally see an older update applied after a newer one for the same key. Why?',
        options: [
          'The partitioner rehashes keys on retry, sending them to different partitions',
          'Consumers in a group can read the same partition concurrently, racing on order',
          'A failed batch is retried while a later batch already succeeded, reordering records within the partition; idempotence (or in-flight=1) prevents this',
          'Broker-side log compaction rewrote the segment order for that key',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A RabbitMQ consumer uses autoAck=true for speed. During a deploy, the process is killed mid-task and those messages never appear again. What happened and what is the fix?',
        options: [
          'With autoAck the broker marks messages delivered the moment they are sent, so a crash loses in-flight work; use manual acks after processing (with prefetch to bound in-flight count)',
          'The messages went to the default dead-letter exchange; re-bind it to the work queue',
          'The queue was non-durable, so the broker dropped its contents when the consumer died',
          'The kill signal triggered a basic.recover that requeued messages to a different vhost',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'For a Kafka consumer that commits offsets manually, which statement about commit timing is correct?',
        options: [
          'Committing after processing yields at-most-once because redelivery is suppressed',
          'Committing before processing gives at-most-once (a crash loses the in-flight batch); committing after gives at-least-once (a crash reprocesses it)',
          'Commit timing is irrelevant because the broker tracks per-message acks',
          'Committing before processing gives exactly-once as long as commits are synchronous',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A popular cache key expires and 5,000 concurrent requests all miss and hit the database simultaneously, briefly taking it down. Which mitigation directly addresses this failure mode?',
        options: [
          'Switch from LRU to LFU eviction so popular keys are evicted less often',
          'Shard the cache cluster so the key is served by more nodes',
          'Coalesce concurrent misses behind a per-key lock (single flight) or refresh the value probabilistically before expiry',
          'Reduce the TTL so the recomputation happens more frequently but with fresher data',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Your service writes to the DB then deletes the cache key (cache-aside). Users still occasionally see stale data for hours. Which interleaving explains it?',
        options: [
          'The DB write and the delete run in one transaction, so the delete is invisible until commit',
          'The delete is asynchronous, so it always lands before the DB write',
          'Cache-aside cannot be stale for longer than one TTL, so the report must be wrong',
          'A reader missed the cache, read the old DB value, was delayed, and wrote the stale value back after the delete completed; mitigate with TTLs, delayed double delete, or CDC-driven invalidation',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'To "keep the cache fresh", a service updates the cache entry inside the request, before committing the database transaction. The transaction then rolls back on a constraint violation. What is the consequence?',
        options: [
          'The cache now serves data that never existed in the database; only touch the cache after commit, and prefer invalidation over update',
          'Nothing — the rollback also reverts the cache because they share the request scope',
          'The cache entry is evicted automatically when the DB connection closes',
          'Subsequent readers get a cache miss because the failed transaction poisons the key',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Your public REST API promises no breaking changes within a major version. Which of these changes can ship without a version bump?',
        options: [
          'Changing the type of an existing response field from string to integer',
          'Adding a new optional field to the response body',
          'Making a previously optional request parameter required',
          'Renaming a response field while keeping its value format identical',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'An API allows 100 requests/minute using fixed one-minute windows. A client sends 100 requests at 00:59 and 100 more at 01:01, all accepted. What is the flaw and a correct remedy?',
        options: [
          'The limiter is counting per endpoint instead of per client; key the counter by API key',
          'Clock drift between limiter nodes doubled the window; sync with NTP',
          'Fixed windows admit up to 2x the limit across a boundary; a sliding-window or token-bucket algorithm smooths the burst',
          'The counter resets on every deploy; persist it to disk',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A rate limit of 1000 req/min per API key is enforced with in-memory counters in each of 8 stateless replicas behind a round-robin load balancer. What actually gets enforced?',
        options: [
          'Exactly 1000 req/min, because round-robin distributes each key evenly',
          'At most 125 req/min per key, because the limit is divided by replica count',
          'The limiter fails open entirely since counters reset on every request',
          'Up to ~8000 req/min per key, since each replica counts independently; use a shared store (e.g. Redis) or route each key to one replica',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Security requires that a compromised user session be killable within seconds, but your API uses stateless JWTs with 24h expiry. Which design meets the requirement while keeping most requests stateless?',
        options: [
          'Issue short-lived access tokens (minutes) via refresh tokens, and consult a server-side denylist of revoked jti/session ids so revocation propagates quickly',
          'Rotate the JWT signing key whenever a user is compromised',
          'Embed the user password hash in the token and re-verify it per request',
          'Extend the token with a "revoked" claim the client sets when logging out',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A load balancer terminates mTLS and forwards the verified client identity to backends in an X-Client-Cert-CN header. Penetration testers authenticate as any service. What was missed?',
        options: [
          'The header should be base64-encoded to prevent injection of control characters',
          'Backends must verify the LB certificate chain before reading headers',
          'mTLS identities cannot be forwarded over HTTP at all; the LB must use TCP passthrough',
          'The edge must strip or overwrite that header on incoming external requests; otherwise any caller can spoof an internal identity by setting it',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In OAuth 2.0 authorization code flow, what specific attack does PKCE neutralize?',
        options: [
          'Replay of the access token against a different resource server',
          'Phishing of the user credentials on the authorization page',
          'Use of an intercepted authorization code by another app: the token exchange requires the code_verifier that only the initiating client holds',
          'Brute-forcing the client_secret of a confidential client',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'An order saga runs: reserve inventory, then charge payment, then create shipment. Midway, other requests read an order that shows inventory reserved but no payment. Which fundamental saga property does this expose?',
        options: [
          'Sagas lack atomicity, so compensations can never fully undo a step',
          'Sagas lack isolation — intermediate states are visible to concurrent transactions; mitigate with semantic locks, status fields, or commutative updates',
          'Sagas require distributed 2PC for reads, which was omitted here',
          'Choreographed sagas expose intermediate state but orchestrated ones do not',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A circuit breaker around a flaky downstream sits in the half-open state. What is the defining behavior of this state?',
        options: [
          'All requests fail fast while a background thread pings the dependency health endpoint',
          'Requests are queued until the dependency recovers, then flushed in order',
          'Half of the traffic is allowed through and half is rejected, weighted by recent error rate',
          'A limited number of trial requests pass through; success closes the breaker, failure re-opens it and restarts the cooldown timer',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A service saves an order to its DB, then publishes OrderCreated to Kafka. Occasionally the DB commit succeeds but the publish fails, and downstream systems never learn about the order. Which pattern fixes this dual-write problem?',
        options: [
          'Transactional outbox: insert the event into an outbox table in the same DB transaction, and have a relay/CDC process publish it, with idempotent consumers absorbing duplicates',
          'Publish to Kafka first, then write the DB inside the producer callback',
          'Wrap the DB write and the Kafka send in a single XA transaction spanning both systems',
          'Retry the publish forever in an in-memory queue until the broker acknowledges',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
    ],
    2: [
      {
        question: 'Client A writes x=2 and gets an acknowledgement. One second later, client B reads x from the same system and receives the old value 1. Which consistency claim is definitively violated by this history?',
        options: [
          'Eventual consistency, since the read did not reflect the newest write',
          'Linearizability — a read that begins after a write completes must observe it; weaker models like sequential consistency can still permit this',
          'Causal consistency, because B\'s read causally follows A\'s write',
          'Read-your-writes, because B read a stale value',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Why does Raft randomize each node\'s election timeout instead of using a fixed value cluster-wide?',
        options: [
          'To spread heartbeat traffic evenly across the network and avoid bursts',
          'To ensure followers with longer logs always time out first and win',
          'To prevent clock synchronization from becoming a safety requirement',
          'So that after a leader failure one candidate usually times out first and wins before others start competing, avoiding repeated split votes',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A service holds a distributed lock with a 10s lease, suffers a 15s GC pause, then resumes and writes to shared storage — corrupting data written by the new lock holder. What mechanism prevents this even when pauses exceed the lease?',
        options: [
          'Fencing tokens: the lock service issues a monotonically increasing number, and the storage rejects writes carrying a token lower than the highest it has already seen',
          'Heartbeating the lock service every second so the lease auto-renews during the pause',
          'Making the lease long enough (e.g. 10 minutes) that GC pauses cannot exceed it',
          'Having the resumed client re-check lock ownership immediately before each write',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Two clients concurrently update the same key on different replicas of an eventually consistent store. With last-write-wins (LWW) resolution keyed on wall-clock timestamps, what is the fundamental risk that vector clocks avoid?',
        options: [
          'LWW causes unbounded storage growth because both versions are retained',
          'Vector clocks pick the correct winner automatically without application input',
          'One concurrent write is silently discarded — and clock skew can even keep the older write; vector clocks detect the concurrency and surface both versions for merging',
          'LWW requires a global lock service to order the timestamps',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Dynamo-style store is configured with R + W > N. An engineer claims this alone guarantees every read sees the latest acknowledged write. What is the strongest correct objection?',
        options: [
          'R + W > N only guarantees freshness when N is odd',
          'Quorum overlap only matters for writes; reads always contact all replicas anyway',
          'The claim holds only if the coordinator is the same node for reads and writes',
          'With sloppy quorums and hinted handoff, writes can land on substitute nodes outside the home set, so the read and write quorums may not actually intersect',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'PostgreSQL has an index on users(email), but this query does a sequential scan:\nSELECT * FROM users WHERE lower(email) = lower($1);\nWhat is the correct fix?',
        options: [
          'Rewrite the query with ILIKE, which automatically uses the plain index',
          'Create an expression index: CREATE INDEX ON users (lower(email)); the planner only matches indexes to the exact expression used in the predicate',
          'Add a hash index on email, since equality predicates cannot use B-trees',
          'Run ANALYZE so the planner learns the column is mostly unique',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Insert throughput on a large InnoDB table with a random UUIDv4 primary key degrades sharply as the table grows, with heavy disk I/O. What is the mechanical cause?',
        options: [
          'Random keys hit pages all over the clustered B+tree, causing page splits, ~50% fill, and constant buffer-pool misses; time-ordered keys (auto-increment, UUIDv7) append to the rightmost pages instead',
          'UUID comparisons are lexicographic, forcing a full index rebuild per insert',
          'The optimizer chooses a table scan to locate the insert position for non-sequential keys',
          'InnoDB stores UUIDs off-page, so every insert costs an extra overflow write',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A transaction runs SELECT count(*) FROM jobs WHERE state=\'queued\' twice. Between the two reads, another committed transaction INSERTed a matching row, and the second count is higher. Which anomaly is this, precisely?',
        options: [
          'Non-repeatable read, because a previously read row changed value',
          'Dirty read, because uncommitted data became visible',
          'Phantom read — the set of rows matching a predicate changed between reads; it involves new rows, not modification of previously read ones',
          'Lost update, because the insert overwrote the count',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Under MySQL InnoDB REPEATABLE READ, two transactions each run SELECT ... WHERE k=10 FOR UPDATE on an empty range, then both INSERT a row with k=10. They deadlock. Why?',
        options: [
          'REPEATABLE READ forbids concurrent inserts entirely, so both block on the table lock',
          'The unique index check escalates to a full table lock on the second insert',
          'Both acquired a shared gap lock on the empty range; each INSERT then needs an insert intention lock that conflicts with the other\'s gap lock — a classic gap-lock deadlock',
          'FOR UPDATE on a missing row is a no-op, so the deadlock must come from the secondary index',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In PostgreSQL, EXPLAIN shows "Index Only Scan" for your covering index, but "Heap Fetches" is huge and the query is still slow right after a bulk update. Why, and what helps?',
        options: [
          'The index does not actually include all selected columns; add INCLUDE columns',
          'Index-only scans require the table to be clustered on the index; run CLUSTER',
          'Heap fetches are a display artifact and never affect latency',
          'Tuples changed since the last vacuum are not marked all-visible, so each index hit re-checks the heap; running VACUUM updates the visibility map and eliminates the fetches',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'One Node.js endpoint parses ~200MB JSON uploads with JSON.parse, and during those requests every other request stalls for seconds. Which change fixes the stalls without adding servers?',
        options: [
          'Increase UV_THREADPOOL_SIZE so libuv parses JSON on more threads',
          'Offload the parse to a worker_threads pool (or an incremental streaming parser) so the main event loop stays free; JSON.parse is synchronous on the main thread',
          'Wrap JSON.parse in a promise so it becomes asynchronous',
          'Use setInterval to poll the parser, letting other callbacks interleave',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Node.js proxy reads from a fast source stream and writes to a slow destination with dest.write(chunk) in a data handler, ignoring the return value. Memory climbs until OOM. What is the correct pattern?',
        options: [
          'Respect backpressure: when write() returns false, pause the source and resume on the drain event — or just use stream.pipeline, which wires this up and propagates errors',
          'Call dest.cork() before writing and uncork() on an interval to batch flushes',
          'Increase the destination highWaterMark so buffering happens in the kernel instead',
          'Switch the data handler to async and await each write call',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Go HTTP server crashes with "fatal error: concurrent map read and map write" even though every handler wraps map access in recover(). Why does the process still die?',
        options: [
          'recover() only works in the goroutine that owns the map, not in handlers',
          'The runtime retries the access and hits a second fault before recover runs',
          'The map must be recovered with debug.SetPanicOnFault disabled first',
          'Concurrent map misuse raises a runtime fatal error (a throw), not a panic — it is not recoverable; protect the map with a mutex or use sync.Map',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In Go, a fan-in pipeline panics with "send on closed channel" under load. Multiple producer goroutines send to one channel and the consumer closes it when it has read "enough". What is the correct ownership rule?',
        options: [
          'Wrap each send in select with a default case so sends to closed channels are skipped',
          'Replace close() with a nil assignment, which safely disables the channel',
          'Only the sender side should close a channel — never the receiver; with multiple producers, close after they finish via sync.WaitGroup, or signal shutdown with a separate done channel',
          'Buffer the channel so pending sends complete before the close takes effect',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Java service declares "volatile long requestCount" and increments it with requestCount++ from many threads. Counts come up short under load. Why?',
        options: [
          'volatile guarantees visibility, but ++ is a read-modify-write of three separate operations, so increments interleave and get lost; use AtomicLong or LongAdder',
          'volatile longs tear on 64-bit writes, corrupting the value',
          'The JIT eliminates volatile increments in hot loops as dead code',
          'volatile writes are buffered per-thread and flushed only at synchronization points',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A team enables Kafka transactions (transactional.id on the producer, read_committed consumers) and declares their pipeline "exactly-once, including the emails it sends". What is wrong with that claim?',
        options: [
          'read_committed consumers still see aborted records, so duplicates remain within Kafka',
          'Kafka EOS covers consume-transform-produce between Kafka topics; side effects on external systems (like sending email) are outside the transaction and can still happen more than once',
          'Transactions require exactly one partition per topic to hold, which is impractical',
          'transactional.id only deduplicates within a single producer session, so restarts break the guarantee entirely',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A service rebuilds its state by replaying a compacted Kafka topic. Which expectation about a compacted topic is CORRECT?',
        options: [
          'It preserves every historical value per key, just reordered by key',
          'Compaction preserves relative ordering across different keys exactly as produced',
          'It eventually retains at least the latest value for each key; deletes appear as tombstones that are themselves removed after delete.retention.ms, so slow re-readers can miss deletions',
          'Reading a compacted topic from the beginning yields each key exactly once',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A RabbitMQ setup loses messages when a broker node restarts, despite "we enabled durable queues". Which combination is actually required for the broker restart not to lose accepted messages?',
        options: [
          'Durable queue plus a consumer prefetch of 1',
          'Persistent delivery mode plus autoAck disabled on consumers',
          'Durable exchange plus lazy queue mode',
          'Durable queue AND persistent delivery mode (deliveryMode=2) AND publisher confirms — a missing piece in any of the three leaves a loss window',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Kafka topic has 12 partitions and 12 consumers, yet lag grows on exactly one partition while the others are idle. Adding more consumers does nothing. What is the likely cause?',
        options: [
          'The consumer group protocol caps active members at the broker count',
          'A hot key: the partitioner hashes one very frequent key to that partition, and per-partition ordering means only one consumer can drain it — fix the keying/partitioning, not the consumer count',
          'The partition leader is on a follower broker, halving its throughput',
          'That partition has compaction enabled, which throttles consumer fetches',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'One malformed message makes a queue consumer throw; the broker requeues it, the consumer crashes again, and the queue is now blocked in an infinite redelivery loop. What is the standard remedy?',
        options: [
          'Track delivery attempts and, after a bounded number of retries, route the message to a dead-letter queue for offline inspection instead of requeueing forever',
          'Catch all exceptions and ack every message regardless of processing outcome',
          'Set the queue to LIFO so newer messages bypass the poisoned one',
          'Increase the consumer count so other instances can work around the bad message',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Attackers request millions of random nonexistent product ids. Every request misses the cache and hits the database (cache penetration). Which mitigation targets this precisely?',
        options: [
          'Lengthen the TTL of existing product entries so real products stay cached longer',
          'Move the cache in-process to remove the network hop',
          'Cache the negative result ("not found") with a short TTL — or screen ids with a Bloom filter — so repeated misses stop reaching the database',
          'Enable write-through so products are cached at creation time',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Redis instance used purely as a cache starts returning "OOM command not allowed when used memory > maxmemory" and the app throws on every write. What is the appropriate configuration?',
        options: [
          'Enable AOF persistence so overflow spills to disk',
          'Set maxmemory to 0 so Redis uses all available system memory without limits',
          'Switch clients to pipelining to reduce per-command memory overhead',
          'The policy is noeviction; for a pure cache set an eviction policy like allkeys-lru (or allkeys-lfu) so writes evict old entries instead of failing',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Every night at 02:00 a cache-warming job loads 1M keys with TTL=3600, and every night at 03:00 the database gets crushed. What is the cheap fix?',
        options: [
          'Add jitter to each TTL (e.g. 3600 + rand(600)) so the keys expire spread out instead of simultaneously (a cache avalanche)',
          'Move the warming job to 03:00 so expiry aligns with low traffic',
          'Double the cache cluster size so expired keys are refilled faster',
          'Use LFU eviction so frequently used keys are exempt from TTL expiry',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'An API returns ETag headers and honors If-None-Match with 304. The team is surprised the origin CPU didn\'t drop. Which statement correctly describes what conditional requests do and do not save?',
        options: [
          'A 304 skips authentication and authorization, saving most CPU',
          'A 304 avoids resending the response body, but the origin still receives the request and typically recomputes or re-validates the resource; to avoid the round-trip entirely you need freshness caching (Cache-Control max-age) or a caching layer',
          '304 responses are cached by the browser forever, so the origin should see zero repeat traffic',
          'ETags only work through a CDN, so direct clients cannot benefit',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A feed endpoint uses OFFSET/LIMIT pagination. Users report both duplicated and skipped items while paging during heavy insert traffic, and page 5000 takes seconds. Which redesign addresses both problems?',
        options: [
          'Cache each page for 60 seconds so consecutive requests see a frozen snapshot',
          'Add an index on the OFFSET column',
          'Keyset (cursor) pagination — WHERE (created_at, id) < ($cursor) ORDER BY created_at DESC, id DESC LIMIT n — which is stable under inserts and O(log n) regardless of depth',
          'Wrap each page query in a REPEATABLE READ transaction to stabilize results across requests',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A mobile client auto-retries any write request that times out. Which API operation is UNSAFE to blindly retry without additional safeguards?',
        options: [
          'PUT /users/42/address with the full address body',
          'DELETE /sessions/abc123',
          'GET /orders?status=open',
          'POST /orders that creates a new order per call — POST is not idempotent, so a timeout retry can create duplicates unless an idempotency key is used',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A JWT library accepts the alg header from the token itself. Your API verifies RS256 tokens with the RSA public key. An attacker crafts a token with alg=HS256. What is the attack, and the defense?',
        options: [
          'None — HS256 tokens fail because the attacker lacks any key material',
          'Algorithm-confusion: the library verifies HS256 using the RSA public key (a known string) as the HMAC secret, so forged tokens validate; servers must pin the expected algorithm and key type, ignoring the token header',
          'The attacker downgrades to alg=none, which HS256 implies as a fallback',
          'HS256 tokens skip expiry validation, extending stolen token lifetime',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A mesh of 40 microservices uses mTLS everywhere. Last month an internal cert silently expired and took down checkout for an hour. Which operational practice most directly prevents a recurrence?',
        options: [
          'Automate certificate issuance and rotation (short-lived certs from an internal CA, e.g. SPIFFE-style) and alert on remaining validity, rotating well before notAfter',
          'Extend all internal certificate lifetimes to 10 years to make expiry rare',
          'Fall back to plaintext automatically whenever a certificate is invalid',
          'Pin leaf certificates in every client so rotation is impossible without a deploy',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'During a partial outage, a downstream service at 10% error rate receives 5x its normal traffic and collapses completely. Each of 3 call layers retries 3 times. What happened and what is the fix?',
        options: [
          'The load balancer amplified traffic by health-checking too aggressively; lengthen the check interval',
          'Connection pooling multiplied sockets per request; cap pool size',
          'Retry amplification: retries compound multiplicatively across layers (up to 27x); retry at one layer only, with exponential backoff, jitter, and a retry budget, and let circuit breakers shed load',
          'The 10% errors poisoned the cache, forcing all requests to origin',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'An event-choreographed order flow has grown to 14 services reacting to each other\'s events. Nobody can state the end-to-end flow, and a recent change created an unnoticed event loop. Which architectural response best targets these specific pains?',
        options: [
          'Merge the 14 services into one deployable to eliminate events entirely',
          'Introduce saga orchestration for the critical flows: an orchestrator makes the sequence, branching, and compensations explicit and observable, at the cost of a central coordinator',
          'Switch the event bus from Kafka to RabbitMQ for better routing visibility',
          'Add a schema registry so event payloads are versioned and validated',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
    ],
    3: [
      {
        question: 'In classic two-phase commit, participants have all voted YES and the coordinator crashes before sending the decision. What is the participants\' predicament?',
        options: [
          'They abort after a timeout, since no decision implies abort by default',
          'They commit after a timeout, since a YES vote makes commit inevitable',
          'They poll each other and adopt the majority state, like a quorum system',
          'They are blocked: unable to safely commit or abort unilaterally, they hold locks until the coordinator recovers — the blocking problem that motivates consensus-based commit',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A distributed system orders cross-node events by attaching each node\'s wall-clock timestamp (NTP-synced). Occasionally an effect is ordered before its cause. Why is this design fundamentally unsound?',
        options: [
          'NTP only synchronizes to the minute, so ordering needs date-level granularity',
          'Timestamps collide too often; adding a random suffix fixes ordering',
          'NTP skew, step corrections, and clock smearing mean wall clocks across nodes can disagree by more than message latency; causal ordering needs logical/hybrid clocks or a single sequencer',
          'Wall clocks are fine for ordering, but only if every node runs in the same timezone',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A client sends a payment with idempotency key K, gets a network timeout, but the server actually committed the charge. What must the client do, and why does it work?',
        options: [
          'Send a compensating refund first, then retry with a fresh key to avoid ambiguity',
          'Retry with the SAME key K: the server recognizes K, skips re-execution, and replays the stored response — retrying with a new key would create a second charge',
          'Query a reconciliation endpoint before every retry to check whether the charge exists',
          'Treat the timeout as failure; idempotency keys make server-side commits impossible on timeout',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In a CRDT-based shopping cart, a user removes an item on a disconnected device while another device concurrently re-adds it. With an OR-Set (observed-remove), what is the merge outcome and why?',
        options: [
          'The item remains: the concurrent add has a unique tag the remove never observed, so the remove only cancels previously seen tags — add wins for concurrent operations',
          'The item is gone: removes always dominate adds to guarantee convergence',
          'The merge conflicts and requires manual user resolution, since CRDTs only handle ordered operations',
          'The outcome depends on which replica syncs first, so it is nondeterministic',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In a leaderless quorum store, a read at R=2 returns version 7 from one replica and version 5 from another. What does the coordinator do under read repair, and what does this mechanism actually provide?',
        options: [
          'It returns version 5 as the safe common ancestor and schedules a merge',
          'It rejects the read entirely, since divergent replicas indicate a failed quorum',
          'It returns version 7 to the client and writes version 7 back to the stale replica — an anti-entropy mechanism that converges frequently read data, not a transaction protocol',
          'It blocks until all N replicas agree on one version, providing linearizability',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In PostgreSQL, what does the deadlock_timeout setting (default 1s) actually control?',
        options: [
          'How long a session waits on a lock before the deadlock detector runs its cycle check; it neither cancels ordinary long lock waits nor bounds transaction duration',
          'The maximum time a transaction may hold any lock before being killed',
          'How long a victim transaction has to retry before being permanently aborted',
          'The interval between full lock-table scans that release orphaned locks',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Postgres-backed job queue uses:\nSELECT * FROM jobs WHERE state=\'ready\' ORDER BY id LIMIT 1 FOR UPDATE SKIP LOCKED;\nWhat does SKIP LOCKED contribute over plain FOR UPDATE here?',
        options: [
          'It upgrades the row lock to an exclusive table lock so no other worker interferes',
          'It prevents the chosen row from being vacuumed while the job runs',
          'It retries the select automatically until an unlocked row appears',
          'Workers skip rows other workers already locked instead of queueing behind them, so N workers grab N different jobs concurrently without serializing on the head row',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A PostgreSQL table with 8 indexes has a status column updated millions of times daily; all indexes churn even though only status changes. The DBA says "you defeated HOT updates". What does that mean?',
        options: [
          'HOT requires all updates to run in a single transaction per page',
          'Heap-only-tuple updates avoid touching indexes only when no indexed column changes and the new tuple fits on the same page; status is indexed, so every update writes to all 8 indexes — drop the status index or lower fillfactor to restore HOT',
          'HOT updates are disabled whenever a table has more than 4 indexes',
          'The status column is TOASTed, and TOAST columns cannot be updated in place',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'After switching to SERIALIZABLE in PostgreSQL, production logs fill with "could not serialize access due to read/write dependencies" (40001), and some user actions silently fail. What is the correct application posture?',
        options: [
          'Serialization failures are expected under SSI — the app must catch 40001 and retry the whole transaction (with backoff); treating them as errors to surface to users is a design bug',
          'Add FOR UPDATE to all reads, which disables SSI conflict tracking',
          '40001 indicates data corruption from concurrent DDL; schedule migrations off-peak',
          'Downgrade those transactions to READ UNCOMMITTED where anomalies are impossible',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'For the query:\nSELECT * FROM orders WHERE tenant_id = $1 AND created_at > $2 ORDER BY created_at;\nwhich composite index allows a single ranged index scan with no separate sort?',
        options: [
          '(created_at, tenant_id) — range column first so the scan starts at $2',
          '(tenant_id, created_at) — equality column first, then the range/sort column; reversed order forces scanning all tenants in the date range and filtering',
          'Two single-column indexes on tenant_id and created_at, bitmap-ANDed',
          '(tenant_id, created_at DESC) with the query rewritten to ORDER BY created_at DESC',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Node.js service shows p99 latency spikes but CPU, DB, and downstream metrics look healthy. Which measurement most directly confirms or rules out event-loop blockage as the cause?',
        options: [
          'Heap usage from process.memoryUsage(), since blockage implies GC pressure',
          'The libuv thread pool queue depth, since all JS callbacks queue there',
          'Event-loop delay (e.g. perf_hooks.monitorEventLoopDelay): sustained high loop lag means synchronous work is starving callbacks, which container CPU graphs can completely hide',
          'Socket count from the OS, since blocked loops leak file descriptors',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Node.js app scales from 1 process to 8 via cluster/PM2, and suddenly ~87% of WebSocket reconnects land on a process without the client\'s session state. What is the underlying issue?',
        options: [
          'The cluster module round-robins TCP connections, corrupting WebSocket frames',
          'PM2 forks share the event loop, so session memory is copy-on-write and diverges',
          'WebSockets cannot be used with more than one process per port',
          'In-memory session/socket state is per-process; with multiple workers you need sticky routing (or better, externalize state to Redis and pub/sub the events)',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'This Go code sometimes prints before any worker runs:\nfor _, t := range tasks {\n  go func() { wg.Add(1); defer wg.Done(); run(t) }()\n}\nwg.Wait(); fmt.Println("done")\nWhat is the bug?',
        options: [
          'defer wg.Done() runs at goroutine start, immediately cancelling the Add',
          'wg.Add is called inside the goroutines, so Wait() can observe counter 0 and return before any goroutine is scheduled; Add must happen in the spawning goroutine before go',
          'The loop variable t is captured by reference, which corrupts the WaitGroup counter',
          'WaitGroup requires Add(len(tasks)) exactly once; incremental Add(1) calls race with each other',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Java web app stores per-request user context in a ThreadLocal. In production (Tomcat thread pool), users intermittently see data belonging to other users. Why?',
        options: [
          'Pooled threads are reused across requests, and ThreadLocal values set during one request survive into the next unless explicitly removed; clear them in a finally block or filter',
          'ThreadLocal is not thread-safe, so concurrent set() calls corrupt its map',
          'Tomcat shares one thread across concurrent requests when the pool is exhausted',
          'The JVM interns ThreadLocal values, so equal keys share one slot globally',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A service with a 10-connection DB pool freezes completely under load. Thread dumps show every request holding one connection while blocked waiting to acquire a second one for a nested query. What is this failure and the cleanest fix?',
        options: [
          'Connection leak — connections are never returned; add leak detection and close in finally',
          'Database-side lock contention — the second acquire waits on row locks, not the pool',
          'Pool self-deadlock: all N connections are held by requests that each need one more, so nobody progresses; restructure to use a single connection per request (or acquire all needed connections up front / size the pool above peak concurrent need)',
          'Thread starvation in the web server — increase worker threads to match the pool',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'To add throughput, an operator increases a Kafka topic from 8 to 16 partitions. Afterwards, consumers relying on per-key ordering see interleaved old and new events for the same key. Why?',
        options: [
          'The rebalance replays each partition from offset 0, duplicating history',
          'New partitions start with negative offsets until the first segment rolls',
          'Adding partitions changes hash(key) % partitionCount, so a key\'s new events go to a different partition while its old events remain in the previous one — per-key ordering is broken across the transition; choose partition counts up front or migrate via a new topic',
          'Consumers must be restarted to see new partitions, so they read stale data first',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Kafka topic has replication.factor=3, producers use acks=all, yet a single broker failure loses acknowledged messages. Investigation shows min.insync.replicas=1. Explain.',
        options: [
          'With min.insync.replicas=1, acks=all degrades to leader-only acknowledgement once followers fall out of the ISR; the leader can ack alone and then die before followers catch up — set min.insync.replicas=2 to make the guarantee real',
          'acks=all is silently ignored when the producer is idempotent',
          'replication.factor=3 only applies to new segments, so old data was unreplicated',
          'unclean.leader.election defaults to true, so any replica can lead without data',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A RabbitMQ cluster suddenly blocks ALL publishers across every vhost. Monitoring shows one abandoned queue with 40M messages and a triggered memory alarm. What is the causal chain?',
        options: [
          'The big queue exhausted Erlang process IDs, halting new channel creation',
          'A queue with no consumers grew unbounded until the broker hit its memory watermark; memory alarms block all publishing connections cluster-wide — enforce queue length limits/TTLs and monitor for consumerless queues',
          'Publishers share a single TCP connection, and the slow queue saturated it',
          'The queue index exceeded the message store limit, forcing a cluster-wide sync',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A consumer must apply each event\'s effect to its database exactly once despite at-least-once delivery. Which implementation is actually correct?',
        options: [
          'Keep a bounded in-memory LRU of seen message ids and skip matches',
          'Check SELECT exists(...) for the message id, and INSERT it after applying the effect in a separate transaction',
          'Rely on the broker\'s redelivered flag and skip any message marked redelivered',
          'Insert the message id into a processed_messages table with a unique constraint IN THE SAME transaction as the effect; a duplicate delivery hits the constraint and the whole transaction no-ops',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A consumer service reads from a queue into an unbounded in-memory list processed by a slow thread pool, and OOMs during traffic spikes while the queue itself stays short. What is the design flaw?',
        options: [
          'The thread pool is too small; sizing it to the spike eliminates the buffer',
          'The queue prefetch is too low, forcing the consumer to buffer locally',
          'The consumer moved the backlog from the broker (durable, bounded, observable) into process memory: it must bound its in-flight work and exert backpressure — stop fetching (pause/limit prefetch) when the local buffer is full',
          'GC cannot keep up with allocation; switching to a low-pause collector fixes it',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A CDN sits in front of an API, but the hit ratio is under 1%. The origin sends "Vary: Cookie" on every response, and every client carries an analytics cookie. What is the mechanism of the failure?',
        options: [
          'Vary: Cookie makes the cache key include the full Cookie header, so each unique cookie value gets its own cache entry — effectively zero sharing; scope Vary correctly, strip irrelevant cookies at the edge, or key on a specific normalized value',
          'CDNs refuse to cache any response carrying Set-Cookie or Vary, treating them as private',
          'Cookies force the CDN into TLS passthrough mode, which cannot cache',
          'Vary headers only work with GET; the API must be using POST',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A write-behind (write-back) cache acknowledges client writes immediately and flushes to the database every 30 seconds. What is the principal risk the team must design for?',
        options: [
          'The database rejects batched flushes because they exceed statement limits',
          'Acknowledged writes exist only in cache memory until the flush; a cache node crash loses up to 30s of confirmed writes — you need replication or a durable log in front of the flush, or accept the loss window',
          'Reads become slower because every read must wait for the flush cycle',
          'The cache and database drift apart permanently because flushes are unordered',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Redis-based lock is taken with SET key val NX EX 30. A client stalls past 30s, its lock expires, another client acquires it, then the first client resumes and runs DEL key. What did the DEL just do, and what is the correct release?',
        options: [
          'Nothing — DEL on a lock key only succeeds for the original owner',
          'It extended the new owner\'s lease by resetting the TTL',
          'It failed, because expired locks leave a tombstone that blocks DEL',
          'It deleted the OTHER client\'s lock, opening the mutual-exclusion hole wider; release must atomically check the stored owner token before deleting (a Lua script comparing value then DEL)',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Your API starts returning 429 with a Retry-After: 7 header to a partner. Their integration immediately re-sends every rejected request in a tight loop, making things worse. What client behavior should you document as required?',
        options: [
          'Reduce payload sizes, since 429 is triggered by request body volume',
          'Honor Retry-After (with added jitter), treat 429 as retryable-later rather than a failure, and shed or queue excess work client-side; hammering resets any sliding window and prolongs throttling',
          'Rotate among multiple API keys to distribute the load and bypass the limit',
          'Switch immediately to a fallback region endpoint on the first 429',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'You add a new value SHIPPED_PARTIAL to an existing status enum in API responses, and several consumers crash on deserialization. Contractually you consider added enum values non-breaking. How should this be handled going forward?',
        options: [
          'Document the tolerant-reader expectation: clients must map unknown enum values to a fallback (and you should stage such additions behind version negotiation or new fields when clients provably do exhaustive matching)',
          'Never extend enums; model every new state as a separate boolean field',
          'Encode enums as integers so unknown values deserialize without error',
          'Bump the major version for every enum addition, since all additions are breaking',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'An identity provider implements refresh token rotation: each refresh returns a new refresh token and invalidates the old one. A previously used (rotated-out) refresh token suddenly arrives. What should the server do, and why?',
        options: [
          'Accept it within a grace period, since mobile clients often retry stale tokens',
          'Reject just that request but leave the current tokens valid, logging a warning',
          'Treat reuse as evidence of theft — either the attacker or the legitimate client holds a stolen copy — and revoke the entire token family/session, forcing re-authentication',
          'Re-issue the same rotated token to resynchronize the client silently',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A new HTTPS endpoint works in every browser but server-to-server calls fail with "unable to verify the first certificate" (curl/openssl). What is the classic cause?',
        options: [
          'The server requires SNI and the clients are too old to send it',
          'The clients only support TLS 1.3 while the server negotiates TLS 1.2',
          'The certificate uses a wildcard CN, which non-browser clients reject',
          'The server sends only the leaf certificate without the intermediate CA; browsers mask this via cached or AIA-fetched intermediates, but strict clients cannot build the chain — configure the server to serve the full chain',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'One slow downstream dependency (recommendations) causes total outage of an API gateway: all worker threads end up blocked on it, so even endpoints that never call it stop responding. Which resilience pattern is specifically designed for this?',
        options: [
          'Retry with exponential backoff on the recommendations client',
          'Blue-green deployment of the gateway to drain the stuck threads',
          'Bulkheads: give each dependency its own bounded thread pool / semaphore / connection pool so saturation from one dependency cannot consume the shared capacity others need (pair with timeouts and a circuit breaker)',
          'Autoscaling the gateway on CPU, adding instances as threads block',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Your tracing backend samples 1% of traces at request start (head-based). An incident involves rare 500s (~0.05% of traffic), and no error traces were captured. Which change makes traces useful for exactly this class of incident?',
        options: [
          'Tail-based sampling: buffer spans and decide after the trace completes, keeping all error/high-latency traces while still sampling normal traffic — head-based sampling cannot know at start which traces will matter',
          'Raise head-based sampling to 10%, which statistically guarantees capturing rare errors',
          'Log full request bodies on errors instead of relying on traces',
          'Sample per-service instead of per-trace so error spans survive independently',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'After adding MySQL read replicas, users complain: they save a profile edit, the next page load shows the old profile, and a refresh fixes it. Writes go to the primary, reads to replicas. Which remedies actually address this read-your-writes violation?',
        options: [
          'Enable synchronous replication for the profiles table only',
          'Route that user\'s reads to the primary for a short window after their write (or until the replica has applied the write\'s GTID/LSN, tracked via a session token)',
          'Add a cache in front of the replicas so stale reads hit cache instead',
          'Lower replica fetch batch size so replication lag stays under one second',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
    ],
  },
};
