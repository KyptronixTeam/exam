module.exports = {
  category: 'Frontend Developer',
  sets: {
    1: [
      {
        question: 'What does this log?\nsetTimeout(() => console.log("A"));\nPromise.resolve().then(() => {\n  console.log("B");\n  queueMicrotask(() => console.log("C"));\n});\nconsole.log("D");',
        options: [
          'D B C A',
          'D B A C',
          'D A B C',
          'B C D A',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What does this log?\nasync function f() {\n  console.log(1);\n  await null;\n  console.log(2);\n}\nf();\nconsole.log(3);',
        options: [
          '1 2 3, because awaiting a non-promise resumes synchronously',
          '1 3 2, because await always yields to the caller and resumes in a microtask',
          '3 1 2, because async functions defer their entire body to a microtask',
          '1 3 2, but only if the engine wraps null in a resolved promise via a macrotask',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What is logged?\nconst fns = [];\nfor (let i = 0; i < 3; i++) fns.push(() => i++);\nfns[0]();\nfns[0]();\nconsole.log(fns[1]());',
        options: [
          '3, because all closures share one binding that was incremented twice',
          '0, because fns[1] was never called before and post-increment returns the old value of a fresh binding starting at 0',
          '1, because each loop iteration gets its own binding of i, so fns[1] returns its untouched captured value',
          '2, because let bindings in a loop are copied forward and fns[0] advanced the shared counter',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What does console.log([] + null + 1) output?',
        options: [
          'NaN, because null cannot be concatenated',
          '1, because [] and null are falsy and dropped',
          'A TypeError, because array plus null is invalid',
          'null1, because [] coerces to an empty string, making the + operators string concatenation',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A parent div has transform: translateZ(0) and contains a child with z-index: 9999. A sibling of the parent has z-index: 1 and overlaps the child. Which element paints on top and why?',
        options: [
          'The sibling, because the transform creates a stacking context on the parent, so the child z-index only competes inside it while the parent itself sits at auto level',
          'The child, because 9999 beats 1 in the root stacking context',
          'The child, but only when the sibling is not positioned',
          'The sibling, because translateZ(0) forces the parent behind all positioned elements permanently',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Two rules target the same element: ":is(.a, #b) .c { color: red }" and ".x .y .z { color: blue }". The element matches every listed class and id path. Which color wins and why?',
        options: [
          'Blue, because three chained class selectors outrank any pseudo-class function',
          'Red, because :is() takes the specificity of its most specific argument, so #b makes it (1,0,1) versus (0,3,0)',
          'Blue, because :is() contributes zero specificity like :where()',
          'Red, but only because it appears later in the stylesheet',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'When an element with content-visibility: auto is off-screen and its rendering is skipped, which containment does the browser apply to it?',
        options: [
          'Only paint containment, so layout still runs every frame',
          'Only size containment, leaving style and paint unmanaged',
          'Style, layout, and paint containment, plus size containment while the rendering is skipped',
          'Strict containment at all times, even while the element is on screen',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A flex row item containing a long unbroken string refuses to shrink below its content width even with flex-shrink: 1, overflowing the container. What is the root cause?',
        options: [
          'flex-shrink only applies when flex-basis is 0',
          'The flex algorithm ignores shrink factors for text nodes',
          'white-space: nowrap is implied on flex items',
          'The default min-width: auto on flex items floors the item at its min-content size; setting min-width: 0 (or overflow: hidden) fixes it',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Consider:\nel.style.width = "100px";\nconst w = el.offsetWidth;\nel.style.width = "200px";\nconst w2 = el.offsetWidth;\nWhat is the performance consequence?',
        options: [
          'Each offsetWidth read after a style write forces a synchronous layout flush, so layout runs twice in one task',
          'Layout runs once at the end of the task; the reads return cached values',
          'Only paint is triggered; offsetWidth never requires layout',
          'The second write cancels the first, so only one style recalculation ever happens',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Within a single frame where the browser decides to render, what is the relative ordering of a pending setTimeout(0) callback, a queued microtask, and a requestAnimationFrame callback scheduled earlier?',
        options: [
          'rAF fires first, then microtasks, then the timeout, all before style recalculation',
          'Microtasks drain after the currently running task, the rAF callback runs during the render steps just before paint, and the timeout runs in a later task',
          'The timeout always fires before rAF because the timer queue has priority over render steps',
          'All three run in a single batch whose order is unspecified by the HTML spec',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In React, state starts at n = 0. A click handler runs:\nsetN(n + 1);\nsetN(n + 1);\nsetN(prev => prev + 1);\nWhat is n after the re-render?',
        options: [
          '1, because React deduplicates identical queued updates',
          '3, because all three updates are applied to the latest state',
          '2, because the first two use the stale captured n (both produce 1) and the functional update then increments the queued result',
          '4, because batching replays the handler once per setter call',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A React parent renders a child; each has useEffect with a cleanup. On a re-render of the whole tree, in what order do cleanups and effects run?',
        options: [
          'Parent cleanup, child cleanup, parent effect, child effect',
          'Child effect, parent effect, child cleanup, parent cleanup',
          'Cleanups and effects interleave per component: child cleanup, child effect, parent cleanup, parent effect',
          'All cleanups fire first child-then-parent, then all new effects fire child-then-parent',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In Vue 3, a component does: const { count } = reactive({ count: 0 }) and renders count in the template. Incrementing the source later does not update the view. Why?',
        options: [
          'Destructuring copies the primitive out of the Proxy, severing the reactive connection; toRefs() would preserve it',
          'reactive() only tracks nested objects, never top-level primitives',
          'Template bindings require ref() objects and silently ignore reactive() state',
          'The Proxy get trap only fires inside computed(), not in templates',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In Vue 3, a watcher mutates the same reactive property three times synchronously in one handler. How many component re-renders does this cause by default?',
        options: [
          'Three, because each mutation triggers an immediate render',
          'One, because render effects are queued and deduplicated, flushing once on the next tick',
          'Zero, until nextTick() is explicitly called by the developer',
          'Two, because the first mutation renders eagerly and the rest are batched',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In a Redux Toolkit createSlice reducer using Immer, what happens if you mutate the draft state and also return a brand-new state object from the same case reducer?',
        options: [
          'Immer merges the returned object into the mutated draft',
          'The returned object silently wins and the mutations are discarded',
          'Immer throws an error, because a reducer may either mutate the draft or return a new value, never combine the two',
          'The mutations win because drafts are finalized after the return value is ignored',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Zustand selector is written as: useStore(s => ({ a: s.a, b: s.b })). The component re-renders on every store update, even when a and b are unchanged. Why?',
        options: [
          'Zustand always re-renders all subscribers regardless of selectors',
          'Object selectors are unsupported and fall back to whole-store subscription',
          'The store proxy invalidates all getters when any key changes',
          'The selector returns a new object each call, and the default Object.is equality sees it as changed; a shallow equality function fixes it',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Which statement accurately describes INP (Interaction to Next Paint) as a Core Web Vital?',
        options: [
          'It reports approximately the worst interaction latency over the page lifetime, measuring from user input until the next frame is painted, covering input delay, processing, and presentation delay',
          'It measures only the input delay of the first click, identical to FID',
          'It averages the duration of all JavaScript event handlers on the page',
          'It measures the time from navigation start until the page becomes interactive',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A user taps a button; 300ms later an inserted banner pushes content down. A separate ad iframe shifts content 900ms after the tap. How does CLS treat these shifts?',
        options: [
          'Neither counts, because CLS pauses for 5 seconds after any interaction',
          'The 300ms shift is excluded because it falls inside the 500ms post-input window, while the 900ms shift counts toward the score',
          'Each counts, because CLS ignores user input entirely',
          'Only shifts caused by iframes are counted, so just the ad shift is scored at double weight',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In webpack, what is the practical difference between /* webpackPrefetch: true */ and /* webpackPreload: true */ on a dynamic import()?',
        options: [
          'Prefetch inlines the chunk into the parent bundle; preload keeps it separate',
          'They are aliases; the only difference is HTTP priority hints',
          'Prefetch downloads the chunk during browser idle time for a likely future navigation, while preload fetches it in parallel with the parent chunk for the current navigation',
          'Preload only works for CSS chunks; prefetch only for JS chunks',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A visible button gets aria-hidden="true" but remains in the tab order. What does a screen reader user experience?',
        options: [
          'The button is skipped by Tab, because aria-hidden removes focusability',
          'The button announces its label but ignores activation',
          'Nothing unusual, because focusable elements ignore aria-hidden',
          'Tab lands on a ghost stop that announces nothing (or "empty"), because the element is focusable yet erased from the accessibility tree',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Dynamically injected status messages are not being announced by screen readers even though the container has aria-live="polite". What is the most common cause?',
        options: [
          'The live region container was inserted into the DOM at the same moment as the message; the region must already exist before its content changes',
          'aria-live="polite" requires role="alert" on the same element to function',
          'Live regions only announce changes triggered by keyboard events',
          'The messages need tabindex="-1" so the virtual cursor can reach them',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A CSP is: script-src \'nonce-abc\' \'strict-dynamic\' https://cdn.example.com. Which scripts may execute?',
        options: [
          'Only scripts from cdn.example.com that also carry the nonce',
          'Scripts with the correct nonce, plus scripts they dynamically create (e.g. via createElement); the host allowlist is ignored because of strict-dynamic',
          'Any script from cdn.example.com and any nonce-carrying script, with dynamic insertion blocked',
          'All same-origin scripts, because strict-dynamic implies self',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A cross-origin fetch() sends a POST. Which configuration avoids a CORS preflight request?',
        options: [
          'Setting Content-Type: application/json, since JSON is a simple type',
          'Adding a custom X-Request-Id header with credentials omitted',
          'Using Content-Type: text/plain with no custom headers, keeping the request within the CORS-safelisted set',
          'Using method PUT with only safelisted headers',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'An app HTML-escapes user input before inserting it as the href of an anchor: <a href="ESCAPED">. Why can this still be an XSS vector?',
        options: [
          'HTML escaping corrupts URLs so browsers re-parse them as scripts',
          'Anchors execute their href in a sandboxed context on hover',
          'The href attribute bypasses the HTML parser entirely',
          'A javascript: URL survives HTML escaping intact and executes when the link is clicked; scheme validation is required, not just escaping',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A new service worker version is deployed, but users keep getting the old one even after reloading the tab. Why, and what changes it?',
        options: [
          'The new worker stays in the waiting state until every controlled client is closed, because a reload keeps an overlapping client alive; skipWaiting() (plus clients.claim()) activates it sooner',
          'The browser caches SW scripts for 7 days with no override',
          'Reloading unregisters pending workers, so the update is discarded',
          'The old worker must call terminate() before a new one can install',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A service worker caches responses from cross-origin no-cors requests. What is a hidden storage consequence?',
        options: [
          'Opaque responses are deduplicated by URL, so storage stays minimal',
          'Opaque responses report status 0 and are stored with large padded quota sizes (potentially megabytes each), rapidly exhausting origin storage',
          'no-cors responses cannot be stored in the Cache API at all',
          'The cache converts them to text, stripping binary data',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A library sets "sideEffects": false in package.json. A consumer does import "lib/styles.css" and the styles vanish from the production build. What happened?',
        options: [
          'The CSS loader requires sideEffects: true globally',
          'CSS imports are only kept when a JS export is also imported from the same file',
          'The bundler treated every module in the package as pure, so the bare import with no used exports was tree-shaken away; the CSS file must be listed in a sideEffects array',
          'Minification removed the CSS because it had no matching selectors',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Given: type F<T> = T extends string ? "yes" : "no";\nWhat is F<string | number>?',
        options: [
          '"no", because a union is not assignable to string',
          '"yes", because the union contains string',
          'never, because unions cannot enter conditional types',
          '"yes" | "no", because conditional types distribute over each member of a naked type parameter union',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In TypeScript, passing the object literal { name: "x", extra: 1 } directly to a parameter typed { name: string } errors, but assigning it to a variable first and passing that compiles. Why?',
        options: [
          'Excess property checks apply only to fresh object literals at the point of assignment; once widened through a variable, normal structural compatibility allows extra properties',
          'Variables are contravariant while literals are covariant',
          'The compiler freezes literals, making extra keys readonly violations',
          'Passing via a variable enables implicit any, silencing the check',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What do [NaN].includes(NaN) and [NaN].indexOf(NaN) evaluate to, respectively?',
        options: [
          'false and -1, because NaN never equals itself anywhere',
          'true and -1, because includes uses SameValueZero (NaN matches NaN) while indexOf uses strict equality',
          'true and 0, because arrays normalize NaN internally',
          'false and 0, because indexOf uses SameValue while includes uses ===',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
    ],
    2: [
      {
        question: 'What does this log?\nPromise.resolve().then(() => console.log(1)).then(() => console.log(3));\nPromise.resolve().then(() => console.log(2)).then(() => console.log(4));',
        options: [
          '1 3 2 4, because each chain drains fully before the next starts',
          '2 4 1 3, because the second chain was resolved more recently',
          '1 2 3 4, because each .then queues one microtask at a time, so the two chains interleave round-robin',
          'The order is nondeterministic across engines',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'function loop() { queueMicrotask(loop); }\nloop();\nversus\nfunction loop2() { setTimeout(loop2, 0); }\nloop2();\nWhat is the key behavioral difference?',
        options: [
          'They behave identically since each schedules one callback at a time',
          'The setTimeout version freezes the page because timers outrank rendering',
          'The microtask version is throttled to 4ms per iteration by spec',
          'The microtask version starves the event loop and blocks rendering forever, because the microtask queue must be empty before the browser can render; the timeout version yields between iterations',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What does console.log([] == ![]) output, and why?',
        options: [
          'true, because ![] is false, [] coerces to "" then 0, and false coerces to 0, so 0 == 0',
          'false, because an object never loosely equals a boolean',
          'A TypeError, because ! cannot be applied to arrays',
          'true, because == compares references after negation normalizes them',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What happens here?\nconsole.log(typeof x);\nlet x = 1;',
        options: [
          'Logs "undefined", because typeof is always safe on undeclared names',
          'Throws a ReferenceError, because x is in the temporal dead zone and even typeof cannot observe a let binding before initialization',
          'Logs "number", because let declarations are fully hoisted with their value',
          'Throws a SyntaxError at parse time due to use-before-declare',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In an ES module:\nconst obj = { x: 1, getX: () => this.x };\nconsole.log(obj.getX());\nWhat is logged?',
        options: [
          '1, because arrow functions bind this to the object they are defined in',
          'A ReferenceError, because this is illegal inside object literals',
          'undefined (after a TypeError-free lookup), because the arrow captures the module-level this, which is undefined in ESM, making this.x throw a TypeError instead',
          'globalThis.x, because arrows default to the global object in all module types',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A grid uses repeat(auto-fill, minmax(200px, 1fr)) and has 2 items in a 1200px container. The items stay 200-ish px wide with empty space. Switching to auto-fit makes them expand. Why?',
        options: [
          'auto-fit raises the minmax minimum to fill the row',
          'auto-fill caps tracks at their min size when the row is not full',
          'auto-fit places items on the implicit grid where 1fr resolves differently',
          'auto-fill keeps the empty repeated tracks in layout, while auto-fit collapses empty tracks to zero so the fr maxima of occupied tracks absorb the freed space',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A child div with margin-top: 40px sits first inside a parent with no padding or border, and the 40px gap mysteriously appears above the parent instead of inside it. What is happening?',
        options: [
          'Parent-child margin collapsing: adjacent top margins with nothing between them collapse through, moving the margin outside; adding parent padding, a border, or establishing a BFC stops it',
          'The child is auto-positioned relative to the viewport',
          'Percentage margin resolution pushed the margin to the nearest positioned ancestor',
          'The browser reparents leading margins to the body by default',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A position: fixed toolbar works site-wide, but inside one page section it scrolls with the content. The section has a CSS animation using transform. What is the cause?',
        options: [
          'Fixed positioning is disabled inside any element with overflow: hidden',
          'A transformed (or filtered, or perspective) ancestor becomes the containing block for fixed descendants, so the toolbar is now positioned relative to that ancestor rather than the viewport',
          'The animation promotes the toolbar to its own layer, which detaches fixed behavior',
          'will-change on a sibling steals the compositing layer from the toolbar',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A page defines @layer base, components; with a very specific selector inside @layer components, and one plain unlayered rule with a single class targeting the same property. Which wins?',
        options: [
          'The layered rule, because layer order beats declaration order',
          'The layered rule, because higher specificity always wins across layers',
          'The unlayered rule, because unlayered author styles rank above all declared layers in the cascade regardless of specificity',
          'Whichever appears later in source order, since the cascade treats them equally',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'The main thread is blocked for 2 seconds by a synchronous loop. Which animation keeps running smoothly during that time?',
        options: [
          'A requestAnimationFrame-driven transform animation',
          'A CSS animation of the left property on a positioned element',
          'A Web Animations API animation of height',
          'A CSS transform/opacity animation already running on its own compositor layer, because the compositor thread advances it without the main thread',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'You need to run code strictly after the browser has painted the result of a DOM change. Which technique is most reliable?',
        options: [
          'Schedule requestAnimationFrame, and from inside it schedule another requestAnimationFrame (or a setTimeout 0); the nested callback runs after the frame containing the change is presented',
          'A single requestAnimationFrame, which always fires post-paint',
          'A microtask via queueMicrotask, since microtasks follow rendering',
          'A setInterval of 16ms aligned to the display refresh rate',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A React list of uncontrolled inputs uses array indexes as keys. After deleting the first item, the remaining rows show the wrong text values. Why?',
        options: [
          'Index keys disable reconciliation, forcing full remounts that clear values',
          'With index keys, React matches old and new elements by position, so DOM state like uncontrolled input values sticks to positions instead of following logical items',
          'Uncontrolled inputs re-read defaultValue whenever a sibling unmounts',
          'React sorts children by key before diffing, scrambling the rows',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In React 18+ development with StrictMode, a useEffect with an empty dependency array logs twice on load. What is React doing?',
        options: [
          'Rendering on both server and client, firing the effect in each pass',
          'Running the effect once per fiber lane as part of concurrent scheduling',
          'Intentionally mounting, running effects, simulating unmount (running cleanups), and remounting to surface effects that are not resilient to re-execution; production runs them once',
          'Deduplicating failed: the second log indicates a memory leak in the effect',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A tooltip must be measured and repositioned before the user can see a misplaced frame. Comparing useEffect and useLayoutEffect for this, which statement is correct?',
        options: [
          'useEffect is synchronous after commit, so either hook prevents flicker',
          'useLayoutEffect runs before the DOM mutations are committed, so measurements are stale',
          'Only a ref callback can run before paint; neither effect hook can',
          'useLayoutEffect fires synchronously after DOM mutation but before paint, and a setState inside it re-renders synchronously so the user never sees the intermediate position; useEffect could flash',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In Vue 3, a watchEffect reads an element via a template ref to measure it, but on first run the DOM is not updated yet. Which explains the default behavior?',
        options: [
          'watchEffect defaults to flush: "pre", running before component re-render/DOM update; use flush: "post" (or watchPostEffect) to observe the updated DOM',
          'watchEffect always runs after paint, so the ref must be null for another reason',
          'Template refs only populate inside computed getters',
          'Reactivity tracking is disabled during the first effect run',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In Vue 3, const s = shallowRef({ count: 0 }); s.value.count++ does not update the template, but s.value = { count: 1 } does. Why?',
        options: [
          'shallowRef proxies one level deep, so count is tracked but not triggered',
          'shallowRef tracks only the .value reference itself; inner properties are not reactive, so only replacing the whole value triggers effects',
          'Increment operators bypass Proxy set traps in Vue',
          'The template compiler caches shallowRef reads per render',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A React context Provider is rendered as <Ctx.Provider value={{ user, theme }}>. Consumers re-render on every parent render even when user and theme are stable. Why?',
        options: [
          'Context consumers always re-render with the provider subtree by design',
          'Provider values are deep-compared, and nested objects defeat the comparison',
          'The inline object literal creates a new value identity each render, and context change detection uses Object.is; memoizing the object (useMemo) prevents the false updates',
          'Two values in one context require two providers to avoid re-renders',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Why does React recommend useSyncExternalStore over a plain useEffect subscription for external stores under concurrent rendering?',
        options: [
          'It batches store updates into transitions automatically',
          'It memoizes snapshots so stores can skip emitting changes',
          'Effect subscriptions leak because concurrent rendering skips cleanups',
          'It prevents tearing: during an interruptible render, different components could otherwise read different store versions in the same UI; the hook forces a consistent synchronous snapshot and re-render on mismatch',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Which element change can make the reported LCP element switch to a different element mid-load?',
        options: [
          'A larger image finishing its render later than the current candidate: LCP updates to the new largest contentful paint until user interaction or scroll locks in the final entry',
          'Any element gaining a box-shadow larger than the viewport',
          'The current LCP element being clicked, which promotes the next candidate',
          'A CSS background-color change on the body, which resets all candidates',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A same-origin WOFF2 font is preloaded with <link rel="preload" as="font" type="font/woff2"> but the network panel shows it downloaded twice. What is missing?',
        options: [
          'A media attribute matching the active viewport',
          'The crossorigin attribute: font fetches are always CORS-mode requests, so the preload without it cannot be matched to the actual anonymous font request',
          'as="woff2" instead of as="font" so the cache keys align',
          'A Vary: Origin response header on the font file',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Module a.mjs imports { b } from b.mjs, and b.mjs imports { a } from a.mjs (a circular import). b.mjs runs first in the cycle and calls a function that reads the binding from a.mjs at top level. What happens?',
        options: [
          'The import silently resolves to undefined with no way to fix it',
          'The bundler duplicates both modules to break the cycle',
          'Because ESM bindings are live but a.mjs has not evaluated yet, accessing a let/const binding throws a ReferenceError (TDZ); deferring access until after evaluation (e.g. inside a function called later) works',
          'ESM forbids cycles, so the graph fails at link time',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'You convert a div into a custom button with role="button" and tabindex="0". Beyond that, what must you implement to match native button semantics for keyboard users?',
        options: [
          'Nothing; role="button" makes browsers synthesize click on Enter and Space',
          'Only an Escape handler to blur the control',
          'A keypress handler for Enter, since Space never activates buttons',
          'Explicit activation on both Enter and Space (including preventing Space from scrolling the page), because ARIA roles change semantics only, never behavior',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'An element has aria-labelledby="title", aria-label="Close dialog", and visible inner text "X". What accessible name is computed?',
        options: [
          'The text content of the element referenced by aria-labelledby, because it has the highest precedence in the accessible name computation, beating aria-label and content',
          '"Close dialog", because aria-label always wins over references',
          '"X", because visible content overrides all ARIA naming',
          'A concatenation of all three sources in DOM order',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A fetch uses credentials: "include" and the server responds with Access-Control-Allow-Origin: *. What happens?',
        options: [
          'The request succeeds because * matches every origin',
          'The browser blocks the response: the wildcard is invalid for credentialed requests, which require the exact origin echoed plus Access-Control-Allow-Credentials: true',
          'The response arrives but cookies are silently stripped from it',
          'The browser retries automatically without credentials',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Which statement about SameSite cookie behavior is correct?',
        options: [
          'SameSite=Lax blocks all cross-site requests including link clicks',
          'SameSite=None works over HTTP as long as the domain matches',
          'SameSite=Lax sends the cookie on top-level cross-site GET navigations (like clicking a link) but not on cross-site subresource or POST requests; SameSite=None requires the Secure attribute',
          'SameSite=Strict permits cross-site iframes but not fetch()',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A page sends the header: Content-Security-Policy: require-trusted-types-for \'script\'. What is the direct effect on element.innerHTML = userString?',
        options: [
          'The string is auto-sanitized by the browser before parsing',
          'Nothing changes; Trusted Types only govern eval()',
          'innerHTML becomes read-only for the whole document',
          'The assignment throws unless the value is a TrustedHTML object produced by a Trusted Types policy, closing off raw-string DOM XSS injection sinks',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'When does the browser check for a byte-different service worker script and start the update process?',
        options: [
          'On navigations in scope and on functional events (and register() calls), fetching the script while bypassing the HTTP cache if it is older than 24 hours per updateViaCache rules',
          'Only when registration.update() is called manually from the page',
          'Every 60 seconds while any client is open, per spec heartbeat',
          'Only when the browser restarts and re-parses all registrations',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In a service worker install handler, event.waitUntil(caches.open("v1").then(c => c.addAll(urls))) is used and one URL returns a 404. What is the outcome?',
        options: [
          'The 404 body is cached and served as a valid response later',
          'cache.addAll rejects on any non-ok response, the waitUntil promise rejects, and the whole install fails, so the new worker never reaches the waiting state',
          'The failing URL is skipped and the remaining URLs are cached',
          'Install succeeds but the activate event is skipped once',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Which assignment is rejected by the TypeScript compiler?\nlet u: unknown; let a: any; let s: string;',
        options: [
          'u = s, assigning a string into unknown',
          'a = u, assigning unknown into any',
          's = u, assigning unknown into string, because unknown is only assignable to unknown and any without narrowing',
          's = a, assigning any into string',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'With strictFunctionTypes enabled, why can an interface method declared as method(x: Dog): void still be implemented unsafely with a Animal-incompatible narrower type, while a property declared as method: (x: Dog) => void is checked strictly?',
        options: [
          'Properties skip variance checks entirely, methods do not',
          'strictFunctionTypes only applies inside classes, not interfaces',
          'Method shorthand infers parameters as any under strict mode',
          'strictFunctionTypes deliberately exempts method shorthand syntax, keeping parameters bivariant for ergonomic reasons (e.g. arrays), while function-typed properties get sound contravariant parameter checking',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
    ],
    3: [
      {
        question: 'What is the output order?\nconst p = Promise.resolve();\n(async () => { await p; console.log("A"); })();\np.then(() => console.log("B")).then(() => console.log("C"));',
        options: [
          'A B C, because await on a native promise costs one microtask tick, and the async function subscribed before the .then chain',
          'B A C, because .then callbacks outrank await resumptions',
          'B C A, because await adds two extra ticks over a then chain',
          'A C B, because chained thens run in reverse registration order',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What does console.log(new Array(3).map(() => 1)) show, and why?',
        options: [
          '[1, 1, 1], because map visits every index up to length',
          'An array of length 3 with only holes (e.g. [empty x 3]), because new Array(3) creates holes and map skips holes entirely while preserving them',
          '[undefined, undefined, undefined], because holes are read as undefined and mapped',
          'A RangeError, because map cannot run on sparse arrays',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What does Object.keys({ b: 1, 2: 2, a: 3, 1: 4 }) return?',
        options: [
          '["b", "2", "a", "1"], preserving pure insertion order',
          '["1", "2", "a", "b"], fully sorted lexicographically',
          '["1", "2", "b", "a"], because integer-like keys come first in ascending numeric order, followed by string keys in insertion order',
          'The order is unspecified and engine-dependent',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What happens when you call structuredClone({ id: 1, format: (x) => x }) in the browser?',
        options: [
          'It returns a clone where format is converted to its source string',
          'It clones id and silently drops the format property',
          'It returns the clone with format sharing the original function reference',
          'It throws a DataCloneError, because functions are not structured-cloneable',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A block element uses padding-top: 56.25% to reserve space for a 16:9 video before load. Why does this percentage produce the correct height?',
        options: [
          'Percentage padding (even vertical) resolves against the inline size (width) of the containing block, so 56.25% of the width yields a 16:9 box',
          'Vertical percentage padding resolves against the parent height, which browsers default to the width',
          'The browser special-cases 56.25% as an aspect-ratio keyword',
          'padding-top percentages resolve against the viewport width',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Markup: <div><h2>t</h2><p>one</p><p>two</p></div>. Which paragraph does p:nth-child(2) select?',
        options: [
          '"two", because only p elements are counted by nth-child',
          '"one", because nth-child counts all siblings regardless of type, and the selector matches only when that nth child also happens to be a p; the h2 occupies position 1',
          'Neither, because nth-child requires an explicit universal parent selector',
          '"two", because heading elements are excluded from child indexes',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A grid column defined as 1fr still overflows the container when one cell holds a very long word. Replacing it with minmax(0, 1fr) fixes it. Why?',
        options: [
          '1fr rounds up to the largest content size in the column by definition',
          'minmax(0, 1fr) enables automatic word wrapping in all cells',
          '1fr is shorthand for minmax(auto, 1fr), so the auto minimum floors the track at its min-content size; an explicit 0 minimum lets the track shrink below the content',
          'fr units ignore the container width unless a minimum is given',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'You want a fade-in when an element goes from display: none to display: block using only CSS transitions. Which modern mechanism makes this possible?',
        options: [
          'Setting transition-delay so display flips after opacity finishes',
          'Wrapping the change in requestAnimationFrame, which forces transitions on display',
          'Using visibility instead, which has always transitioned discretely with keyframes',
          'Declaring @starting-style for the entry state plus transition-behavior: allow-discrete so display participates in the transition and the opacity animates from the starting style',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A long list uses content-visibility: auto, and the scrollbar thumb jumps around while scrolling. Which companion CSS property addresses this?',
        options: [
          'contain-intrinsic-size, which supplies a placeholder size for skipped elements so the layout estimate (and scrollbar geometry) stays stable',
          'overflow-anchor: none, which pins the scrollbar to the top',
          'scroll-snap-stop: always, which quantizes thumb movement',
          'min-block-size: max-content, which forces full layout of skipped items',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A touchstart listener is registered with { passive: true } and calls event.preventDefault(). What happens?',
        options: [
          'Scrolling is prevented, but only after the handler returns',
          'The preventDefault call is ignored (with a console warning) because passive listeners promise not to cancel; the browser can start scrolling without waiting for the handler',
          'The listener is demoted to non-passive at first preventDefault call',
          'An exception is thrown synchronously inside the handler',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'React code:\nconst [n, setN] = useState(0);\nuseEffect(() => {\n  const id = setInterval(() => console.log(n), 1000);\n  return () => clearInterval(id);\n}, []);\nAfter clicking a button that calls setN(5), what does the interval log?',
        options: [
          '5, because state reads always resolve against the fiber, not the closure',
          'Nothing, because setN(5) restarts the effect and kills the interval',
          '0 forever, because the interval callback closed over the initial render scope where n was 0, and the empty dependency array means it is never re-created',
          '0 then 5, because closures re-bind after each commit',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In React, wrapping an expensive filter-state update in startTransition changes behavior how?',
        options: [
          'The update runs in a Web Worker off the main thread',
          'The update is debounced by a fixed 300ms interval',
          'React skips rendering entirely until the browser is idle',
          'The update is marked non-urgent: React keeps showing the previous UI, can interrupt the in-progress render if urgent input arrives, and isPending lets you show inline feedback',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A component wrapped in React.memo re-renders every time its parent renders, even though its only prop is children (static-looking JSX). Why does memo fail here?',
        options: [
          'The parent creates a new React element object for children on every render, and memo compares props with Object.is, so the children prop is always unequal; hoisting or memoizing the element fixes it',
          'memo ignores the children prop and always re-renders when it exists',
          'JSX children are functions, which memo cannot compare',
          'memo only works for class components receiving primitive props',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In Vue 3, <li v-for="item in items" v-if="item.active"> fails with item being undefined. Why?',
        options: [
          'v-if compiles into a slot that executes before props resolve',
          'When both are on the same element, v-if has higher priority than v-for in Vue 3, so the condition is evaluated before the loop variable exists; Vue 2 had the opposite priority',
          'v-for skips rendering entirely when a sibling directive is present',
          'The template compiler evaluates directives alphabetically',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Which limitation of Vue 2 reactivity was eliminated by the Proxy-based system in Vue 3?',
        options: [
          'Vue 2 could not track reads inside computed properties',
          'Vue 2 re-rendered the whole app on any mutation',
          'Vue 2 could not detect property addition/deletion on objects or direct index assignment on arrays (requiring Vue.set), because Object.defineProperty must wrap known keys ahead of time',
          'Vue 2 required all state to be declared as refs',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Why can signal-based frameworks (Solid, Vue Vapor-style, Svelte 5 runes) update the DOM without diffing a virtual tree?',
        options: [
          'They re-run the whole component function and patch by string comparison',
          'They compile templates to WebAssembly for direct memory writes',
          'They snapshot the DOM before every update and restore deltas',
          'Reads are tracked at fine granularity so each signal maps to the specific DOM effects that consumed it; a write runs exactly those effects, making structural diffing unnecessary',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'How does Total Blocking Time (TBT) score a single 120ms long task on the main thread between FCP and TTI?',
        options: [
          'It contributes 70ms, because only the portion beyond the 50ms long-task threshold counts as blocking time',
          'It contributes the full 120ms, since any long task counts wholly',
          'It contributes 50ms, capped at the threshold per task',
          'It contributes nothing unless an input occurred during the task',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Back/forward navigations feel slow on your site because pages never restore from the bfcache. Which is a documented blocker?',
        options: [
          'Using history.pushState anywhere on the page',
          'Registering an unload event handler (or serving Cache-Control: no-store), both of which make many browsers refuse to store the page in the back/forward cache',
          'Having more than one iframe on the page',
          'Using passive scroll listeners on document',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A hero image is the LCP element. Which combination most damages LCP?',
        options: [
          'fetchpriority="high" plus preloading the image',
          'Inlining the image dimensions as width/height attributes',
          'loading="lazy" on that image, which defers the request until layout confirms visibility, removing it from the preload scanner critical path',
          'Serving it as AVIF with a JPEG fallback via srcset',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In a client-side routed SPA, a user activates a nav link and the view swaps silently; screen reader users hear nothing and keyboard focus stays on the removed link. What is the recommended remediation?',
        options: [
          'Trigger a full page reload on every route change',
          'Add aria-live="assertive" to the body element',
          'Set autofocus on the first input of every page',
          'On route change, move focus programmatically to the new content (e.g. a tabindex="-1" heading or main region) and/or announce the new page title via a live region',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'For text meant only for screen reader users, why is the visually-hidden CSS pattern (1px clipped absolute positioning) used instead of display: none?',
        options: [
          'display: none removes the element from the accessibility tree entirely, so it would never be announced; the clip pattern keeps it in layout-invisible but accessible form',
          'display: none text is announced twice by most screen readers',
          'The clip pattern is required for aria-label to take effect',
          'display: none blocks keyboard focus for all following siblings',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A client-side deep-merge of user-supplied JSON into a config object lets an attacker send {"__proto__": {"isAdmin": true}}. What is the resulting vulnerability class and impact in the browser?',
        options: [
          'CSRF: the merged object forges cross-site request tokens',
          'Prototype pollution: properties land on Object.prototype, so unrelated objects inherit attacker-controlled values, which can flip security checks or feed XSS gadgets in libraries',
          'Clickjacking: the prototype chain overlays invisible frames',
          'Cache poisoning: the merged keys alter HTTP cache partitioning',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A response includes X-Frame-Options: DENY and Content-Security-Policy: frame-ancestors \'self\'. A same-origin page tries to iframe it in a modern browser. What happens?',
        options: [
          'The frame is blocked, because the strictest header always wins',
          'The frame loads only if the parent sends an Origin header',
          'The frame loads: when frame-ancestors is present, CSP-supporting browsers must ignore X-Frame-Options, and self permits same-origin framing',
          'The browser refuses to parse conflicting headers and blocks the page entirely',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A page listens for messages: window.addEventListener("message", e => eval-like-use(e.data)). It only expects messages from its own popup. What is the essential missing control?',
        options: [
          'Wrapping the handler in a try/catch to contain malicious payloads',
          'Switching to BroadcastChannel, which authenticates senders by default',
          'Calling e.stopImmediatePropagation() to block other listeners',
          'Validating e.origin against an exact allowlist (and treating e.data as untrusted), because any window that obtains a reference can post messages to it',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A service worker script is served from /static/js/sw.js but must control the entire origin including /. What is required?',
        options: [
          'The server must send a Service-Worker-Allowed header (e.g. "/") on the SW script response, because the default maximum scope is the script URL path; register() alone cannot widen it',
          'Passing scope: "/" to register(), which always overrides the path restriction',
          'Adding a <base href="/"> tag to the registering document',
          'Nothing; a service worker always controls the whole origin',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Inside a service worker fetch handler, respondWith is called inside a setTimeout callback and the browser throws "respondWith cannot be called". Why?',
        options: [
          'respondWith may only be called once per worker lifetime',
          'respondWith must be called synchronously during fetch event dispatch; once the handler returns without calling it, the event is past the point of interception (put async work inside the promise passed to respondWith)',
          'Timers are forbidden in service workers, so the callback never ran',
          'The fetch event only allows respondWith for same-origin requests',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Why does Vite start a dev server almost instantly on a huge app while a classic webpack dev build takes much longer?',
        options: [
          'Vite compiles the whole app ahead of time during npm install',
          'Vite caches the previous webpack build output and replays it',
          'Vite serves source files over native ESM on demand, transforming only what the browser requests, and prebundles bare-import dependencies once with esbuild instead of bundling the whole graph upfront',
          'Vite skips TypeScript and JSX transforms in development mode',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'After fixing one typo in a single module, users have to re-download nearly every [contenthash] chunk of your webpack app. Which mechanism explains this cache invalidation cascade?',
        options: [
          'The CDN purges all sibling assets when any one file changes',
          'contenthash includes the build timestamp in every chunk',
          'Brotli compression dictionaries link chunks to each other',
          'Changing one module shifts module/chunk ids and the runtime manifest embedded in other chunks, changing their bytes and thus hashes; stable ids (moduleIds: "deterministic") and extracting the runtime chunk restore long-term caching',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In TypeScript, what does "satisfies" give you in: const cfg = { port: 8080, host: "localhost" } satisfies Config, compared to const cfg: Config = ...?',
        options: [
          'The literal is checked against Config, but cfg keeps its narrower inferred type (e.g. port: number, host: string literal-level detail) instead of being widened to Config',
          'It disables excess property checks that annotations perform',
          'It converts the value to Config at runtime with a validation pass',
          'It makes cfg readonly, equivalent to as const plus the annotation',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Given: type IsNever<T> = T extends never ? true : false;\nWhat is IsNever<never>, and why?',
        options: [
          'true, because never trivially extends never',
          'never, because the conditional distributes over the members of never (an empty union), producing no branches at all; wrapping as [T] extends [never] yields true',
          'false, because never extends nothing including itself',
          'A compile error, because never cannot instantiate a type parameter',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
    ],
  },
};
