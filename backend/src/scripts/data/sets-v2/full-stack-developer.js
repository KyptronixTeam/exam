module.exports = {
  category: 'Full Stack Developer',
  sets: {
    1: [
      {
        question: 'What is logged?\nPromise.resolve().then(() => console.log(1));\nqueueMicrotask(() => console.log(2));\nsetTimeout(() => console.log(3), 0);\nconsole.log(4);',
        options: [
          '4 1 2 3, because sync code runs first, then the microtask queue drains in FIFO order before the timer macrotask',
          '4 2 1 3, because queueMicrotask has higher priority than promise reactions',
          '4 3 1 2, because a zero-delay timer fires before microtasks scheduled in the same tick',
          '1 2 4 3, because promise callbacks resolved synchronously run before the remaining sync code',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What is logged?\nconst arr = [1, 2, 3];\narr[10] = 11;\nconsole.log(arr.filter(x => x === undefined).length);',
        options: [
          '7, because the holes between index 3 and 9 read as undefined and pass the predicate',
          '0, because filter skips holes in sparse arrays entirely, so the predicate never sees them',
          '1, because only index 10 is treated as a defined slot next to a single collapsed hole',
          'A RangeError is thrown, because assigning past arr.length is not allowed on a literal array',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In TypeScript, what does the type `keyof any` resolve to, and why does it matter for writing generic mapped types?',
        options: [
          'unknown, because any has no statically known keys',
          'string, because object keys are always coerced to strings at runtime',
          'string | number | symbol, the set of all valid property key types, which is why it is used as the constraint for key type parameters',
          'never, because keyof cannot be applied to a non-object type',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What is logged?\nconst obj = {\n  x: 10,\n  getX: () => this.x\n};\nconsole.log(obj.getX.call({ x: 20 }));\n(assume non-strict module top level where `this` is not the object)',
        options: [
          '20, because call() rebinds this to the passed object',
          '10, because a method defined on obj closes over obj as its receiver',
          'A TypeError is thrown, because arrow functions cannot be invoked with call()',
          'undefined, because arrow functions ignore call() rebinding and capture the enclosing scope’s this, which has no x',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A React list uses the array index as key, and each row renders an uncontrolled <input>. When the user deletes the first item, the remaining rows show the wrong text in their inputs. What is the root cause?',
        options: [
          'React matches elements by key during reconciliation, so with index keys the DOM nodes (and their input state) are reused for different logical rows',
          'Uncontrolled inputs re-mount on every parent render, wiping and reshuffling their values',
          'The delete handler mutates the array in place, so React batches the wrong state snapshot',
          'React always re-renders list children top-down, so any deletion shifts refs but not props',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'function C({ show }) {\n  const [a, setA] = useState(0);\n  if (show) {\n    const [b] = useState(1);\n  }\n  const [c] = useState(2);\n  ...\n}\nWhy does React break when `show` changes between renders?',
        options: [
          'Conditional hooks create duplicate state cells that leak memory until unmount',
          'React associates hook state with call order, so skipping a hook shifts every later hook onto the wrong stored state slot',
          'useState inside a conditional runs outside the render phase, so updates are silently dropped',
          'The b state is hoisted to module scope and shared between all component instances',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'React 18, createRoot. A click handler runs:\nsetCount(c => c + 1);\nsetFlag(f => !f);\nsetTimeout(() => {\n  setCount(c => c + 1);\n  setFlag(f => !f);\n}, 0);\nHow many re-renders does this component perform in total?',
        options: [
          '4, because each state setter always triggers its own render pass',
          '1, because React coalesces all updates scheduled within the same event loop turn',
          '2, because React 18 batches updates inside the handler into one render and also automatically batches the two updates inside the timeout into another',
          '3, because handler updates are batched but timeout updates each render separately',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In React 18 development mode with <StrictMode>, a useEffect opens a WebSocket but returns no cleanup. QA reports every message is handled twice in dev but once in prod. Why?',
        options: [
          'StrictMode runs the render phase twice, creating two component instances that share one effect',
          'Dev builds disable effect deduplication, so identical effects run per render',
          'The WebSocket API buffers and replays messages when a page has multiple listeners in dev tools',
          'StrictMode intentionally mounts, unmounts, and remounts effects; without cleanup the first socket is never closed, leaving two live subscriptions',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In Node.js, inside an fs.readFile callback you schedule both setTimeout(fn, 0) and setImmediate(fn2). Which runs first, and why?',
        options: [
          'setImmediate always runs first, because the check phase directly follows the poll phase where the I/O callback executed, while timers wait for the next loop iteration',
          'setTimeout always runs first, because timers have the highest phase priority in libuv',
          'The order is nondeterministic here, just as it is in the main module, because it depends on process startup timing',
          'They run in registration order, because both queues are merged inside I/O callbacks',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Node.js service streams rows to a slow HTTP client. writable.write(chunk) starts returning false but the code keeps writing in a tight loop. What is the correct handling and the consequence of ignoring it?',
        options: [
          'Call writable.cork() until the client catches up; ignoring it causes chunks to be dropped silently',
          'Stop writing and resume on the drain event; ignoring the false return buffers unbounded data in memory and can exhaust the heap',
          'Switch the stream to objectMode; ignoring it throws ERR_STREAM_WRITE_AFTER_END',
          'Retry the same write in a setImmediate loop; ignoring it deadlocks the poll phase',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Using the Node.js cluster module, four workers all call server.listen(3000) without an EADDRINUSE error. How is this possible?',
        options: [
          'Each worker binds with SO_REUSEPORT so the kernel load-balances sockets between processes',
          'Only the first worker actually binds; the rest silently become hot standbys',
          'The primary process binds the port and distributes incoming connections to workers over IPC (round-robin by default on Linux)',
          'The cluster module rewrites each worker to listen on port 3000 + worker id and proxies internally',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In Node.js, what is logged?\nprocess.nextTick(() => console.log(1));\nPromise.resolve().then(() => console.log(2));\nqueueMicrotask(() => console.log(3));',
        options: [
          '2 3 1, because promise microtasks preempt the nextTick queue',
          '3 2 1, because queueMicrotask enqueues ahead of promise reactions',
          '2 1 3, because nextTick interleaves between individual microtasks',
          '1 2 3, because the nextTick queue fully drains before the microtask queue, which then runs FIFO',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A PATCH /accounts/42 endpoint appends an entry to the account’s audit array. A client retries the request after a network timeout. What is the API design flaw?',
        options: [
          'The operation is not idempotent, so an automatic retry after an ambiguous failure can apply the append twice',
          'PATCH must not modify arrays; only PUT may replace collection-valued fields',
          'PATCH requests are cached by intermediaries, so the retry may be served a stale 200 without executing',
          'The flaw is only that PATCH lacks a request body schema; retries are otherwise safe',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'An API responds with Cache-Control: no-cache. What behavior does this actually mandate for a shared cache?',
        options: [
          'The response must never be written to the cache at all; that requires no-store semantics',
          'The response may be stored, but it must be revalidated with the origin (e.g., via ETag/If-None-Match) before every reuse',
          'The response may be served stale for up to 60 seconds while revalidating in the background',
          'Only browsers must revalidate; shared proxies may serve it without contacting the origin',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A GraphQL query { posts { title author { name } } } triggers 1 query for posts and then one SQL query per post for its author. What is the standard fix?',
        options: [
          'Enable persisted queries so the server caches the SQL execution plan across resolvers',
          'Mark the author field @defer so author queries run after the response is flushed',
          'Use a per-request DataLoader that collects author IDs during the resolution tick and batches them into a single IN query, deduplicating repeats',
          'Increase the database connection pool so the N author queries run in parallel',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A monitoring team complains a GraphQL API "never returns errors" — every response is HTTP 200, even when resolvers fail. What explains this convention?',
        options: [
          'GraphQL servers swallow resolver exceptions unless a global error plugin is installed',
          'HTTP status codes are reserved for schema validation failures only, never execution failures',
          'The spec requires 200 so that CDNs can cache error responses alongside data',
          'GraphQL distinguishes transport errors from field errors: execution failures are reported in the errors array, often alongside partial data, so the HTTP layer still succeeds',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A table has a composite B-tree index on (tenant_id, created_at). Which query can NOT use this index effectively?',
        options: [
          'SELECT * FROM t WHERE created_at > now() - interval ‘1 day’ (no tenant filter)',
          'SELECT * FROM t WHERE tenant_id = 7 ORDER BY created_at DESC LIMIT 10',
          'SELECT * FROM t WHERE tenant_id = 7 AND created_at BETWEEN $1 AND $2',
          'SELECT * FROM t WHERE tenant_id IN (3, 7) AND created_at > $1',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A login query runs WHERE LOWER(email) = LOWER($1). There is a plain B-tree index on email, yet EXPLAIN shows a sequential scan. Why, and what is the fix?',
        options: [
          'The planner refuses case-insensitive comparisons on B-trees; switch to a hash index on email',
          'The index stores raw email values, so LOWER(email) is an expression the index cannot answer; create a functional index on LOWER(email) or use a case-insensitive collation/citext',
          'LOWER() forces a parameter type cast that disables all index usage on that connection',
          'B-tree indexes only support prefix matching; add a trigram index and rewrite with LIKE',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'At READ COMMITTED, two concurrent transactions each run: SELECT balance FROM acc WHERE id=1; then UPDATE acc SET balance = <read value - 50>. Both commit; only one debit persists. Which change fixes this lost update without going fully SERIALIZABLE?',
        options: [
          'Wrap the two statements in an explicit transaction; READ COMMITTED then locks the row at SELECT time',
          'Raise the isolation level to READ UNCOMMITTED so each transaction sees the other’s in-flight write',
          'Lock the row when reading with SELECT ... FOR UPDATE (or make the update atomic: SET balance = balance - 50), so the second transaction waits and reads the committed result',
          'Add an index on balance so the planner serializes the two updates through the index lock',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Two doctors are on call. Two concurrent snapshot-isolation (REPEATABLE READ) transactions each verify at least two doctors are on call, then each takes a different doctor off call. Both commit; now zero doctors are on call. What anomaly is this?',
        options: [
          'A dirty read, because each transaction saw the other’s uncommitted update',
          'A phantom read, because a re-run of the count query would return new rows',
          'A non-repeatable read, because the count changed between the read and the write',
          'Write skew: two transactions read overlapping data, make disjoint writes based on it, and snapshot isolation allows the combined result to violate the invariant',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'MongoDB query: db.orders.find({ status: ‘open’, total: { $gt: 100 } }).sort({ createdAt: 1 }). Per the equality-sort-range guideline, which compound index serves this best?',
        options: [
          '{ status: 1, createdAt: 1, total: 1 } — equality field first, then the sort key, then the range field',
          '{ status: 1, total: 1, createdAt: 1 } — match the filter order exactly, then the sort',
          '{ createdAt: 1, status: 1, total: 1 } — sort key first so no in-memory sort is needed',
          '{ total: 1, status: 1, createdAt: 1 } — range field first because it is most selective',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A JWT library verifies tokens using the algorithm named in the token header. The API validates RS256 tokens with an RSA public key. How can an attacker forge a valid token?',
        options: [
          'Brute-force the RSA private key from repeated signatures, since RS256 leaks key bits per token',
          'Craft a token with header alg=HS256 signed using the public key as the HMAC secret; the server verifies HMAC with the same (publicly known) key and accepts it',
          'Reuse the signature from an expired token, since exp is not part of the signed payload',
          'Set alg=RS512 so the library skips verification for unsupported algorithm variants',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In OAuth 2.0, why does PKCE protect a public client (SPA/mobile) against authorization-code interception, given the attacker can steal the code from the redirect?',
        options: [
          'PKCE encrypts the authorization code in transit so an intercepted code is unreadable',
          'PKCE binds the code to the client secret, which mobile apps store in the keychain',
          'The token endpoint requires the code_verifier matching the code_challenge sent at authorization; the interceptor has the code but not the verifier, so the exchange fails',
          'PKCE forces one-time-use redirect URIs, so a replayed redirect is rejected by the browser',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A pentest reports session fixation: an attacker sets a known session ID in the victim’s browser before login, and after the victim authenticates the attacker uses the same ID. What is the essential fix?',
        options: [
          'Store the session ID in localStorage instead of a cookie so it cannot be pre-set',
          'Sign session cookies so the server can detect attacker-issued identifiers',
          'Shorten session lifetime to minutes so a fixated session expires quickly',
          'Regenerate the session identifier upon privilege change (login), invalidating any pre-authentication ID the attacker planted',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A hot cache key for a rendered homepage expires; 5,000 concurrent requests all miss and simultaneously recompute it, briefly overwhelming the database. Which mitigation targets this exact failure mode?',
        options: [
          'Request coalescing (a per-key lock/single-flight so one caller recomputes while others wait) or probabilistic early refresh before expiry',
          'Consistent hashing of keys across cache nodes to spread the load of the miss',
          'Write-through caching so the database is always in sync with the cache',
          'Lowering the TTL so the value is recomputed more frequently in smaller bursts',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A service uses cache-aside: on write it updates the DB, then deletes the cache key; on read-miss it loads from DB and populates the cache. What stale-data race remains?',
        options: [
          'The delete can arrive before the DB commit is durable, so the cache is permanently empty',
          'A reader that missed can read the old DB value, get delayed, and write it into the cache after the writer’s delete, pinning stale data until the next eviction',
          'Two writers can interleave their DB updates so the cache holds a merged value from each',
          'Cache-aside requires transactions spanning the cache, so any concurrent read deadlocks',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A chat app scales from one Node.js WebSocket server to five behind a load balancer. Users on different instances stop receiving each other’s messages. What is the standard architecture fix?',
        options: [
          'Enable WebSocket compression so frames fit within the balancer’s relay buffer',
          'Switch the balancer to least-connections so chat peers land on the same instance',
          'Introduce a pub/sub backplane (e.g., Redis) that every instance subscribes to, so a message received on one node is broadcast to sockets held by all nodes',
          'Move message delivery to HTTP long-polling, which load balancers replicate natively',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A team sets the session cookie HttpOnly and declares stored XSS "mitigated" since scripts cannot read the cookie. What can an injected script still do?',
        options: [
          'Nothing meaningful; without the cookie value, requests from the injected script are unauthenticated',
          'Only deface the page visually; network APIs are blocked for injected code',
          'Read the cookie anyway via document.cookie, since HttpOnly only applies to subdomains',
          'Issue authenticated same-origin requests (fetch/XHR send the cookie automatically) and act as the user — change email, exfiltrate data — without ever reading the cookie',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Session cookies are set with SameSite=Lax and no CSRF tokens. Which cross-site request will still include the session cookie?',
        options: [
          'A top-level navigation via a link or GET form to the site — so any state-changing GET endpoint remains CSRF-exploitable',
          'A cross-site POST form auto-submitted by attacker JavaScript',
          'A cross-site fetch() with credentials: ‘include’ from the attacker’s origin',
          'An <img> tag on the attacker’s page pointing at the API',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Dockerized Node API never runs its SIGTERM shutdown handler; docker stop always takes 10 seconds and connections drop. The Dockerfile has: CMD npm start. What is the most likely cause?',
        options: [
          'Docker only delivers SIGKILL to containers; graceful shutdown requires docker pause first',
          'The shell-form CMD makes a shell (and npm) PID 1, which does not forward SIGTERM to the node child, so the app never sees it and Docker escalates to SIGKILL after the grace period',
          'Node.js ignores SIGTERM unless started with --enable-signals',
          'The handler must be registered before require() calls; late registration is discarded',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      }
    ],
    2: [
      {
        question: 'What is logged?\nconsole.log([1, 2, 3] + [4, 5, 6]);',
        options: [
          '[1,2,3,4,5,6], because + concatenates arrays like strings',
          'NaN, because arrays cannot be coerced to primitive numbers',
          '"1,2,34,5,6", because each array is coerced via toString() and the resulting strings are concatenated',
          'A TypeError is thrown, because + is not defined for object operands',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What is logged?\nasync function f() {\n  try {\n    return Promise.reject(‘boom’);\n  } catch (e) {\n    return ‘caught inside’;\n  }\n}\nf().then(v => console.log(‘ok’, v), e => console.log(‘outer’, e));',
        options: [
          'ok caught inside — the try/catch intercepts the rejected promise being returned',
          'ok boom — the rejection is unwrapped into a fulfilled value by the async return',
          'Nothing; the rejection is unhandled and crashes before the then callbacks run',
          'outer boom — returning (without awaiting) a rejected promise exits try normally; the rejection surfaces only in the caller',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'TypeScript: f({ a: ‘hi’, b: 1 }) is an error for f(x: { a: string }), yet this compiles:\nconst obj = { a: ‘hi’, b: 1 };\nf(obj);\nWhy?',
        options: [
          'Excess property checking applies only to fresh object literals at the call site; assigning to a variable first widens it and structural typing then permits extra properties',
          'const declarations freeze the type, disabling all property checks on subsequent uses',
          'The compiler infers obj as any because it has properties not mentioned in any signature',
          'Function parameters are bivariant, so the wider object type is accepted only via variables',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What is logged?\nconst m = new Map();\nm.set(NaN, ‘a’);\nconsole.log(m.get(NaN), m.get(Number(‘oops’)));',
        options: [
          'undefined undefined, because NaN !== NaN so the key can never be retrieved',
          'a a, because Map key equality uses SameValueZero, under which NaN equals NaN regardless of how it was produced',
          'a undefined, because only the original NaN reference matches the stored key',
          'A TypeError is thrown, because NaN is not a valid Map key',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What is logged, in order?\nconst p = new Promise(resolve => {\n  resolve(1);\n  console.log(‘A’);\n});\np.then(v => console.log(‘B’, v));\nconsole.log(‘C’);',
        options: [
          'B 1, A, C — resolving synchronously invokes then callbacks immediately',
          'A, B 1, C — the executor completes, then pending reactions flush before remaining sync code',
          'A, C, B 1 — the executor runs synchronously (resolve does not return early), and the then callback is always deferred to a microtask after sync code',
          'C, A, B 1 — the executor itself is deferred until the constructor call stack unwinds',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A component sets up a listener once:\nuseEffect(() => {\n  socket.on(‘msg’, () => console.log(count));\n}, []);\nAfter several setCount updates, incoming messages still log 0. What is happening?',
        options: [
          'The socket library caches the first payload of every handler for performance',
          'useEffect with [] runs before the first render, when state is still uninitialized',
          'setCount is asynchronous, so count only updates after the socket disconnects',
          'The handler closed over the count binding from the initial render; since the effect never re-runs, it keeps reading that stale closure value forever',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'With React concurrent rendering (e.g., inside startTransition), React may start rendering a component, pause, throw the work away, and re-render later. What constraint does this place on your component code?',
        options: [
          'Render must be pure: side effects (subscriptions, mutations, logging state changes) belong in effects/handlers, because render output may run multiple times or be discarded before commit',
          'Components must be memoized with React.memo, or concurrent rendering will loop forever',
          'All state updates must go through useReducer, since useState is not interrupt-safe',
          'Renders must complete within one frame, or React downgrades to synchronous mode permanently',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A parent renders <Profile key={userId} userId={userId} />. When userId changes, what does the key change cause, and why might a team choose this over syncing state in an effect?',
        options: [
          'React re-renders in place but resets only the props cache; state is preserved for performance',
          'React unmounts the old instance and mounts a fresh one, resetting all internal state — avoiding buggy "reset state when prop changes" effects',
          'React throws, because key must be unique among siblings, not vary over time',
          'React skips reconciliation entirely and reuses the previous committed DOM subtree',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In React, a click handler calls setValue(v) where Object.is(v, currentValue) is true. What does React do?',
        options: [
          'Always re-renders the component and all children, since setters unconditionally schedule work',
          'Throws a warning and ignores the call in development mode only',
          'Bails out of the update: it may re-render this component one extra time as a check, but skips deeper re-rendering and does not commit changes',
          'Defers the update until the next different value arrives, then applies them all at once',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In Node.js, why is stream.pipeline(a, b, c, cb) preferred over a.pipe(b).pipe(c) in production code?',
        options: [
          'pipeline runs the streams in worker threads, parallelizing transforms across cores',
          'pipe() copies chunks between streams, while pipeline passes them by reference',
          'pipeline disables backpressure checks, giving higher throughput on fast consumers',
          'pipe() does not propagate errors or reliably destroy peer streams on failure, which leaks file descriptors/memory; pipeline forwards errors to the callback and tears all streams down',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Node service calls fs.readFile on a 3 GB log file and crashes (buffer allocation error / OOM), while the same processing works via createReadStream. Why?',
        options: [
          'readFile materializes the entire file as one Buffer in memory — subject to heap limits and the maximum Buffer size — while streaming processes bounded chunks',
          'readFile uses the thread pool, which caps files at 1 GB per read operation',
          'createReadStream memory-maps the file, so the OS pages it without using Node memory at all',
          'readFile decodes the file as UTF-8 by default, quadrupling its in-memory size',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A single-instance Express API accepts a 300 MB JSON upload. During the ~8s JSON.parse, /health requests time out and the container is restarted by its orchestrator. What is the root cause?',
        options: [
          'Express queues at most one request at a time unless clustering is enabled',
          'JSON.parse is synchronous and CPU-bound, so it blocks the single event loop thread; no other request (including health checks) can be served until it finishes',
          'The kernel socket backlog overflows at payloads above 256 MB, dropping new connections',
          'V8 pauses all network I/O during large string allocations to run the garbage collector',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Node.js service registers process.on(‘uncaughtException’, err => logger.error(err)) and continues running. Why is this considered dangerous rather than resilient?',
        options: [
          'The handler disables the event loop timer phase, so scheduled jobs silently stop',
          'uncaughtException fires once per process; subsequent errors terminate without logging',
          'After an uncaught exception the process is in an undefined state — in-flight requests, locks, and resources may be corrupted or leaked — so the recommended pattern is to log, then exit and let a supervisor restart',
          'Catching it prevents Promise rejections from ever being reported anywhere',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Two admin users edit the same resource in a REST API; the last writer silently overwrites the first. Which HTTP mechanism implements optimistic concurrency control here?',
        options: [
          'Require Content-Range on PUT so partial updates never collide',
          'Use PATCH instead of PUT, since PATCH merges concurrent changes server-side',
          'Send Retry-After on every update so clients naturally serialize their writes',
          'Return an ETag with each GET; clients send If-Match on update, and the server rejects with 412 Precondition Failed if the current representation no longer matches',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A payments API sees duplicate orders when mobile clients retry POST /orders after network timeouts. The team cannot make order creation naturally idempotent. What is the standard remedy?',
        options: [
          'Clients send a unique Idempotency-Key header; the server stores the key with the first response and replays that stored response for retries with the same key',
          'Switch order creation to PUT, which HTTP guarantees to deduplicate at intermediaries',
          'Have clients wait for a 100 Continue before sending the body, eliminating ambiguous failures',
          'Return 202 Accepted so retries merely re-poll the queue instead of re-creating',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'An attacker sends a GraphQL query nesting friends { friends { friends { ... } } } 40 levels deep, exhausting the server. Which defense addresses this class of attack most directly?',
        options: [
          'Disable introspection so attackers cannot discover the recursive relationship fields',
          'Static query analysis before execution: enforce maximum depth and a computed complexity/cost budget, rejecting queries that exceed it',
          'Move the API behind a CDN so repeated deep queries hit the cache instead of resolvers',
          'Batch resolvers with DataLoader so the nested levels collapse into one database query',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A query that was fast is now slow. EXPLAIN shows the planner chose a sequential scan and its row estimate is 50x off from reality after a large bulk import. What should you try first?',
        options: [
          'Rebuild all indexes, since bulk imports fragment B-trees beyond planner tolerance',
          'Increase work_mem so the planner can afford to pick the index path',
          'Refresh the table’s statistics (e.g., ANALYZE), because cost-based planning depends on row-count/selectivity estimates that the import invalidated',
          'Add an index hint, because cost-based planners never revisit a cached plan choice',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Page 5000 of a paginated feed (OFFSET 100000 LIMIT 20) takes seconds while page 1 is instant, despite an index on the sort column. Why, and what is the standard fix?',
        options: [
          'The index only stores the first few thousand entries; partition the table by page number',
          'OFFSET forces a full re-sort of the table per request; adding a covering index fixes it outright',
          'Deep pages fall out of the buffer cache; the fix is to pre-warm the cache with a cron job',
          'The engine must walk and discard all 100,000 preceding rows before returning 20; switch to keyset (cursor) pagination using WHERE sort_col > last_seen ORDER BY sort_col LIMIT 20',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'SELECT * FROM users WHERE id NOT IN (SELECT user_id FROM banned) suddenly returns zero rows, even though most users are not banned. What is the classic cause?',
        options: [
          'banned.user_id contains a NULL; comparison with NULL yields UNKNOWN, so NOT IN can never evaluate to true for any row — NOT EXISTS avoids this',
          'The subquery result exceeded the IN-list size limit, so the predicate degraded to false',
          'NOT IN requires an index on the outer column; without one the optimizer returns an empty set',
          'The subquery runs in a separate snapshot that sees the banned table as containing all users',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Transaction A updates row 1 then row 2; transaction B updates row 2 then row 1, concurrently. Both hang briefly, then one receives a deadlock error. What actually happened and what should the application do?',
        options: [
          'The database escalated to a table lock and timed out; the app should retry with a smaller batch size',
          'The lock manager detected a cycle in the waits-for graph and aborted one transaction as the victim; the application should catch the error and retry the whole aborted transaction (ideally with consistent lock ordering to prevent recurrence)',
          'Both transactions were rolled back; the app must replay them inside a serializable transaction',
          'The victim’s statements were partially committed; the app must manually undo its first update',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In MongoDB, which condition makes a query fully covered by an index (no document fetch at all)?',
        options: [
          'The query planner selects an index whose first field matches the sort specification',
          'All filtered fields appear in the index and the collection has fewer documents than the index',
          'Every field in the filter and in the projection is contained in the index, and _id is explicitly excluded from the projection (when not in the index)',
          'The query uses $eq only; range operators always force a document fetch per match',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Users report that "logout" does not prevent a stolen JWT access token from being used until it expires. The tokens are stateless and verified by signature only. What is the practical mitigation pattern?',
        options: [
          'Re-sign the user’s tokens with a new secret at logout; old tokens fail verification globally',
          'Embed the session cookie inside the JWT so logout clears it transitively',
          'Have clients delete the token from storage; server-side action is unnecessary for stateless auth',
          'Keep access tokens short-lived (minutes) and pair them with revocable refresh tokens; check a server-side denylist/token-version claim for the small remaining window where needed',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A team debates storing the API access token in localStorage versus an HttpOnly, Secure, SameSite cookie. Which statement correctly characterizes the trade-off?',
        options: [
          'localStorage tokens are readable/exfiltratable by any XSS payload; HttpOnly cookies are not readable by scripts but are auto-attached to requests, so they reintroduce CSRF concerns needing SameSite/anti-CSRF tokens',
          'HttpOnly cookies are exposed to XSS while localStorage is script-isolated per tab',
          'localStorage is wiped on every navigation, so cookies are the only persistent option',
          'The two are equivalent security-wise; the choice affects only payload size limits',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'An API validates webhook signatures with: if (receivedSig === expectedSig) { ... } using ordinary string comparison. What is the vulnerability class and remediation?',
        options: [
          'Signature replay; include a random pepper in the string so comparisons never repeat',
          'A timing attack: === short-circuits at the first differing character, so response-time measurements can reveal the expected value byte-by-byte; compare fixed-length digests with a constant-time function like crypto.timingSafeEqual',
          'Unicode confusion; normalize with NFC before comparing to prevent forged equivalents',
          'Hash-length extension; switch from string comparison to comparing SHA-1 truncations',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A proxy cache once served a gzip-compressed API response to a client that did not send Accept-Encoding: gzip, breaking it. Which response header prevents this class of bug, and how?',
        options: [
          'Content-Encoding: identity, which forces caches to store only the uncompressed variant',
          'Cache-Control: no-transform, which forbids proxies from storing encoded responses',
          'Vary: Accept-Encoding, which makes caches key stored variants by that request header so each client gets the variant matching its own capabilities',
          'Transfer-Encoding: chunked, which is hop-by-hop and therefore never cached at all',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A response is served with Cache-Control: public, max-age=0, s-maxage=3600. How do a user’s browser and the CDN each treat it?',
        options: [
          'The browser caches it for an hour; the CDN revalidates every request (s-maxage applies to private caches)',
          'Neither caches it; max-age=0 always overrides s-maxage everywhere',
          'The CDN caches it for an hour but only for logged-out users; browsers ignore s-maxage and cache for a session',
          'The CDN (shared cache) may serve it for 3600s without revalidating, while the browser treats its copy as immediately stale and revalidates on each use',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'WebSocket connections fail with the client stuck on "101 expected, got 400" after moving the Node app behind an nginx reverse proxy; plain HTTPS routes work fine. What is the usual missing piece?',
        options: [
          'nginx must be configured to pass the upgrade handshake: proxy_http_version 1.1 with proxy_set_header Upgrade $http_upgrade and Connection "upgrade" (hop-by-hop headers are not forwarded by default)',
          'The Sec-WebSocket-Key must be re-hashed by nginx using the proxy’s TLS certificate',
          'WebSockets require HTTP/2 end-to-end, so nginx must enable http2_push',
          'The app must listen on a UDP port as well, since WS negotiates transport via QUIC first',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A WebSocket server keeps thousands of connections that appear open, but many clients silently died (mobile network drops); memory climbs and messages to dead peers back up. What is the standard detection mechanism?',
        options: [
          'Rely on TCP FIN packets; the OS always notifies the server the moment a peer disappears',
          'Application-level heartbeats: send ping frames periodically and terminate connections that miss pong responses within a deadline, since half-open TCP connections can persist indefinitely',
          'Set the HTTP Keep-Alive header timeout on the original upgrade request to 30 seconds',
          'Enable per-message deflate; compression errors reveal dead peers on the next send',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A "fetch image by URL" feature runs on an EC2 instance. An attacker submits http://169.254.169.254/latest/meta-data/iam/security-credentials/. What is the attack and its impact?',
        options: [
          'DNS rebinding; the attacker poisons the resolver cache for the app’s own domain',
          'An open-redirect chain; the attacker bounces users through the metadata IP to phishing pages',
          'SSRF against the cloud metadata service: the server fetches internal-only URLs on the attacker’s behalf and can leak temporary IAM credentials, enabling full cloud-account pivoting (mitigate with IMDSv2, egress allowlists, and blocking link-local ranges)',
          'A cache-poisoning attack that stores AWS credentials in the CDN edge cache',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A codebase uses parameterized queries everywhere, yet a pentest still finds SQL injection in a reporting endpoint that lets users pick the sort field. How is that possible?',
        options: [
          'The database driver downgrades parameterized queries to string concatenation under load',
          'Numeric parameters are not escaped by prepared statements, only string ones',
          'Second-order injection through stored procedures re-executing bound parameters',
          'Placeholders can only bind values, not identifiers or keywords — the ORDER BY column/direction was string-interpolated; the fix is to allowlist-map user input to known column names',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      }
    ],
    3: [
      {
        question: 'What happens?\nlet x = 1;\nfunction f() {\n  console.log(x);\n  let x = 2;\n}\nf();',
        options: [
          'Logs 1; the inner declaration only takes effect after its line executes',
          'Throws a ReferenceError: the inner let hoists x to the top of f’s scope but leaves it in the temporal dead zone until the declaration line, shadowing the outer x',
          'Logs undefined; let hoists and initializes to undefined exactly like var',
          'Logs 2; function bodies are evaluated after all their declarations run',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What is logged?\nconsole.log((‘b’ + ‘a’ + + ‘a’ + ‘a’).toLowerCase());',
        options: [
          '"baaa", because the extra + between strings is ignored by the parser',
          '"ba+aa", because the stray operator is preserved as a literal character',
          '"banana", because + ‘a’ is unary plus producing NaN, which stringifies to "NaN" before concatenation and lowercasing',
          'A SyntaxError, because two + operators cannot be adjacent',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What is logged?\nclass A {\n  constructor() { this.x = 1; }\n  get y() { return this.x + 1; }\n}\nconst copy = { ...new A() };\nconsole.log(copy.x, copy.y);',
        options: [
          '1 2, because spread copies the full prototype chain including accessors',
          'undefined undefined, because class instances are not spreadable',
          '1 2, because getters are evaluated and their results stored as data properties on prototypes too',
          '1 undefined, because object spread copies only own enumerable properties; the getter y lives on A.prototype and is not copied',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'TypeScript function:\nfunction applyDiscount(pct?: number) {\n  if (pct) {\n    return price * (1 - pct);\n  }\n  return price;\n}\nA valid discount silently stops working for one specific input. Which input and why?',
        options: [
          '0 — the truthiness check treats a legitimate 0 the same as undefined, so the branch is skipped; the guard should be pct !== undefined',
          '100 — numbers above 99 overflow the percentage type and narrow to never',
          'NaN — TypeScript narrows NaN out of number, making the branch unreachable',
          '-5 — negative numbers fail the implicit range check the compiler inserts for optional params',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'What is logged, in order?\nasync function a() {\n  console.log(1);\n  await null;\n  console.log(2);\n}\na();\nconsole.log(3);',
        options: [
          '1 2 3, because awaiting a non-promise value completes synchronously without yielding',
          '1 3 2, because the async body runs synchronously until await, which always yields to a microtask even for non-promise values, letting the outer sync code finish first',
          '3 1 2, because async function bodies are deferred to the microtask queue from the start',
          '1 3 followed by 2 on the next macrotask, because await null schedules a timer tick',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A team wraps an expensive calculation in useMemo and relies on it as a hard guarantee that the function runs exactly once per dependency change, using it to also increment an analytics counter. What is wrong with this?',
        options: [
          'useMemo only memoizes primitives; object results are recomputed every render regardless',
          'Nothing is wrong; useMemo is specified to invoke its factory exactly once per dependency set',
          'useMemo is a performance hint, not a semantic guarantee — React may discard the cache and recompute (e.g., offscreen re-renders, memory pressure), and the factory must stay side-effect free',
          'The analytics call makes the memo async, which forces React into legacy synchronous mode',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Inside a component body (not in an effect or handler):\nif (items !== prevItems) {\n  setPrevItems(items);\n  setSelection(null);\n}\nIs this legitimate React code, and what does React do with it?',
        options: [
          'It is a rules-of-hooks violation; setters may only be called from event handlers and effects',
          'It causes a hydration mismatch because state diverges between server and client passes',
          'It is legal but the update is silently postponed until the next external event triggers a render',
          'It is the supported "render-phase update" pattern for derived state: React discards the current render output and immediately re-runs the component with the new state before touching the DOM — but it must be guarded by a condition or it loops infinitely',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'After upgrading to React 18, an app still calls ReactDOM.render(<App />, container). What is the consequence?',
        options: [
          'The app runs in legacy mode: a deprecation warning is logged and React 18 features (automatic batching everywhere, transitions, concurrent rendering) are unavailable until createRoot is used',
          'The app fails to compile, because ReactDOM.render was removed from the package',
          'It behaves identically to createRoot; the two entry points are aliases in React 18',
          'The app renders but all useEffect hooks are skipped until the first user interaction',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'useEffect(() => {\n  const id = setInterval(() => setCount(count + 1), 1000);\n  return () => clearInterval(id);\n}, []);\nThe counter goes 0 to 1 and then stops. Which fix works WITHOUT recreating the interval every tick?',
        options: [
          'Add count to the dependency array; the interval identity is preserved automatically by React',
          'Use the functional updater setCount(c => c + 1), which reads the latest state at update time instead of the stale count captured by the one-time closure',
          'Wrap setCount in useCallback so the closure re-binds to fresh state on each render',
          'Move the setInterval call outside useEffect so it sees live component variables',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'In the main module of a Node.js script (not inside an I/O callback), you schedule setTimeout(fn, 0) and setImmediate(fn2). What is the execution order?',
        options: [
          'setTimeout always first, because the timers phase precedes the check phase in every iteration',
          'setImmediate always first, because check-phase callbacks preempt fresh timers',
          'Nondeterministic: it depends on whether the loop enters the timers phase before or after the 1ms timer threshold is reached, which varies with process startup timing',
          'They run simultaneously on separate threads from libuv’s pool',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Node process crashes with an uncaught ‘error’ exception from a Readable stream, even though every read() call is wrapped in try/catch. Why does try/catch not help here?',
        options: [
          'Stream errors are thrown on the libuv thread pool, outside V8’s exception machinery',
          'try/catch only captures errors from object-mode streams, not binary ones',
          'read() re-throws asynchronously by design unless highWaterMark is zero',
          'Stream failures are emitted asynchronously as ‘error’ events on the EventEmitter; with no ‘error’ listener attached, Node throws the error globally — synchronous try/catch around read() never sees it',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'Login sessions randomly vanish after a team enables the cluster module with 4 workers; sessions were stored in an in-process Map keyed by session ID. What is happening?',
        options: [
          'Each worker is a separate process with its own memory, and successive requests from one user land on different workers that lack the session entry — fix with a shared store (Redis) or sticky sessions',
          'The cluster module clears process memory on every fork rebalance cycle',
          'Workers share the Map but its keys collide because process IDs are prepended per worker',
          'Session cookies are stripped by the primary process when proxying to workers',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Node API must compute expensive image transforms (pure CPU, shared read-only lookup tables) without blocking request handling. Why choose worker_threads over cluster for this?',
        options: [
          'cluster cannot run CPU-bound code at all; its workers only handle network I/O',
          'worker_threads run inside one process and can share memory via SharedArrayBuffer/transferable ArrayBuffers, making them suited to parallel CPU work on shared data, while cluster forks whole processes aimed at scaling connection handling',
          'worker_threads bypass the event loop entirely, so transforms finish with lower latency than any process-based option',
          'cluster workers cannot use native addons, which image processing requires',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A client updates a resource with PUT and If-Match: "abc123", but another client changed the resource moments earlier. What response should a correctly implemented server return?',
        options: [
          '409 Conflict with a Location header pointing at the newer representation',
          '304 Not Modified, instructing the client to retry with its cached copy',
          '412 Precondition Failed, because the current entity tag no longer matches the one the client’s update was based on',
          '428 Precondition Required, signaling that a fresh If-Match must accompany the retry',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A REST endpoint kicks off a video-transcode job that takes ~5 minutes. Holding the HTTP connection open times out at the load balancer. Which is the canonical REST pattern for this?',
        options: [
          'Return 100 Continue immediately, then push the final result over the same connection',
          'Return 301 to a result URL; clients follow the redirect after the job completes',
          'Keep the connection alive with chunked keep-alive whitespace until the job finishes',
          'Return 202 Accepted with a status resource URL (e.g., /jobs/123); the client polls it (or receives a webhook) and the status resource links to the final result when done',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A GraphQL platform team adopts persisted queries (clients send a query hash instead of full query text). Which pair of benefits is this primarily buying?',
        options: [
          'Smaller request payloads and an execution allowlist — the server runs only pre-registered queries, shrinking the attack surface for arbitrary expensive queries',
          'Automatic schema versioning and resolver-level authorization without custom code',
          'Server-side result caching with automatic invalidation on any mutation',
          'Elimination of the N+1 problem, since persisted queries are compiled to single SQL statements',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'An index exists on orders(created_at), yet this stays a sequential scan:\nWHERE created_at + interval ‘1 day’ > now()\nWhat is the fix?',
        options: [
          'Add created_at to an INCLUDE clause, since the planner ignores key columns inside expressions',
          'Rewrite the predicate so the bare column is compared to a computed constant — WHERE created_at > now() - interval ‘1 day’ — because wrapping the column in an expression prevents a plain index lookup (making it sargable)',
          'Cast now() to timestamptz explicitly; the implicit cast disables index range scans',
          'Use BETWEEN instead of >, since open-ended ranges cannot use B-tree indexes',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'On PostgreSQL, SELECT COUNT(*) FROM big_table takes many seconds even though the primary key index is small and cached. What is the fundamental reason?',
        options: [
          'COUNT(*) always parses every column of every row, so wide tables count slowly',
          'Postgres serializes all aggregate queries through a single background worker',
          'Under MVCC, row visibility is per-snapshot, so tuples must be visibility-checked — requiring heap access unless the visibility map allows an index-only scan for mostly-vacuumed pages',
          'The planner never uses indexes for aggregates without a WHERE clause by specification',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A PostgreSQL database bloats and queries degrade over days; investigation finds a monitoring connection holding a transaction open ("idle in transaction") since last week. What is the mechanism of damage?',
        options: [
          'The idle transaction holds an ACCESS EXCLUSIVE lock that queues behind every write',
          'Its WAL segment cannot rotate, so the disk fills with unarchived logs',
          'Each idle transaction pins one CPU core for snapshot maintenance',
          'Its old snapshot forces VACUUM to keep every dead tuple created since it began (they might still be visible to it), so garbage accumulates in tables and indexes across the database',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A production Redis (single instance, ~50M keys) intermittently stalls for seconds; slowlog shows a dashboard running KEYS "user:*:pending". Why is this so harmful, and what is the correct alternative?',
        options: [
          'KEYS is O(N) over the entire keyspace and Redis executes commands on a single thread, so everything else blocks until the scan completes; use cursor-based SCAN with MATCH to iterate incrementally',
          'KEYS acquires a global write lock that persists until the next BGSAVE completes; disable RDB snapshots',
          'KEYS decompresses every value to test the pattern; store keys uncompressed instead',
          'KEYS forks the process like BGREWRITEAOF; the stall is copy-on-write memory pressure, fixed by more RAM',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A sharded time-series store uses the event timestamp as the shard/partition key. Write throughput does not improve as shards are added, and one shard always runs hot. Why?',
        options: [
          'Timestamps hash unevenly because their low bits repeat every second',
          'A monotonically increasing key routes all current writes to the same "latest" shard/partition, creating a hot spot; adding shards adds only idle capacity — fix by prefixing/hashing the key or compounding it with a high-cardinality field',
          'Time-series data compresses poorly, so the first shard fills its disk before others receive data',
          'Range queries lock the newest shard for reads, starving its write queue',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'An auth system uses refresh token rotation: each refresh issues a new refresh token and invalidates the old one. The server then sees an already-rotated (old) refresh token being presented. What should it conclude and do?',
        options: [
          'The client clock is skewed; respond with the current server time and let the client retry',
          'The rotation job failed; re-issue the same refresh token and extend its expiry',
          'The token was likely stolen and replayed (either the attacker or the legitimate user holds a superseded token); revoke the entire token family/session, forcing re-authentication',
          'A concurrent refresh race occurred; silently accept the old token once and issue a duplicate chain',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'An OAuth2 authorization server accepts any redirect_uri whose prefix matches the registered value (https://app.example.com). An attacker registers https://app.example.com.evil.io and also finds an open redirect at https://app.example.com/out?url=. What is the robust server-side defense?',
        options: [
          'Require the state parameter, which cryptographically pins the redirect target',
          'Only issue authorization codes over POST so redirects cannot leak them in Referer headers',
          'Use fragment (#) responses, since fragments are never sent to the redirect host',
          'Enforce exact string matching of redirect_uri against pre-registered URIs — prefix or substring matching enables both lookalike-domain and open-redirect exfiltration of codes',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'An API "protects" against CSRF solely by requiring Content-Type: application/json on state-changing endpoints (no tokens, no SameSite). Why does this offer real (though incomplete) protection?',
        options: [
          'HTML forms can only submit a limited set of content types cross-site; sending application/json via fetch triggers a CORS preflight the attacker’s origin will fail — though this collapses if any endpoint accepts form-encoded fallbacks or the CORS policy is lax',
          'Browsers block all cross-site requests carrying a JSON body at the network layer',
          'JSON bodies cannot encode attacker-controlled values without breaking their signature',
          'The JSON parser rejects requests missing the X-Requested-With header automatically',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Node service deep-merges user-supplied JSON into a config object: merge(config, JSON.parse(body)). An attacker sends {"__proto__": {"isAdmin": true}}. What happens if merge copies keys recursively without guards?',
        options: [
          'Nothing; JSON.parse strips __proto__ keys during parsing per the JSON spec',
          'Object.prototype gets an isAdmin=true property, so EVERY object in the process now inherits isAdmin — prototype pollution that can flip auth checks like if (user.isAdmin); guard by rejecting __proto__/constructor/prototype keys or using null-prototype objects',
          'The process throws immediately, because __proto__ is read-only on frozen objects',
          'Only the config object gains isAdmin, which is a bug but scoped to config consumers',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'An endpoint builds a report:\nfor (const id of userIds) {\n  results.push(await db.getUser(id));\n}\n200 IDs make the endpoint take ~6s; each query takes ~30ms and the queries are independent. What is the highest-impact fix?',
        options: [
          'Move the loop into a worker thread so the event loop stays free while total time stays 6s',
          'Add an index to the users table; sequential awaits are fine once each query is sub-millisecond',
          'Issue the independent queries concurrently — e.g., Promise.all(userIds.map(id => db.getUser(id))) (bounded by pool size) — collapsing 200 sequential round trips into parallel ones',
          'Enable HTTP keep-alive on the response so the client stops timing out at 5 seconds',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Node service leaks memory: heap snapshots show a Map named pendingRequests growing without bound, keyed by request ID, holding response objects. Entries are deleted in the success handler. Where is the leak?',
        options: [
          'Maps in V8 never release deleted entries; switch to a plain object keyed by symbol',
          'Response objects are pinned by the HTTP agent until keep-alive expires, not by the Map',
          'Request IDs are numbers, and numeric Map keys are interned permanently by the runtime',
          'Failure paths (errors, timeouts, client disconnects) never delete their entries, so every non-success request leaks — cleanup must run in a finally/close path covering all outcomes, ideally with a TTL sweep as backstop',
        ],
        correctAnswer: 3,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'A Dockerfile does:\nCOPY package.json package-lock.json ./\nRUN npm ci\nCOPY . .\nWhy this specific ordering instead of a single COPY . . followed by npm ci?',
        options: [
          'Each instruction’s cache is invalidated when its input files change; copying only the manifests first means source-code edits reuse the cached npm ci layer, so dependencies are reinstalled only when the lockfile actually changes',
          'npm ci refuses to run if application source files are present in the working directory',
          'COPY . . before npm ci would bake node_modules from the host into the image, which Docker forbids',
          'Two COPY layers compress better, halving the final image size regardless of caching',
        ],
        correctAnswer: 0,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'An image builds and runs fine locally, but the production container (built with NODE_ENV=production) crashes at startup: Cannot find module ‘ts-node’. What is the most likely explanation?',
        options: [
          'Alpine images strip TypeScript support from the Node binary at build time',
          'npm ci (or install) with production settings omits devDependencies, and the runtime entrypoint still depends on a package (ts-node) declared as a devDependency — either compile ahead of time in a build stage or move the dependency where it belongs',
          'NODE_ENV=production makes require() resolve only built-in modules for security',
          'The lockfile pins ts-node to a version that only publishes macOS binaries',
        ],
        correctAnswer: 1,
        difficulty: 'hard',
        points: 1,
      },
      {
        question: 'During a rolling deployment, the new release’s migration drops a column the old release still selects; old pods crash-loop until the rollout finishes, causing a partial outage. Which practice prevents this class of failure?',
        options: [
          'Run migrations after all pods are replaced by pausing the app’s readiness probe during rollout',
          'Use blue-green deployments only, since rolling deployments are incompatible with any schema change',
          'Expand/contract (parallel change) migrations: every schema change stays backward-compatible with the currently running release — add and backfill first, migrate code, and only drop the column in a later release once nothing reads it',
          'Pin each pod to its own database schema version using connection-string routing',
        ],
        correctAnswer: 2,
        difficulty: 'hard',
        points: 1,
      }
    ],
  },
};
